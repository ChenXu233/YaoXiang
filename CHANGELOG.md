# Changelog

## :bookmark: V0.8.1: 泛型单态化加固、内存暴涨根治与 std 统一

> 发布日期: 2026-09-20

### 📦 版本信息

| 项目     | 值                  |
| -------- | ------------------- |
| 发布日期 | 2026-09-20          |
| 版本变更 | `0.8.0` → `0.8.1`   |
| 提交数   | 152 个 commit       |

### 📋 本次更新概要

本次发版以稳定性为核心：根治了 Heap 分配注册表导致引用计数永不归零的内存暴涨缺陷，系统性修复了泛型单态化的类型参数对齐、漏特化与构建不确定性问题。语言功能侧落地命名参数、`Vec(Int)()` 内置容器构造（RFC-011）、unsafe 块全链路（#347）与元组下标 `t.0`；std 内建 List 统一到 Vec 并硬切换为纯 yx 实现。同步完成 middle 层与解释器的大规模重构（LocalSlot 槽位、ops 指令族拆分），并修复 std 文件句柄、clamp panic、入口 main 校验等 12 个已报告 issue。

### ✨ 新功能

#### 命名参数与显式类型实参

函数调用体验完善：命名参数从构造器扩展到普通函数与限定名调用，文档规范同步修正（冒号→等号文法）。泛型调用支持显式类型实参 `mk(Int)(5)`，并落地 RFC-011 内置容器构造 `Vec(Int)()`，容器创建不再需要绕行。

- 命名参数支持普通函数与限定名调用
- 显式类型实参调用 `mk(Int)(5)`
- 内置容器构造 `Vec(Int)()`（RFC-011）
- 语言规范 §2.3 命名实参文法修正

#### std 内建 List 统一到 Vec

内建 List 与 Vec 语义统一，`std.list` 硬切换为纯 YaoXiang 源码实现，摆脱对内建特殊路径的依赖。容器命名分层 Array/Vec/List 在 RFC 中定稿，`Vec(T).length` 赋值支持同步移植。

- 内建 List 统一到 Vec
- `std.list` 纯 yx 实现硬切换
- `Vec(T).length` 赋值支持

#### unsafe 块全链路（#347）

unsafe 块从"语法能写、语义不可用"补齐为完整特性：块内允许类型定义并提升到外层作用域，值出口（尾表达式/return）与语句位置修正，类型定义绑定打通 codegen——RFC-010 示例现在可以实际运行。

- unsafe 块的值出口与语句位置修正
- 块内类型定义提升与外层绑定
- RFC-010 示例端到端可用

#### 泛型类型实例展开与函数值

泛型类型实例在方法体内可访问字段，`L(T)` 注解不再被错误展开为 Struct。函数名可作为一等值传递，括号语义正式区分柯里化调用与返回函数。

- 泛型类型实例展开，方法体访问字段
- 函数名作一等值，修正 const 泛型误判
- 括号语义区分柯里化与返回函数

#### `{}` 语义统一与元组下标

空块 `{}` 二义性按 B 方案定案：空块 + 内容决定类型（#359），消除与 Dict 字面量的冲突。元组下标 `t.0` 全链路打通（词法/语法/类型/IR/执行），并支持引用接收者，`list.yx` 补齐迭代器。

- `{}` 空块语义统一（B 方案）
- 元组下标 `t.0` 全链路 + 引用接收者
- `list.yx` 迭代器补齐

#### 调试可见性（.42 调试段 v2）

字节码调试能力大幅增强：`dump` 现在显示局部变量名与指令源码位置（ip→span 可见），越界错误直接指出变量名。局部变量名写入 IR 槽位，语句级 span 侧表补全指令位置覆盖。

- dump 显示局部变量名与指令源码位置
- 越界错误指出变量名
- 索引赋值与容器长度读取，补齐 String 指令解码

#### 顶层绑定与 Script 模式

顶层绑定重构为全局槽位 + 运行时初始化，支持前向引用。Script 模式入口语义明确：顶层语句即程序，`main` 不再被隐式调用。

- 顶层绑定全局槽位 + 运行时初始化 + 前向引用
- Script 模式：顶层语句即程序

#### nightly 多语言 shootout 基准

nightly CI 增补多语言 shootout 基准，结果自动写入 release notes，YaoXiang 改跑 `.42` 字节码使读数与其他语言可比。

### 🐛 Bug 修复

#### 内存暴涨根治（Heap 分配注册表）

Heap 的分配注册表让所有分配的引用计数永不归零，长时间运行的程序内存持续暴涨。移除注册表后引用计数正确归零，内存随作用域释放。

- 移除 Heap 分配注册表
- 引用计数恢复正确归零

#### 泛型单态化系列

单态化器的系统性修复：类型参数按声明序对齐（此前按使用序会错位），特化函数按名排序发射消除构建不确定性，被按名调用的 std 泛型函数不再被误删，跨模块调用单态化恢复只读自动借用（RFC-009 §2.8）。值位置类型构造器白名单收窄至 Vec/Array，防止非法构造。

- 类型参数按声明序对齐，泛型函数不再漏特化
- 特化函数按名排序发射，构建结果确定
- std 泛型函数按名调用保留
- 跨模块单态化 + 只读自动借用恢复
- 值位置构造器白名单收窄至 Vec/Array

#### 类型检查与尾表达式语义

落地 RFC-010a 尾表达式语义并修正系列检查缺口：`if`/`match` 等组合表达式作尾表达式时不再跳过类型检查（#354），尾表达式类型不符改用 E1012 并指向注解（#353），函数值隐式 Dup 不再误报消耗（#352），三条早退路径补齐返回类型校验。赋值语义按 spec §4.3 落地——块作用域隔离 + 三个诊断；字段赋值不再被误报为变量重赋值（E2010）。

- 组合表达式尾表达式类型检查补齐（#354）
- 尾表达式诊断改 E1012 并指向注解（#353）
- 函数值隐式 Dup（#352）
- 块作用域隔离的赋值语义 + 字段赋值误报修复
- 箭头 lambda 返回类型取载荷
- 返回位类型参数与实参统一

#### spawn 尾表达式与并发（#365）

spawn 块尾表达式出口不可用、值丢失或报 E3006 的问题修复，spawn 块现在正确产出尾表达式值。

- spawn 块尾表达式出口可用（#365）

#### std 库修复（#337–#340）

文件句柄改按引用传递，`open → write → close` 流程可编译可用（#337）；`clamp` 在 `min > max` 时不再 panic 掉解释器进程（#339）；DateTime 别名化导出可从源码调用（#338）；`parse_time` 尊重 fmt 参数、年月日计算修正（#340）。`pop` 空列表行为与 list.md 文档同步。

- 文件句柄按引用传递（#337）
- clamp 不再 panic（#339）
- DateTime 导出可用（#338）+ parse_time 尊重 fmt（#340）
- pop 空列表与文档同步

#### 入口与前端校验

入口 `main` 签名校验——带参数的 main 不再被零参调用产生未初始化值（#357）；顶层函数重复定义新增诊断，同名函数不再都进函数表（#358）。

- main 签名校验（#357）
- 顶层函数重复定义诊断（#358）

#### 字节码编解码（middle）

补齐 24 个 opcode 的解码分支，未知 opcode 不再静默变 Nop；`size()` 表中 7 个指令的错误长度修正；新增编解码漂移护栏测试覆盖 40 个指令往返。

- 24 个 opcode 解码分支补齐
- size() 表 7 个指令长度修正
- 40 指令往返漂移护栏

#### 运行时与后端

`to_bool` 不再把 Int 当真值（仅接受 Bool）；跳转偏移加校验，消除原地死循环与负目标越界；`GetField` 不再静默留 Void，集合越界显式报错；用户类型名不再被 std 同名常量遮蔽。

- to_bool 仅接受 Bool
- 跳转偏移校验
- GetField/集合越界显式报错
- 用户类型名遮蔽修复

#### shootout 基准套件修复

基准套件从"静默空跑"修复为真实可用：源码静默空跑与路径错误修正、YaoXiang 改跑 `.42` 字节码、matrix 基准改用输入规模、`.exe` 后缀判定修正。

- shootout 源码静默空跑 + 路径错误修复
- matrix 基准改用输入规模
- 基准与现行语义兼容性修复

### ♻️ 重构优化

#### 解释器 ops 指令族拆分

`execute_instr` 巨型匹配按指令族拆分为 ops 模块，每条指令族独立文件，解释器核心循环可读性与可维护性显著提升。

#### middle 层表达式生成器外提

ir_gen 表达式生成器全部外提（28/28 臂），locals 迁移为 LocalSlot 槽位，span 信息落到指令本身、拆除语句级侧表。为 RFC-034 前置的 ir_gen 结构治理铺路。

- 表达式生成器 28/28 臂外提
- LocalSlot 槽位迁移
- span 落到指令本身

#### 并发运行时去裸指针

并发任务改共享 `Arc<Image>`，删除 `SendPtr` 裸指针，帧原地驻留修复调用链丢失问题。并发路径的内存安全性从"人工保证"升级为类型系统保证。

### ⚡ 性能优化

- 解释器拆分只读 Image，Frame 改持 func_id，调用开销降低
- 帧原地驻留，修复调用链丢失
- `CallStatic` 标签按需构造
- `CreateVariant` 不再解析后丢弃组名
- 新增解释器热路径基准，编译与执行段分离

### 📝 文档

- RFC-010a 尾表达式求值与 return 语义定稿
- RFC-014 核心包源互斥决议落章；RFC-011b 运算符重载草案
- stdlib API 参考文档落地
- 「勘误」块全部取消——修订直接融入正文并立为规则
- 语言规范命名实参文法、教程语法同步修正
- 大量文档自动翻译与 locale 文件同步

### 🔧 其他变更

- 死代码清理：RegisterFile、called_func 字段、expect_list 等
- clippy 门禁阻塞项清零
- 依赖升级：rustls TLS 通告修复、npm 依赖组合升级、文档构建链 6 项安全告警消除
- 测试合规：AAA 分段 + 语料惯例对齐
