# Changelog

## :bookmark: V0.7.12: 模块系统、测试框架与静默错误根治

> 发布日期: 2026-08-07

### 📦 版本信息

| 项目     | 值                  |
| -------- | ------------------- |
| 发布日期 | 2026-08-07          |
| 版本变更 | `0.7.11` → `0.7.12` |
| 提交数   | 99 个 commit        |

### 📋 本次更新概要

本次发版是三线并进：**模块系统落地**（RFC-029 多文件项目编排、use 条目别名、符号表统一）、**测试基础设施成型**（RFC-036 `yaoxiang test` 命令 + std.test 断言库 + `[test:error]` 编译错误判定）、以及**静默错误系统性根治**（#251 泛型调用丢值、#261 顶层绑定初始化丢值、#255 泛型构造器误路由、#266 &mut 令牌写通路缺失、#271#1 构造器缺参静默填 0 等 P1 修复全部闭环）。同时伴随所有权检查器接入证明管线（#265）、精化约束静默丢失洞（#263）、语句终止规则（RFC-038）、元组支持（#253）等一批语法与类型系统修复，以及符号表/IR DefId 化/字节码 vtable 化的大规模重构。

### ✨ 新功能

#### 模块系统（RFC-029 多文件项目）

实现了多文件项目编译编排：模块发现沿 use 追踪、双根解析，IR 按 DefId 引用、codegen 按 DefId 分发，编译期 vtable 写入字节码。配合统一的符号表（SymbolTable 共享 + 名字解析器），模块限定名、整体导入、use 条目别名（`use lib.{x as y}`，#245）全部落地。第三方包导入（#246）与 RFC-014 系列作为后续里程碑继续推进。

- 多文件项目编译编排与模块发现沿 use 追踪（#247）
- use 条目别名（#245）
- IR 按 DefId 引用、codegen 按 DefId 分发（Stage 3）
- 编译期 vtable 写入字节码，删除运行时前缀扫描

#### 测试基础设施（RFC-036）

`yaoxiang test` 命令 Phase 1 落地：发现 → 逐文件子进程执行 → exit code 判定 → 汇总报告。配套 std.test 断言库（纯 YaoXiang 自举实现）与嵌入源加载机制。#94 标准库测试覆盖从阻塞变为可行。

- `yaoxiang test` 命令（#95，RFC-036）
- std.test 断言库与嵌入源加载机制（#94，RFC-036）
- `[test:error]` 文件头标记：编译错误测试反向判定，06-compile-errors 10 个欠账清零

#### 语句终止规则（RFC-038）

实现换行终止语句、行首 `(`/`[` 不并入上一行、括号内换行续行、行尾运算符续行（Swift 式）等语句终止规则。

#### 元组支持（#253）

实现元组字面量与元组返回类型，配套 '?' 参数类型错误检测（E0012）。

#### dump 支持直接 dump .42 字节码

`yaoxiang dump` 可直接 dump 字节码文件（内容即身份分流），不再要求源码路径。

### 🐛 Bug 修复

#### 静默错误根治（P1 系列）

本次发版核心主题：编译器内部用 0 当"不知道/失败"替身的反模式系统性修复。

- **#261 顶层绑定初始化丢值**：`sum = 1+2+3` 被编译成返回 0 的函数——`eval_const_expr` 补 BinOp/UnOp 折叠
- **#255 泛型构造器误路由**：泛型类型构造器与泛型函数同文件时被误当 native call（E6006）——build_output 保留泛型 TypeDecl，extract_concrete_type_args 只收集 TypeVar 位
- **#266 &mut 令牌写通路缺失**：`q.shift(1,1)` 不写回、`m.shift(...)` 报 E6006、`m.x = 100` 静默无效——Assign FieldAccess 生成 StoreField、方法派发穿透 &mut、删死字段 function_param_types
- **#271#1 构造器缺参静默填 0**：`Point(5)` 缺参静默 0——typecheck 补参数个数检查（位置/命名参数、函数缺参/超参），泛型与 lambda 豁免

#### 类型系统修复

- **#265 所有权检查接入证明管线**：move/借用冲突检查由独立 pass 接入依赖类型证明管道
- **#263 精化约束静默丢失洞**：谓词/证明函数非法用法报 E1092，诊断结构化（消灭 reason 自由文本）
- **#268 check 命令透传类型诊断**：E8001 仅作兜底
- **#270 E1002 补源码定位 span**
- **#262 类型级比较不可判定时悬置**而非伪造真值
- **#256/#260 类型驱动分类根治**：&T 读取令牌按 Dup 复制而非 Move
- **#257 拒绝 &mut T 令牌赋值复制**（Linear 语义）
- **#251 P0-1 类型参数层编译期擦除**，泛型单次调用返回正确值
- 解析/词法错误透传诊断而非包装 E8001（#251 同类，禁止 match _ 静默吞错）

#### 代码生成与语法

- 逻辑非改回 Zig 式（`!` + and/or 关键字），逻辑运算符权威化为 and/or/not（#251）
- 多文件 --debug-info 运行时错误源码定位（#252）

### ♻️ 重构优化

#### 符号表与名字解析统一

引入项目级符号表随模块登记增量构建，名字解析器统一命名空间解析，模块限定名所有权移入 IR 生成——消除了 ir_gen 与 typecheck 之间重复的模块命名空间推导。

- 符号表承载绑定类别归属与静态 vtable
- 注册表拥有项目级符号表
- 限定名所有权移入 IR 生成，删除限定后置 pass

#### IR 与字节码体系

- IR 按 DefId 引用，codegen 按 DefId 分发（Stage 3）
- 编译期 vtable 写入字节码，删除运行时前缀扫描
- vtables 段携带函数表索引，加载期零解析
- CallStatic 按函数表索引分发，删除按名分发
- 删除 ModuleIR 三个死映射（mut_locals/loop_binding_locals/local_names）

#### 依赖与代码清理

- 移除未使用依赖（once_cell/atomic/ron 等）并迁移至标准库同步原语
- 删除未实现的 FullRuntime 和 work_stealing 机制
- 清理 backends/common 死代码、未使用 trait 与函数
- 移除位置式条目别名、废弃的 task.rs 模块
- 全局 `_ => None` 静默兜底审计（88 处，修复确认项）

### 🔧 其他变更

- RFC-038 语句终止规则（已接受）、RFC-036 测试框架（已接受）、RFC-029 模块语义草案晋升已接受
- 文档自动翻译、VitePress lastUpdated 时效标记、CI 文档时效性检查
- 依赖组更新（toml、npm 组 13 项）
- ponytail 过度工程全仓审计报告（#269）
