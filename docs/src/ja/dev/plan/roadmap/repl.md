---
title: "REPL 状態"
---

# REPL

> **モジュール状態**：未完（3 項目改善待ち）
> **位置**：`src/backends/dev/repl/`
> **最終更新**：2026-06-01

---

## モジュール概要

REPL（Read-Eval-Print Loop）モジュールは対話型プログラミング環境を提供する。trait 抽象アーキテクチャを採用しており、異なるバックエンド実装をサポートしている。

**コード量**：1,037 行（8 ファイル）

---

## 機能一覧

### REPLBackend trait（backend_trait.rs）

- ✅ `eval()` 求値
- ✅ `complete()` 補完候補
- ✅ `get_symbols()` シンボル一覧
- ✅ `get_type()` 型クエリ
- ✅ `clear()` 状態クリア
- ✅ `stats()` 実行統計

### 求値エンジン（engine/evaluator.rs - 299 行）

- ✅ コードコンパイル実行
- ✅ 括弧/引用符整合性検出
- ✅ 式/文の自動ラップ
- ✅ バイトコードからの定義抽出

### 実行コンテキスト（engine/context.rs - 168 行）

- ✅ 変数定義/クエリ
- ✅ 関数定義/クエリ
- ✅ シンボル型クエリ
- ✅ 実行統計

### コマンドシステム（commands/mod.rs - 95 行）

- ✅ `:quit/:q` 終了
- ✅ `:help/:h` ヘルプ
- ✅ `:clear/:c` クリア
- ✅ `:type/:t` 型表示
- ✅ `:symbols/:info` シンボル一覧
- ✅ `:stats` 統計
- ⚠️ `:history` コマンド — **未実装**（ヒント出力のみ）

### セッション REPL（session/mod.rs - 247 行）

- ✅ rustyline 統合
- ✅ 複数行入力サポート
- ✅ 履歴保存/読込
- ✅ VI/Emacs 編集モード
- ✅ ファイル読込実行
- ✅ カスタム設定

### 自動補完（session/completer.rs - 126 行）

- ✅ キーワード補完
- ✅ 変数/関数補完
- ✅ 組み込み関数補完

---

## テストカバレッジ

**0 件のユニットテスト**

REPL モジュールにはテストコードが一切存在しない。ディレクトリ `src/backends/dev/repl/` 全体に渡って `#[test]` や `#[cfg(test)]` アノテーションが一切ない。

---

## コード品質評価

| 次元 | スコア | 説明 |
|------|--------|------|
| 未完了事項 | 3 | テスト補完、:history コマンド、境界条件テスト |
| テストカバレッジ | 0% | テストなし |
| ドキュメント品質 | 良好 | 完全なユーザーガイド（`docs/src/guide/repl.md`、436 行）とコードコメントあり |
| アーキテクチャ設計 | 優秀 | trait 抽象化、階層分明確、拡張性あり |

---

## 統合状態

REPL は以下のコンポーネントに統合済み：

1. **DevShell**（`src/backends/dev/shell.rs`）：`:repl` コマンドで REPL モードに切り替え
2. **モジュールエクスポート**（`src/backends/dev/mod.rs`）：`SessionREPL`, `Evaluator`, `REPLBackend` をエクスポート
3. **CLI エントリポイント**：`yaoxiang repl` または `yaoxiang` で起動

---

## 改善事項

1. **ユニットテストの補完**（テストゼロは最大の問題点）
2. **`:history` コマンドの実装**
3. **境界条件テストの補完**