---
title: "フォーマッターの状態"
---

# フォーマッター (Formatter)

> **モジュール状態**：安定（改善項目 0 件）
> **位置**：`src/formatter/`
> **最終更新**：2026-06-04

---

## モジュール概要

フォーマッターは YaoXiang ソースコードを自動的にフォーマットする役割を担います。式、文、型など、完全な AST ノードのフォーマットをサポートしています。

**コード量**：2,397 行（ソースファイル 19 個）

---

## 機能一覧

### 実装済みの機能

**式のフォーマット**（handlers/expr.rs、すべての Expr バリアントをカバー）：
- ✅ リテラル：Int / Float / Bool / Char / String（エスケープ含む）
- ✅ 変数参照
- ✅ 二項演算子 / 単項演算子（行幅超過時の自動折り返しを含む）
- ✅ 関数呼び出し（名前付き引数を含む）
- ✅ 関数定義（式形式の fn def）
- ✅ if / elif / else
- ✅ match 式（パターンの整列、長すぎる場合は折り返し）
- ✅ for / while ループ（ラベルを含む）
- ✅ コードブロック（注釈保持、行末コメント処理を含む）
- ✅ return / break / continue
- ✅ 型変換（as）
- ✅ タプル / リスト / リスト内包表記 / 辞書
- ✅ インデックス / フィールドアクセス（チェーン呼び出しの折り返し）
- ✅ try 演算子（?）
- ✅ ref / borrow（& / &mut）
- ✅ unsafe ブロック
- ✅ spawn ブロック
- ✅ ラムダ式（単一式簡潔形式）
- ✅ f-string（フォーマット仕様を含む）
- ✅ Error ノード（`/* error */` プレースホルダーの挿入）

**文のフォーマット**（handlers/stmt.rs）：
- ✅ 変数宣言（mut / 型注釈 / 初期化式）
- ✅ for ループ文
- ✅ 統一バインディング文（関数 / 型 / メソッドバインディング）
- ✅ use インポート文（items、alias を含む）
- ✅ if 文
- ✅ 外部バインディング文（External / Anonymous / DefaultExternal）

**型のフォーマット**（handlers/types.rs、すべての Type バリアントをカバー）：
- ✅ 基本型：Int(size) / Float(size) / Char / String / Bytes / Bool / Void
- ✅ 名前付き型 / 構造体 / 名前付き構造体
- ✅ Union / Enum / Variant
- ✅ タプル / 関数型 / Option / Result
- ✅ ジェネリクス / 関連型 / Sum 型
- ✅ リテラル型 / 参照型 / ポインタ型 / MetaType

**その他の機能**：
- ✅ 区切りリストの自動折り返し（handlers/delimited.rs）
- ✅ コメント保持（source_map.rs）
- ✅ インポート並び替え（rules/sort_imports.rs）
- ✅ CLI コマンド：check / write / stdout モード（command.rs）
- ✅ 設定オプション：line_width / indent_width / use_tabs / single_quote / sort_imports（options.rs）

---

## 完全には実装されていない仕様

| 仕様 | 状態 | 差異説明 |
|------|------|----------|
| §2.2 折り返し戦略の優先度 | ✅ 実装済み | 優先度チェーンを完全に実装 |
| §4.2 引数リストの折り返し（末尾カンマ） | ✅ 実装済み | 引数リストが行幅を超えた場合に自動折り返しし、末尾カンマを使用 |
| §6.2 単一行コードブロック | ✅ 実装済み | 単一文のコードブロックで行幅を超えない場合は単一行形式を使用 |
| §6.3 空コードブロック | ✅ 実装済み | 空コードブロックは `{\n}` ではなく `{}` を出力 |
| §8.3 シングルクォートモード | ✅ 実装済み | `single_quote` 設定が有効 |
| §E2 終了コード | ✅ 実装済み | 仕様通り特定の終了コードを使用 |

---

## テストカバレッジ

**77 件のテスト + 1 件の proptest 冪等性テスト**：

| テストグループ | 件数 | カバレッジ内容 |
|--------|------|----------|
| handlers/tests/expr | 50 | リテラル、二項演算、関数呼び出し、リスト、辞書、return、cast、match、f-string、try、unsafe、構文エラー許容、シングルクォートモード、空コードブロック、単一行コードブロック、引数リスト折り返し、**変数参照、return/break/continue、タプル、インデックスアクセス、フィールドアクセス、ref、エラー回復** |
| handlers/tests/types | 15 | int / float / bool / string / char / void / tuple / option / fn / ref / mut_ref / ptr / name / enum / sum |
| rules/tests/sort_imports | 2 | 分類関数 + 完全な並び替え検証 |
| tests/source_map | 9 | 単一行 / 複数行 / ドキュメント / ネストされたコメント、空白行、オフセット変換 |
| tests/properties | 1 | **冪等性プロパティテスト**（proptest） |

---

## コード品質評価

| 観点 | 評価 | 説明 |
|------|------|------|
| 未完了事項 | 0 | — |
| テストカバレッジ | 良好 | 77 件のテスト + 1 件の proptest、すべての Expr バリアントをカバー |
| ドキュメント完成度 | 高 | ソースコードのコメントが完備、設計ドキュメントが詳細（18 件のルール + 4 件の原則） |
| コード品質 | 良好 | モジュール分割が明確、handler/rules/tests の階層が合理的 |

---

## 改善項目（優先度順）

すべての仕様が実装済みであり、改善項目はありません。