# Список пустого (заглушечного) кода

> Дата генерации: 2026-06-13
> Область проверки: весь проект (`src/`)
> Тип проверки: `todo!()`, пустые тела функций, захардкоженные возвращаемые значения, мёртвый код

## Обзор статистики

| Тип | Количество | Распределение по приоритетам |
|------|------|-----------|
| `todo!()` | 4 | P0: 4 |
| Пустые тела функций | 6 | P0: 2, P1: 2, P2: 2 |
| Захардкоженные возвращаемые значения | 14 | P0: 5, P1: 8, P2: 1 |
| Мёртвый код | 14 | P2: 14 |
| Дублирующиеся реализации | 4 | P2: 4 |
| **Всего** | **42** | |

---

## P0 — Высокий приоритет (отсутствие ключевой функциональности)

### 1. Методы пошагового выполнения отладчика (4 случая `todo!()`)

**Файл**: `src/backends/interpreter/executor/debug.rs`

| Строки | Сигнатура функции | Ожидаемая функциональность | Состояние |
|------|----------|----------|------|
| 32–34 | `fn step(&mut self) -> ExecutorResult<()>` | Пошаговое выполнение одной инструкции | `todo!()` |
| 36–38 | `fn step_over(&mut self) -> ExecutorResult<()>` | Шаг с обходом (без захода внутрь функции) | `todo!()` |
| 40–42 | `fn step_out(&mut self) -> ExecutorResult<()>` | Шаг с выходом (выполнение текущей функции до завершения) | `todo!()` |
| 44–46 | `fn run(&mut self) -> ExecutorResult<()>` | Продолжить выполнение до следующей точки останова | `todo!()` |

**Контекст**: Остальные методы trait `DebuggableExecutor` (`set_breakpoint`, `has_breakpoint`, `current_ip`, `current_function`, `breakpoints`) уже реализованы; не реализовано только управление пошаговым выполнением.

**Рекомендации по реализации**:
- `step()`: выполнить инструкцию по текущему IP, затем IP++
- `step_over()`: если текущая инструкция — вызов функции, установить временную точку останова на следующей инструкции, затем вызвать `run`
- `step_out()`: запомнить текущую глубину стека вызовов, выполнять `run`, пока глубина стека не уменьшится
- `run()`: циклически выполнять инструкции до точки останова или завершения программы

---

## P1 — Средний приоритет (неполная функциональность)

### 6. Уведомления о прогрессе LSP (1 пустое тело функции)

**Файл**: `src/frontend/events/subscribe.rs`

| Строки | Сигнатура функции | Ожидаемая функциональность | Состояние |
|------|----------|----------|------|
| 357–364 | `fn on_event(&self, _event: &dyn Any, _metadata: &EventMetadata)` | Преобразование событий в LSP-уведомления | Пустая реализация |

**Контекст**: Комментарий «Здесь можно добавить логику LSP-уведомлений, например, преобразовывать события прогресса в уведомления `$/progress`».

**Рекомендации по реализации**:
- Проверять тип события (Progress, Diagnostic и т.д.)
- Для событий Progress отправлять уведомления `window/workDoneProgress/create` и `$/progress`
- Для событий Diagnostic отправлять уведомление `textDocument/publishDiagnostics`

---

### 7. Функция пропуска старого синтаксиса (1 пустое тело функции)

**Файл**: `src/frontend/core/parser/statements/declarations.rs`

| Строки | Сигнатура функции | Ожидаемая функциональность | Состояние |
|------|----------|----------|------|
| 171–174 | `fn skip_old_function_syntax(_state: &mut ParserState<'_>)` | Пропуск всего объявления старого синтаксиса функции | Пустая реализация |

**Контекст**: Комментарий «Старый синтаксис удалён, эта функция больше не нужна».

**Рекомендация**: Проверить наличие вызывающих; если их нет — удалить напрямую.

### 11. Проверка границ (2 захардкоженных возвращаемых значения)

**Файл**: `src/frontend/core/typecheck/inference/bounds.rs`

| Строки | Сигнатура функции | Ожидаемая функциональность | Состояние |
|------|----------|----------|------|
| 70–79 | `pub fn check_const_bounds(&self, _ty: &MonoType, _bounds: &[ConstBound]) -> Result<()>` | Проверка const-границ | Захардкожен `Ok(())` |
| 81–90 | `pub fn check_lifetime_bounds(&self, _ty: &MonoType, _bounds: &[LifetimeBound]) -> Result<()>` | Проверка границ времени жизни | Захардкожен `Ok(())` |

**Рекомендации по реализации**:
- `check_const_bounds`:
  - Проверить, удовлетворяют ли const-параметры ограничениям
  - Проверить, можно ли вычислить const-выражения
- `check_lifetime_bounds`:
  - Проверить, удовлетворяют ли параметры времени жизни ограничениям
  - Проверить, превышает ли время жизни заданное ограничение

---

### 13. Разбор ограничений дженериков (1 захардкоженное возвращаемое значение)

**Файл**: `src/frontend/core/typecheck/inference/generics.rs`

| Строки | Сигнатура функции | Ожидаемая функциональность | Состояние |
|------|----------|----------|------|
| 53–59 | `pub fn infer_generic_constraints(&mut self, _constraints: &[String]) -> Result<()>` | Разбор строки ограничений во внутреннее представление | Захардкожен `Ok(())` |

**Рекомендации по реализации**:
- Разобрать строку ограничений (например, `T: Clone + Debug`)
- Извлечь параметры типа и ограничения
- Преобразовать ограничения во внутреннее представление (`TraitConstraint`)
- Добавить их в окружение типов

---

## P2 — Низкий приоритет (мёртвый код, который можно безопасно удалить)

### 28–31. Дублирующиеся реализации `substitute_type`

| № | Файл | Строки | Сигнатура | Различия | Рекомендация |
|---|------|------|------|------|------|
| 28 | `types/eval/evaluator.rs` | 1040 | `fn substitute_type(body, param_name, replacement)` | Только замена `TypeRef` | Удалить (нет вызывающих) |
| 29 | `types/solver.rs` | 685 | `fn substitute_type(&self, ty, substitution)` | Полная замена дочерних узлов | Сохранить |
| 31 | `middle/passes/mono/cross_module.rs` | 609 | `fn substitute_type(generic_type, type_args, type_params)` | Замена по списку параметров | Сохранить |

---

## Обоснованные пустые реализации (сохраняются)

| Файл | Функция | Причина |
|------|------|------|
| `frontend/events/mod.rs:131` | `NullEmitter::emit/emit_with` | Шаблон Null Object |
| `backends/runtime/facade.rs:306,331` | `EmbeddedRuntime::cancel/drive_until` | Семантика встроенного рантайма |
| `backends/common/allocator.rs:195` | `BumpAllocator::dealloc` | Особенности Bump-аллокатора |
| `frontend/core/typecheck/passes/dead_code.rs:190` | `collect_definitions` | Устаревшая, обоснованная заглушка |

---

## Отслеживание прогресса реализации

---

## Примечания
- P0/P1 требуют тщательного проектирования; рекомендуется реализовывать поэтапно и добавлять тесты
- Некоторые функции могут иметь скрытых вызывающих (через trait object или макросы); перед удалением рекомендуется повторно убедиться в их отсутствии
- Дублирующиеся `substitute_type` рекомендуется объединить в единую реализацию