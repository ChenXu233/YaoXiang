# yaoxiang check

Выполняет статическую проверку исходного кода YaoXiang (проверка типов, проверка владения), не генерируя никакого кода.

## Использование

```
yaoxiang check [OPTIONS] [PATH]...
```

## Параметры

| Параметр | Описание                                             |
| -------- | ---------------------------------------------------- |
| `PATH`   | Один или несколько путей к файлам или каталогам. Если не указан, проверяется текущий проект. |

## Опции

| Опция                 | Описание                                           | Значение по умолчанию |
| --------------------- | -------------------------------------------------- | --------------------- |
| `--json`              | Вывод диагностической информации в формате JSON    | Нет                   |
| `-w`, `--watch`       | Наблюдение за изменениями файлов и автоматическая повторная проверка | Нет    |
| `--color <MODE>`      | Режим цветного вывода: `auto`, `always`, `never`   | `auto`                |
| `--exclude <PATH>`     | Исключить указанный путь (можно использовать несколько раз) | Нет          |
| `--no-progress`       | Подавить сообщения о ходе выполнения и сводке     | Нет                   |

## Коды завершения

| Код завершения | Описание               |
| -------------- | ---------------------- |
| `0`            | Ошибок не обнаружено   |
| `1`            | Обнаружены ошибки при проверке |
| `2`            | Файлы `.yx` не найдены |

## Межфайловый анализ

`yaoxiang check` поддерживает межфайловую проверку типов. При проверке нескольких файлов:

1. Параллельный парсинг всех файлов `.yx`
2. Построение графа зависимостей модулей
3. Обнаружение циклических зависимостей (с ошибкой)
4. Проверка в порядке топологической сортировки
5. Использование общей среды типов для корректного обнаружения межфайловых ссылок

```bash
# Проверить весь проект (автообнаружение межфайловых ссылок)
yaoxiang check src/

# Проверить указанные файлы
yaoxiang check src/main.yx src/lib.yx
```

## Инкрементная проверка (режим watch)

Используйте `-w` или `--watch` для включения режима наблюдения за файлами. При изменениях файлов проверка автоматически повторяется.

```bash
yaoxiang check --watch
```

## Формат JSON-вывода

При использовании `--json` формат вывода следующий:

```json
{
  "error_count": 0,
  "warning_count": 0,
  "diagnostics": [
    {
      "file": "src/main.yx",
      "severity": "error",
      "code": "E1001",
      "message": "Unknown variable: 'x'",
      "line": 5,
      "column": 3,
      "end_line": 5,
      "end_column": 4,
      "lsp": { ... }
    }
  ]
}
```

## Примеры

```bash
# Проверить текущий проект
yaoxiang check

# Проверить указанные файлы
yaoxiang check src/main.yx

# Проверить каталог и вывести JSON
yaoxiang check src/ --json

# Режим наблюдения
yaoxiang check --watch

# Режим CI (без цвета, без прогресса)
yaoxiang check --color never --no-progress

# Исключить каталог тестов
yaoxiang check src/ --exclude tests/
```

## Интеграция с CI

```yaml
# GitHub Actions
- name: Type check
  run: yaoxiang check --color never --no-progress
```

Подробные настройки CI см. в [Руководстве по интеграции CI](../guide/ci-integration.md).

## Смотрите также

- [`yaoxiang format`](./format-command.md) -- Форматирование кода
- [Справочник кодов ошибок](./error-codes.md) -- Полный список кодов ошибок
- [Руководство по интеграции CI](../guide/ci-integration.md) -- Интеграция с CI/CD
- [Проектирование диагностической системы](../design/check/diagnostic-system.md) -- Документация по архитектуре
