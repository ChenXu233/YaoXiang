# Справочник по кодам ошибок

Компилятор YaoXiang использует коды ошибок для идентификации различных типов диагностических
сообщений. Коды ошибок сгруппированы по диапазонам номеров, каждый код соответствует определённому
сценарию ошибки.

---

## E0xxx — Лексический и синтаксический анализ

Ошибки, возникающие на этапах лексического анализатора (Lexer) и синтаксического анализатора
(Parser).

| 错误码 | 模板                                                                                                     | 说明                            |
| ------ | -------------------------------------------------------------------------------------------------------- | ------------------------------- |
| E0001  | `Недопустимый символ: '{char}'`                                                                          | Недопустимый символ             |
| E0002  | `Недопустимый числовой литерал: '{literal}'`                                                             | Недопустимый числовой литерал   |
| E0003  | `Незавершённая строка, начинающаяся со строки {line}`                                                    | Незавершённая строка            |
| E0004  | `Недопустимый символьный литерал: '{literal}'`                                                           | Недопустимый символьный литерал |
| E0010  | `Ожидалось {expected}, найдено {found}`                                                                  | Ожидаемый токен                 |
| E0011  | `Неожиданный токен: '{token}'`                                                                           | Неожиданный токен               |
| E0012  | `Недопустимый синтаксис: {reason}`                                                                       | Недопустимый синтаксис          |
| E0013  | `Несоответствующая скобка {bracket_type}: открыта на строке {open_line}, столбце {open_col}, не закрыта` | Несоответствующая скобка        |
| E0014  | `Отсутствует точка с запятой после {statement}`                                                          | Отсутствует точка с запятой     |

## E1xxx — Проверка типов

Ошибки, возникающие на этапе проверки типов, охватывающие типы переменных, вызовы функций,
сопоставление с образцом, инстанцирование обобщений, семантику параллелизма и распространение ошибок
и т.д.

| 错误码 | 模板                                                                                                      | 说明                                                                     |
| ------ | --------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------ |
| E1001  | `Неизвестная переменная: '{name}'`                                                                        | Неизвестная переменная                                                   |
| E1002  | `Ожидался тип '{expected}', найден тип '{found}'`                                                         | Несоответствие типов                                                     |
| E1003  | `Неизвестный тип: '{type}'`                                                                               | Неизвестный тип                                                          |
| E1010  | `Функция '{func}' ожидает {expected} аргумент(ов), найдено {found}`                                       | Несоответствие количества аргументов                                     |
| E1011  | `Несоответствие типа параметра: ожидался '{expected}', найден '{found}'`                                  | Несоответствие типа параметра                                            |
| E1012  | `Несоответствие возвращаемого типа: ожидался '{expected}', найден '{found}'`                              | Несоответствие возвращаемого типа                                        |
| E1013  | `Функция не найдена: '{func}'`                                                                            | Функция не найдена                                                       |
| E1020  | `Не удаётся вывести тип для '{expr}'`                                                                     | Не удаётся вывести тип                                                   |
| E1021  | `Конфликт вывода типов: {reason}`                                                                         | Конфликт вывода типов                                                    |
| E1030  | `Сопоставление с образцом не исчерпывающее: отсутствуют образцы {patterns}`                               | Неполное сопоставление с образцом                                        |
| E1031  | `Недостижимый образец: '{pattern}'`                                                                       | Недостижимый образец                                                     |
| E1040  | `Операция '{op}' не поддерживается для типа '{type}'`                                                     | Операция не поддерживается                                               |
| E1041  | `Индекс вне границ: допустимый диапазон 0..{max}, найден {index}`                                         | Индекс вне границ                                                        |
| E1042  | `Поле '{field}' не найдено в структуре '{struct}'`                                                        | Поле не найдено                                                          |
| E1050  | `Логическая операция требует логических операндов, найдены '{left}' и '{right}'`                          | Требуются логические операнды                                            |
| E1051  | `Логическое NOT требует логический операнд, найден '{type}'`                                              | Логическое NOT требует логический операнд                                |
| E1052  | `Не удаётся разыменовать тип '{type}', ожидается тип указателя`                                           | Недопустимое разыменование                                               |
| E1053  | `Не удаётся получить доступ к полю для неструктурного типа '{type}'`                                      | Доступ к полю неструктурного типа                                        |
| E1054  | `Условие должно быть логическим, найден '{type}'`                                                         | Несоответствие типа условия                                              |
| E1055  | `Ограничение типа '{type}' может использоваться только в обобщённом контексте`                            | Ограничение в необобщённом контексте                                     |
| E1060  | `Ожидалось {expected} аргумент(ов) типа, найдено {found}`                                                 | Несоответствие количества аргументов типа                                |
| E1061  | `Не удаётся инстанцировать обобщённый тип с заданными аргументами`                                        | Не удаётся инстанцировать обобщение                                      |
| E1081  | `` `?` допускается только внутри функций, возвращающих Result ``                                          | `?` допускается только в функциях, возвращающих Result                   |
| E1082  | `` `?` требует выражение Result, найдено '{type}' ``                                                      | `?` можно использовать только с выражениями Result                       |
| E1083  | ``Несоответствие типа ошибки Result для `?`: ожидался '{expected}', найден '{found}'``                    | Несоответствие типа ошибки для `?`                                       |
| E1090  | `Тип: Тип = Тип`                                                                                          | Непроизносимое (пасхалка)                                                |
| E1091  | `Самоссылка обобщённого метатипа не допускается: '{decl}'`                                                | Недопустимый обобщённый метатип                                          |
| E1062  | `Нарушение ограничения const-обобщения: {reason}`                                                         | Нарушение const-ограничения                                              |
| E1064  | `Недопустимая позиция(ии) связывания {positions} для функции с {total} параметром(ами)`                   | Недопустимый индекс позиции связывания (RFC-004)                         |
| E1095  | `Неизвестный интерфейс: '{name}'`                                                                         | Неизвестный интерфейс (RFC-011a)                                         |
| E1096  | `Интерфейс '{name}' ожидает {expected} аргумент(ов) типа, найдено {found}`                                | Несоответствие количества аргументов инстанцирования интерфейса          |
| E1097  | `Член интерфейса '{member}' конфликтует с полем типа '{type}'`                                            | Конфликт имени члена интерфейса с полем                                  |
| E1098  | `Тип '{type}' не реализует '{interface}.{method}'`                                                        | Метод интерфейса не реализован                                           |
| E1099  | `Несоответствие сигнатуры для '{type}.{method}': ожидалась '{expected}', найдена '{found}'`               | Несоответствие сигнатуры метода интерфейса                               |
| E1100  | `Дублирующаяся реализация '{type}.{method}' (переопределение не допускается)`                             | Дублирующаяся реализация с той же сигнатурой (переопределение запрещено) |
| E1101  | `Тип '{type}' не реализует интерфейс '{interface}' и не может находиться в этой экзистенциальной позиции` | Тип не реализует интерфейс (проверка членов экзистенциального типа)      |

## E2xxx — Семантический анализ

Ошибки, возникающие на этапе семантического анализа, охватывающие область видимости, время жизни
переменных, ownership и разрешение сигнатур функций.

| 错误码 | 模板                                                                                        | 说明                                          |
| ------ | ------------------------------------------------------------------------------------------- | --------------------------------------------- |
| E2001  | `Переменная '{name}' не находится в области видимости`                                      | Ошибка области видимости                      |
| E2002  | `Дублирующееся определение: '{name}' уже определено в этой области видимости`               | Дублирующееся определение                     |
| E2003  | `Нарушение ограничения ownership: {reason}`                                                 | Ошибка ownership                              |
| E2010  | `Не удаётся присвоить значение неизменяемой переменной '{name}'`                            | Присвоение неизменяемой переменной            |
| E2011  | `Использование неинициализированной переменной '{name}'`                                    | Использование неинициализированной переменной |
| E2012  | `Конфликт изменяемости: невозможно использовать изменяемую ссылку в неизменяемом контексте` | Конфликт изменяемости                         |
| E2013  | `Не удаётся затенить существующую переменную '{name}'`                                      | Затенение переменной                          |
| E2014  | `'{name}' был перемещён и не может быть использован`                                        | Использование перемещённой переменной         |
| E2090  | `Недопустимая сигнатура: {reason}`                                                          | Недопустимая сигнатура                        |
| E2091  | `Недопустимая сигнатура: неизвестный тип '{type_name}'`                                     | Неизвестный тип в сигнатуре                   |
| E2092  | `Недопустимая сигнатура: отсутствует '->'`                                                  | В сигнатуре отсутствует стрелка               |
| E2093  | `Недопустимая сигнатура: дублирующийся параметр '{name}'`                                   | Дублирующееся имя параметра                   |
| E2094  | `Недопустимая сигнатура: обобщённый '{name}' затеняет внешний обобщённый`                   | Затенение обобщённого параметра               |
| E2095  | `Недопустимая сигнатура: параметр '{name}' затеняет обобщённый`                             | Затенение обобщённого именем параметра        |

## E4xxx — Обобщения и trait'ы

Ошибки, связанные с обобщёнными ограничениями и системой trait'ов.

| 错误码 | 模板                                                          | 说明                              |
| ------ | ------------------------------------------------------------- | --------------------------------- |
| E4001  | `Тип '{type}' не удовлетворяет ограничению trait'а '{trait}'` | Нарушение обобщённого ограничения |
| E4002  | `Trait '{trait}' не найден`                                   | Trait не найден                   |
| E4003  | `Отсутствует реализация trait'а '{trait}' для типа '{type}'`  | Отсутствует реализация trait'а    |
| E4004  | `Конфликтующие реализации trait'а для '{trait}'`              | Конфликт реализаций trait'а       |
| E4005  | `Связанный тип '{assoc_type}' не найден в '{container}'`      | Связанный тип не найден           |

## E5xxx — Модули и импорт

Ошибки модульной системы и импорта.

| 错误码 | 模板                                                   | 说明                          |
| ------ | ------------------------------------------------------ | ----------------------------- |
| E5001  | `Модуль '{module}' не найден`                          | Модуль не найден              |
| E5002  | `Не удалось импортировать модуль '{module}': {reason}` | Ошибка импорта                |
| E5003  | `Экспорт '{export}' не найден в модуле '{module}'`     | Экспорт не найден             |
| E5004  | `Обнаружена циклическая зависимость: {path}`           | Циклическая зависимость       |
| E5005  | `Недопустимый путь модуля: '{path}'`                   | Недопустимый путь модуля      |
| E5006  | `Дублирующийся импорт: '{name}' уже импортирован`      | Дублирующийся импорт          |
| E5007  | `Модуль '{module}' экспортирует: {available}`          | Подсказка об экспортах модуля |

## E6xxx — Runtime

Ошибки, возникающие на этапе runtime.

| 错误码 | 模板                                                                      | 说明                         |
| ------ | ------------------------------------------------------------------------- | ---------------------------- |
| E6001  | `Деление на ноль в выражении: {expr}`                                     | Деление на ноль              |
| E6003  | `Индекс массива вне границ: допустимый диапазон 0..{max}, найден {index}` | Индекс массива вне границ    |
| E6004  | `Переполнение стека: глубина рекурсии превысила предел {limit}`           | Переполнение стека           |
| E6005  | `Сбой утверждения: {condition}`                                           | Сбой утверждения             |
| E6006  | `Функция не найдена: '{func}'`                                            | Функция не найдена (runtime) |
| E6007  | `Ошибка runtime: {message}`                                               | Ошибка runtime               |

## E7xxx — I/O и системные

Операции I/O и системные ошибки.

| 错误码 | 模板                        | 说明            |
| ------ | --------------------------- | --------------- |
| E7001  | `Файл не найден: '{path}'`  | Файл не найден  |
| E7002  | `Доступ запрещён: '{path}'` | Доступ запрещён |
| E7003  | `Ошибка I/O: {reason}`      | Ошибка I/O      |
| E7004  | `Сетевая ошибка: {reason}`  | Сетевая ошибка  |

## E8xxx — Внутренние ошибки компилятора

Внутренние ошибки компилятора, обычно указывающие на баг в самом компиляторе. При обнаружении таких
ошибок, пожалуйста, сообщите о них в [GitHub Issues](https://github.com/yaoxiang/yaoxiang/issues).

| 错误码 | 模板                                           | 说明                          |
| ------ | ---------------------------------------------- | ----------------------------- |
| E8001  | `Внутренняя ошибка компилятора: {message}`     | Внутренняя ошибка компилятора |
| E8002  | `Неожиданная паника компилятора: {reason}`     | Неожиданная паника            |
| E8003  | `Ошибка фазы компилятора: {phase} - {message}` | Ошибка фазы компилятора       |

## W1xxx — Предупреждения

Предупреждения, связанные с обнаружением мёртвого кода. Предупреждения не препятствуют компиляции,
но указывают на возможные проблемы в коде.

| 错误码 | 模板                                                   | 说明                                       |
| ------ | ------------------------------------------------------ | ------------------------------------------ |
| W1001  | `Неиспользуемая экспортированная функция: '{name}'`    | Неиспользуемая экспортированная функция    |
| W1002  | `Неиспользуемый экспортированный тип: '{name}'`        | Неиспользуемый экспортированный тип        |
| W1003  | `Неиспользуемый импорт: '{name}'`                      | Неиспользуемый импорт                      |
| W1004  | `Неиспользуемая экспортированная переменная: '{name}'` | Неиспользуемая экспортированная переменная |
| W1005  | `Неиспользуемый экспортированный метод: '{name}'`      | Неиспользуемый экспортированный метод      |

| W1063 | `Ограничение const-обобщения не вычисляется во время компиляции` | Ограничение
const-обобщения не вычисляется во время компиляции |

---

Всего **85** диагностических кодов (79 кодов ошибок + 6 кодов предупреждений).
