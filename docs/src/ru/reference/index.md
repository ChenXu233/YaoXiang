# Справочная документация YaoXiang

> Этот документ находится в разработке...

YaoXiang в настоящее время находится на стадии **экспериментальной проверки**; стандартная
библиотека и API постепенно дорабатываются.

## Спецификация языка

- [Обзор спецификации языка](./language-spec/index.md)
- [Спецификация синтаксиса](./language-spec/syntax.md) — Лексическая структура, правила грамматики,
  приоритет операторов
- [Система типов](./language-spec/type-system.md) — Базовые типы, составные типы, generics, trait
- [Система модулей](./language-spec/modules.md) — Определение модулей, импорт/экспорт, область
  видимости
- [Модель параллелизма](./language-spec/concurrency.md) — Асинхронное программирование, примитивы
  параллелизма, модель памяти
- [Стандартная библиотека](./language-spec/stdlib.md) — Ядро, библиотека ввода-вывода,
  математическая библиотека

## Текущее состояние

| Модуль           | Статус           | Описание                     |
| ---------------- | ---------------- | ---------------------------- |
| `std.io`         | 🔨 В разработке  | Ввод/вывод                   |
| `std.string`     | 🔨 В разработке  | Операции со строками         |
| `std.list`       | 🔨 В разработке  | Операции со списками         |
| `std.dict`       | ✅ Реализовано   | Операции со словарями        |
| `std.range`      | ✅ Реализовано   | Диапазоны и итераторы (#302) |
| `std.math`       | 🔨 В разработке  | Математические функции       |
| `std.net`        | 📋 Запланировано | Сетевые операции             |
| `std.concurrent` | 📋 Запланировано | Примитивы параллелизма       |

## Встроенные типы

### Примитивные типы

| Тип      | Описание                                            | Пример          |
| -------- | --------------------------------------------------- | --------------- |
| `Void`   | Пустое значение / отсутствие возвращаемого значения | `()`            |
| `Bool`   | Логическое значение                                 | `true`, `false` |
| `Int`    | Целое число                                         | `42`, `-10`     |
| `Float`  | Число с плавающей точкой                            | `3.14`, `-0.5`  |
| `Char`   | Символ                                              | `'a'`, `'中'`   |
| `String` | Строка                                              | `"hello"`       |

### Составные типы

| Тип                  | Описание                     | Пример         |
| -------------------- | ---------------------------- | -------------- |
| `Tuple(T1, T2, ...)` | Кортеж разнородных элементов | `(1, "hello")` |
| `(Args) -> Ret`      | Тип функции                  | `(Int) -> Int` |

> #299: Типы-контейнеры (`List(T)` / `Vec(T)` / `Array(T, N)` / `Dict(K, V)`) не являются
> встроенными примитивами — это конструкторы обобщённых типов, имеющие тот же статус, что и
> пользовательские дженерики, и обрабатываются через единый механизм инстанцирования дженериков.
> Синтаксис литералов (`[...]` / `{...}`) остаётся в ядре, целевой тип определяется аннотацией
> контекста. Set исключён (#300), подробности см. в [Спецификации языка](language-spec/syntax.md).
>
> Три концепции контейнеров различаются по принадлежности информации о длине: `Array(T, N)` — длина
> в типе (фиксированная), `Vec(T)` — длина как значение runtime (примитив сырого буфера), `List(T)`
> — тип стандартной библиотеки (`{ data: Vec(T), length: Int }`, вся логика в библиотеке).

### Пользовательские типы

```yaoxiang
// 记录类型（结构体）
Point: Type = { x: Float, y: Float }

// 枚举类型
Result: (T: Type, E: Type) -> Type = { ok: (T) -> Result(T, E), err: (E) -> Result(T, E) }

// 接口类型（所有字段为函数）
Callable: Type = { call: (String) -> Void }
```

## Встроенные функции

### Вывод

```yaoxiang
print(value)           // 打印，无换行
println(value)         // 打印，有换行
```

### Преобразование

```yaoxiang
to_string(value)       // 转换为字符串
to_int(value)          // 转换为整数
to_float(value)        // 转换为浮点数
```

### Проверка типов

```yaoxiang
typeof(value)         // 返回类型名称
is_type(value, type)  // 检查类型
```

## Ключевые слова

| Ключевое слово            | Описание                       |
| ------------------------- | ------------------------------ |
| `Type`                    | meta type                      |
| `spawn`                   | помечает spawn-функцию         |
| `spawn for`               | Параллельный цикл (spawn loop) |
| `spawn {}`                | spawn-блок                     |
| `if` / `else if` / `else` | Условное ветвление             |
| `match`                   | pattern matching               |
| `while` / `for`           | Цикл                           |
| `return`                  | Возврат значения               |
| `ref`                     | Создание ссылки                |
| `mut`                     | Метка изменяемости             |

## Краткий справочник по синтаксису

### Объявление переменных

```yaoxiang
// 不可变变量（默认）
x: Int = 42
y = 42                 // 类型推断

// 可变变量
mut count: Int = 0
count = count + 1
```

### Определение функций

```yaoxiang
// 普通函数
add: (a: Int, b: Int) -> Int = a + b

// 并作函数（自动并发）
fetch: (url: String) -> JSON spawn = HTTP.get(url).json()

// 泛型函数
identity: [T](x: T) -> T = x
```

### Управление потоком

```yaoxiang
// 条件
if x > 0 {
    print("positive")
} else if x < 0 {
    print("negative")
} else {
    print("zero")
}

// 模式匹配
match result {
    ok(value) => print("success: " + value),
    err(error) => print("error: " + error),
}

// 循环
for i in 0..10 {
    print(i)
}
```

### Обработка ошибок

```yaoxiang
// ? 运算符传播错误
data = fetch_file(path)?
```

## Приоритет операторов

| Приоритет  | Оператор                              |
| ---------- | ------------------------------------- |
| Наивысший  | `( )` Вызов функции                   |
|            | `.` Доступ к полю                     |
|            | `[ ]` Индексация                      |
|            | `unary -` Унарный минус               |
|            | `* / %` Умножение / деление / остаток |
|            | `+ -` Сложение / вычитание            |
|            | `== != < > <= >=` Сравнение           |
|            | `and or` Логические операции          |
| Наименьший | `=` Присваивание                      |

## Примеры использования стандартной библиотеки

```yaoxiang
// 导入标准库
use std.io.{print, println}

// 列表操作
use std.list.{list_push, list_pop, list_len}

// 数学函数
use std.math.{sqrt, sin, cos, PI}

// 使用
println("Hello, YaoXiang!")
result = sqrt(16.0)  // 4.0
```

## Инструменты командной строки

```bash
# 运行脚本
yx run hello.yx

# 构建字节码
yx build hello.yx -o hello.42

# 解释执行
yx eval 'println("Hello")'

# 查看帮助
yaoxiang --help
```

## Полный пример

```yaoxiang
use std.convert
use std.io

// 计算斐波那契数列
fib: (n: Int) -> Int = if n <= 1 {
    n
} else {
    fib(n - 1) + fib(n - 2)
}

// 主函数
main: () -> Void = {
    io.println("Fibonacci(10) = " + convert.to_string(fib(10)))
}
```

## Связанные ресурсы

- [Учебник](../tutorial/) — Изучение YaoXiang
- [Проектная документация](../design/) — Решения по дизайну языка
- [GitHub](https://github.com/ChenXu233/YaoXiang)

## Руководство по участию

Стандартная библиотека находится в разработке; будем рады вашему вкладу!

1. Выберите модуль (например, `std.io`, `std.net`)
2. Реализуйте функции в `src/std/`
3. Добавьте комментарии-документацию
4. Отправьте PR
