```markdown
---
title: "RFC-001：Модель параллельного выполнения и система обработки ошибок"
---

# RFC-001：Модель параллельного выполнения и система обработки ошибок

> **Статус**: Принято
> **Автор**: Чэнь Сюй (晨煦)
> **Дата создания**: 2025-01-05
> **Последнее обновление**: 2026-05-11 (упрощение: удалён @auto, эвристика L1 fallback, сокращены записи обсуждений)

## Источники дизайна

| Документ | Связь |
|----------|-------|
| [async-whitepaper](/src/archive/async-whitepaper) | Источник дизайна, теоретическая база |
| [language-spec](/src/design/language-spec) | Целевая спецификация |

## Аннотация

Предлагается модель параллельного выполнения YaoXiang: синхронный синтаксис для описания логики с автоматическим параллельным выполнением во время исполнения. Ключевые механизмы: трёхуровневая параллельная архитектура + анализ DAG зависимостей + система типов Result.

## Краткий выбор

| Сценарий | Запись | Описание |
|----------|--------|----------|
| Автоматический параллелизм | Без аннотаций (по умолчанию) | Максимальный параллелизм |
| Синхронное ожидание | `@eager` | Ожидание завершения зависимостей |
| Полностью последовательно | `@block` | Без параллелизма, для отладки |
| Частичный параллелизм | `spawn` | Параллелизм внутри области видимости @block |

## Мотивация

Текущие модели параллелизма в мейнстрим языках имеют явные недостатки:

| Язык | Модель параллелизма | Проблема |
|------|---------------------|----------|
| Rust | async/await + tokio | Распространение асинхронности, крутая кривая обучения |
| Go | goroutine | Отсутствие типобезопасности |
| Python | asyncio | Ограничения GIL |
| JavaScript | Promise/async | Сложные колбэки |

### Ключевое противоречие

1. **Прозрачность vs управляемость**: полная прозрачность без контроля vs полный контроль без прозрачности
2. **Параллелизм vs отладка**: параллельные программы сложно отлаживать vs отлаживаемые программы сложно распараллелить

---

## Предложение

### 1. Модель параллельного выполнения: трёхуровневая параллельная архитектура

> **Описание**: L1/L2/L3 — это ментальная модель, помогающая пользователям понять различные сценарии. Фактическая реализация одна: автоматический анализ DAG + контроль через аннотации.

| Уровень | Ментальная модель | Синтаксис | Способ выполнения | Степень параллелизма |
|---------|-------------------|-----------|-------------------|---------------------|
| **L1** | Запрет параллелизма | `@block` | Чисто последовательное выполнение | ❌ Нет |
| **L2** | Параллелизм внутри @block | `spawn` | Управляемый параллелизм в области видимости @block | ⚠️ Частичный |
| **L3** | Полный параллелизм | По умолчанию (без аннотаций) | Автоматический анализ DAG | ✅ Полный |

#### L1: Синхронный режим @block

```yaoxiang
main: () -> Void @block = {
    data1 = fetch_sync("api1")
    data2 = fetch_sync("api2")
    process(data1, data2)    # Строго последовательно, без параллелизма
}
```

#### L2: Управляемый параллелизм внутри @block

```yaoxiang
# spawn можно использовать только внутри функций @block
main: () -> Void @block = {
    spawn { data1 = fetch_data("api1") }
    spawn { data2 = fetch_data("api2") }
    # Ожидание завершения всех spawn (контроль стандартной библиотеки)
    process(data1, data2)
}
```

#### L3: Полная прозрачность (по умолчанию)

```yaoxiang
# Не нужны аннотации, компилятор автоматически анализирует DAG
heavy_calc: (n: Int) -> Int = fibonacci(n)

auto_parallel: (n: Int) -> Int = {
    a = heavy_calc(1)    # Автоматический параллелизм
    b = heavy_calc(2)    # Автоматический параллелизм
    c = heavy_calc(3)    # Автоматический параллелизм
    a + b + c            # При необходимости значения — ожидание всех результатов
}
```

### 2. Полное сравнение аннотаций

| Измерение | По умолчанию (без аннотаций) | `@eager` | `@block` | `spawn` |
|-----------|------------------------------|----------|----------|---------|
| **Способ выполнения** | Автоматический анализ DAG | Синхронное ожидание зависимостей | Чисто последовательно | Параллелизм внутри @block |
| **Степень параллелизма** | ✅ Полный | ⚠️ По порядку зависимостей | ❌ Нет | ⚠️ Частичный |
| **Построение DAG** | ✅ | ✅ | ❌ | ✅ |

**Руководство по выбору**:
- Максимальный параллелизм → без аннотаций (по умолчанию)
- Нужны упорядоченные побочные эффекты → `@eager`
- Отладка/новички/критический код → `@block`
- Нужен параллелизм внутри @block → `spawn`

```yaoxiang
# По умолчанию: максимальный параллелизм
calc_all: () -> Int = {
    a = heavy_calc(1)    # Автоматический параллелизм
    b = heavy_calc(2)    # Автоматический параллелизм
    a + b
}

# @eager: синхронное ожидание
calc_seq: () -> Int @eager = {
    a = heavy_calc(1)    # Синхронное выполнение
    b = heavy_calc(2)    # Синхронное выполнение
    a + b
}

# @block: чисто последовательно
calc_simple: () -> Int @block = {
    a = heavy_calc(1)    # Принудительно синхронно
    b = heavy_calc(2)    # Синхронно
    a + b
}

# spawn: параллелизм внутри @block
calc_mixed: () -> Int @block = {
    spawn { heavy_calc(1) }
    spawn { heavy_calc(2) }
    heavy_calc(3)        # Синхронно
}
```

### 3. Анализ DAG зависимостей

#### 3.1 Ключевой принцип: выполнение снизу вверх

```
Пользовательский код (синхронный синтаксис):
    a = fetch(url0)
    b = fetch(url1)
    print(a)

Анализ при компиляции (снизу вверх):
    print(a) требует a → зависит от fetch(url0)
    fetch(url1) никому не нужен → изолированный DAG

Планирование выполнения (от листьев):
    fetch(url0) → print(a)    ← цепочка зависимостей, по порядку
    fetch(url1)                ← изолированный, параллельно независимо
```

**Ключевой инсайт**: не "сверху вниз" генерация Future, а "снизу вверх" обратный анализ зависимостей от результатов.

#### 3.2 Изолированный DAG: независимый параллелизм

```
Основной поток: fetch(url0) → process → print
Изолированный:  fetch(url1)  ← результат никому не нужен, параллельно независимо

Планировщик: основной поток выполняется по цепочке зависимостей, изолированный — на другом ядре параллельно
```

#### 3.3 Типы ресурсов и побочные эффекты

**Ключевая идея**: операции с ресурсами маркируются через типы, DAG автоматически строится. Операции с одним ресурсом автоматически сериализуются, с разными ресурсами — автоматически распараллеливаются.

**Границы типов ресурсов — чёткое определение**:

Типы ресурсов — это встроенные в компилятор маркерные типы. Следующие типы распознаются компилятором как ресурсы:

| Тип ресурса | Описание | Поведение компилятора |
|-------------|----------|----------------------|
| `FilePath` | Путь файловой системы | Операции с одним путём автоматически сериализуются |
| `HttpUrl` | HTTP endpoint | Операции с одним URL автоматически сериализуются |
| `DBUrl` | Подключение к БД | Операции с одним подключением автоматически сериализуются |
| `Console` | Стандартный вывод | Все операции Console автоматически сериализуются |

Пользовательские типы ресурсов требуют явной маркировки:
```yaoxiang
Database: Resource              # Явная маркировка как тип ресурса
query: (Database, String) -> Result(Row, Error)
# Параметр Database является Resource, автоматически распознаётся как операция с ресурсом
```

Типы без маркера Resource не отслеживаются компилятором для зависимостей по ресурсам.

**Правила использования**:
- Передавайте дескрипторы ресурсов через переменные, DAG автоматически управляет порядком
- Использование литералов для одного ресурса — проблема дизайна пользователя, не ответственность языка

```yaoxiang
# ✅ Правильно: передача через переменную, DAG автоматически сериализует
filename: String = "data.txt"
File.write(filename, x)
File.write(filename, y)    # DAG сериализует

# ⚠️ Ответственность пользователя: литералы
File.write("data.txt", x)
File.write("data.txt", y)  # Возможно параллельно, пользователь сам отвечает
```

#### 3.4 Обработка бесконечных циклов

```
1 цикл → прямое синхронное выполнение, нулевые накладные расходы планировщика
Несколько циклов → планировщик разбивает на срезы, переключается, реальный параллелизм
```

### 4. Тип Result и обработка ошибок

```yaoxiang
Result: (T: Type, E: Type) -> Type = { ok: (T) -> Self, err: (E) -> Self }

# Оператор ? прозрачно распространяет ошибки
process: () -> Result(Data, Error) = {
    data = fetch_data()?
    processed = transform(data)?
    save(processed)?
}
```

### 5. Дизайн узлов DAG

```rust
enum NodeKind {
    Task,      // Узел задачи
    Value,     // Узел значения
    Control,   // Узел управления потоком
}

struct Node {
    id: NodeId,
    kind: NodeKind,
    inputs: Vec<ValueNodeId>,   // Входные зависимости
    outputs: Vec<ValueNodeId>,  // Выходные значения
    span: Span,                 // Позиция в исходном коде
}
```

| Тип ребра | Символ | Семантика |
|-----------|--------|-----------|
| DataEdge | → | Зависимость по данным (поток значений) |
| ControlEdge | ● | Зависимость по управлению (последовательное выполнение) |
| SpawnEdge | ◎ | Вход параллелизма (точка возможного параллелизма) |

### 6. Система типов

```
Send → Безопасно для передачи между потоками
Sync → Безопасно для совместного использования между потоками
Arc(T) реализует Send + Sync (потокобезопасный подсчёт ссылок)
```

---

## Компромиссы

### Преимущества

1. **Постепенное принятие**: трёхуровневая модель адаптируется к разному уровню навыков
2. **Естественный синтаксис**: синхронный код получает производительность параллельного
3. **Компиляционная безопасность**: ограничения Send/Sync устраняют гонки данных
4. **Отлаживаемость**: граф ошибок обеспечивает чёткое представление распространения ошибок

### Недостатки

1. **Кривая обучения**: требуется понимание концепции DAG зависимостей
2. **Время компиляции**: анализ DAG для всей программы может быть медленным
3. **Сложность инструментария**: требуются принципиально новые инструменты отладки и визуализации

## Альтернативные решения

| Решение | Почему не выбрано |
|---------|-------------------|
| Только явный async/await | Не позволяет реализовать прозрачный параллелизм |
| Только полностью прозрачный параллелизм | Пользователь теряет контроль |
| Goroutine в стиле Go | Отсутствие типобезопасности, невозможность проверки при компиляции |
| Только режим L1 | Отказ от ключевой ценности модели параллельного выполнения |

## Стратегия реализации

### Фазы

1. **Фаза 1 (v0.1)**: Синхронный режим @block, базовые типы
2. **Фаза 2 (v0.2)**: Планировщик FlowScheduler
3. **Фаза 3 (v0.3)**: Блоки spawn, явный параллелизм
4. **Фаза 4 (v0.5)**: L3 полная прозрачность, автоматический анализ DAG
5. **Фаза 5 (v0.6)**: Граф ошибок, отладчик графов
6. **Фаза 6 (v1.0)**: Оптимизация для производственного использования

### Зависимости

- RFC-001 без внешних зависимостей (базовое ядро)
- RFC-008 (Параллельная модель Runtime) → Дизайн завершён
- RFC-011 (Система обобщений) → Дизайн завершён

### Риски

1. **Производительность анализа DAG**: анализ всей программы может быть O(n²), требуется оптимизация
2. **Отсутствие инструментария**: отладчик нужно разрабатывать с нуля
3. **Принятие пользователями**: прозрачный параллелизм требует качественной документации

---

## Журнал решений по дизайну

| Решение | Решено | Дата |
|---------|--------|------|
| Трёхуровневая параллельная архитектура | L1/L2/L3 постепенно | 2025-01-05 |
| Позиция аннотации @block | После типа возврата | 2025-01-05 |
| Распространение ошибок DAG | Вдоль рёбер зависимостей вверх по потоку | 2025-01-06 |
| Оптимизация производительности DAG | Инкрементное построение + кэширование | 2025-01-06 |
| Выбор runtime | Обобщения + инъекция при компиляции | 2025-01-06 |
| Интерфейс узлов | Обобщения + инъекция функций (без trait) | 2025-01-06 |
| Память графа ошибок | DAG строится только внутри одной функции | 2025-01-06 |
| Обнаружение конфликтов ресурсов | Потоковая зависимость DAG, передача пользовательских переменных | 2025-01-06 |
| Система типов ресурсов | Маркер Resource + автоматическая зависимость DAG | 2026-01-06 |
| Ментальная модель L1/L2/L3 | Три уровня абстракции, не механизм реализации | 2026-01-06 |
| Аннотация @auto | Удалена, дублирует поведение по умолчанию | 2026-05-11 |
| Автоматический fallback L1 | Удалён, поведение непредсказуемо | 2026-05-11 |

---

## Приложение: Глоссарий терминов

| Термин | Определение |
|--------|-------------|
| Модель параллельного выполнения | Парадигма параллелизма YaoXiang: синхронный синтаксис, асинхронная суть |
| DAG | Направленный ациклический граф, описывающий зависимости вычислений |
| spawn | Управляемый параллелизм в области видимости @block |
| @block | Синхронная аннотация, отключает оптимизацию параллелизма |
| @eager | Срочное вычисление, ожидание завершения зависимостей |
| Resource | Маркер типа ресурса, операции автоматически строят DAG зависимости |
| Граф ошибок | Визуализированный путь распространения ошибок |

## Ссылки

- [Rust async book](https://rust-lang.github.io/async-book/)
- [Параллельные паттерны Go](https://golang.org/doc/effective_go#concurrency)
- [Work-stealing планирование](https://en.wikipedia.org/wiki/Work_stealing)
- [Whitepaper модели параллельного выполнения](/src/archive/async-whitepaper)
- [Спецификация языка YaoXiang](/src/design/language-spec)
```