```markdown
---
title: RFC-007: Единая схема синтаксиса определения функций
---

# RFC-007: Единая схема синтаксиса определения функций

> **Статус**: Принято
> **Автор**: 沫郁酱
> **Дата создания**: 2025-01-05
> **Последнее обновление**: 2026-03-21 (согласование правил конструктора типов с семантикой возврата из блоков кода)

## Аннотация

Данный RFC определяет финальный вариант **синтаксиса определения функций** в языке YaoXiang. Используется унифицированный синтаксис `name: (params) -> Return = body`, полностью соответствующий модели `name: type = value` из RFC-010.

Для предотвращения неоднозначности: при наличии входных параметров функции тип параметра должен быть явно указан либо в «сигнатуре», либо в «заголовке lambda» — как минимум в одном из этих мест; опущение типа с обеих сторон не допускается.

Последнее выражение в блоке кода `{ ... }` используется в качестве возвращаемого значения; пустой блок `{}` возвращает `Void`.

## Мотивация

### Зачем нужна эта функциональность?

1. **Синтаксическая согласованность**: устранение исторического багажа старого синтаксиса, унификация стиля
2. **Краткость**: алгоритм HM автоматически выводит типы, сокращая шаблонный код
3. **Типовая безопасность**: алгоритм HM гарантирует типобезопасность; явная аннотация применяется только когда вывод невозможен
4. **Зрелость языка**: алгоритм HM — проверенное решение в современных функциональных языках

### Унифицированная модель синтаксиса

**Основной принцип**: `name: Signature = LambdaBody`

- **Полная форма**: сигнатура (с именами параметров + типы + `->` + тип возврата) + заголовок Lambda (с именами параметров)
- **Правила сокращения**: опускание допускается везде, где это не приводит к неоднозначности
  - `->` нельзя опускать (маркер типа функции; иначе будет распознано как кортеж)
  - **При наличии входных параметров** тип параметра должен быть явно указан хотя бы в одном месте — в сигнатуре или в заголовке lambda
  - Заголовок Lambda можно опустить → если сигнатура уже объявляет имена и типы параметров
  - Тип возврата можно указать явно или опустить при возможности вывода

```yaoxiang
# Полная форма (полная сигнатура + полный заголовок Lambda)
add: (a: Int, b: Int) -> Int = (a, b) => a + b

# Сокращение: опущен заголовок Lambda (сигнатура уже объявляет параметры)
add: (a: Int, b: Int) -> Int = a + b

# Сокращение: опущена сигнатура (заголовок lambda указывает типы параметров)
add = (a: Int, b: Int) => a + b

# ❌ Ошибка: тип параметра не указан ни с одной стороны
# add = (a, b) => a + b
```

### Цели проектирования

```yaoxiang
# === Полная форма ===
add: (a: Int, b: Int) -> Int = (a, b) => { a + b }

# === Сокращённые формы ===
add: (a: Int, b: Int) -> Int = a + b                 # опущен заголовок Lambda
add = (a: Int, b: Int) => a + b                      # опущена сигнатура

# === Функции без параметров ===
main: () -> Void = () => { println("Hello") }          # полная форма
main: () -> Void = { println("Hello") }                # опущен заголовок Lambda
main = { println("Hello") }                            # минимальная форма (выводится как () -> Void)

# === Обобщённые функции (используется унифицированный синтаксис RFC-010) ===
identity: (T: Type) -> ((x: T) -> T) = (x) => x         # полная форма
identity: (T: Type) -> ((x: T) -> T) = x                # опущен заголовок Lambda
identity = (x: T) => x                                  # опущена сигнатура (заголовок lambda указывает типы)

# === Рекурсивные функции ===
factorial: (n: Int) -> Int = (n) => {
    if n <= 1 { 1 } else { n * factorial(n - 1) }
}
```

### Синтаксические правила

| Сценарий | Синтаксис | Описание |
|------|------|----------|
| **Полная форма** | `name: (a: Type, b) -> Ret = (a, b) => { return ... }` | Полная сигнатура + полный заголовок Lambda |
| **Опущен заголовок Lambda** | `name: (a: Type, b: Type) -> Ret = { ... }` | Сигнатура уже объявляет параметры |
| **Опущена сигнатура** | `name = (a: Type, b: Type) => { ... }` | Заголовок lambda указывает типы параметров |
| **Без параметров, полная** | `name: () -> Void = () => { return ... }` | Полная форма для функции без параметров |
| **Без параметров, сокращённая** | `name: () -> Void = { return ... }` | Опущен заголовок Lambda |
| **Без параметров, минимальная** | `name = { return ... }` | Минимальная форма без параметров и возврата |

**Примечание**: последнее выражение в блоке `{ ... }` используется в качестве возвращаемого значения; для досрочного завершения используется `return`. Пустой блок `{}` выводится как `Void`.

**Примечание**: `->` является маркером типа функции и не может быть опущен (иначе будет распознано как кортеж).

**Важно**: выражение `if` использует фигурные скобки `{}` для ветвей и не поддерживает ключевые слова `then/else`:
```yaoxiang
# Правильно: использовать фигурные скобки
if n <= 1 { return 1 } else { return n * factorial(n - 1) }

# Неправильно: ключевые слова then/else не поддерживаются
# if n <= 1 then return 1 else return n * factorial(n - 1)
```

## Предложение

### Алгоритм HM и поддержка полиморфизма высших порядков

**Основная функциональность**: алгоритм HM поддерживает полиморфизм высших порядков (Higher-rank polymorphism) через аннотации обобщённых типов.

**Принцип проектирования**:
- **Функции высших порядков**: при передаче функций в качестве параметров необходимо использовать обобщённые ограничения для типов функций
- **Форма аннотации типа**: `(T: Type) -> ((f: (T) -> T, x: T) -> T)` — обобщённый параметр ограничивает тип функции
- **Рабочий процесс HM**: вывод типов функций через обобщённые параметры, реализация полиморфной композиции функций

**Пояснение примеров**:
```yaoxiang
# ✅ Поддержка полиморфизма высших порядков: обобщённое ограничение параметра типа функции
call_twice: (T: Type) -> ((f: (T) -> T, x: T) -> T) = {
    return f(f(x))
}
# Использование: call_twice((x) => x + 1, 5)  # выводится T=Int

compose: (A: Type, B: Type, C: Type) -> ((f: (B) -> C, g: (A) -> B, x: A) -> C) = {
    return f(g(x))
}
# Использование: compose((x) => x * 2, (x) => x + 1, 5)  # выводится A=Int, B=Int, C=Int

# ❌ Не поддерживается: функция высших порядков без обобщённого ограничения
# bad_hof: (f, x) => f(f(x))  # HM не может вывести, отсутствует обобщённый параметр
```

**Процесс вывода по HM**:
1. Определение параметра функции высшего порядка: `f: (T) -> T`
2. Создание обобщённого ограничения: `(T: Type)`
3. Вывод конкретного типа через обобщённую инстанциацию
4. Реализация полиморфной композиции функций

### Синтаксические правила для Lambda-выражений

**Важное правило**: блок кода `{ ... }` возвращает значение последнего выражения; для досрочного завершения используется `return`. Пустой блок `{}` возвращает `Void`.

| Синтаксическая форма | Синтаксис | Способ возврата |
|---------|------|----------|
| **Форма блока кода** | `{ statements }` | Последнее выражение как возвращаемое значение; можно использовать `return` для досрочного возврата |
| **Форма выражения** | `expression` | Непосредственное возвращение значения выражения |

**Примеры**:
```yaoxiang
main: () -> Void = { println("Hello") }         # возвращает Void (последнее выражение — println)
add: (a: Int, b: Int) -> Int = { a + b }        # возвращает Int (последнее выражение — a + b)
empty: () -> Void = {}                          # пустой блок возвращает Void

# Досрочный возврат: использование return
factorial: (n: Int) -> Int = {
    if n <= 1 { return 1 }
    n * factorial(n - 1)
}

# Форма выражения: непосредственный возврат значения (без return)
add: (a: Int, b: Int) -> Int = a + b            # правильно: форма выражения
main: () -> Void = println("Hello")               # правильно: форма выражения
```

**Ключевая идея**:
1. Определения функций используют алгоритм HM для вывода типов — максимально выводить, выдавать ошибку с явной аннотацией при невозможности вывода
2. **Принцип работы алгоритма HM**: автоматический вывод типов через контекстную информацию — ограничения типов операторов, отношения вызовов функций и т.д.
3. **Поддержка обобщений**: полиморфные функции используют обобщённый синтаксис `(T: Type)` для явного ограничения параметров типа (RFC-010/011)
4. **Границы вывода**: тип возврата и локальные переменные выводимы; типы параметров функций с параметрами требуют явной аннотации (хотя бы в сигнатуре или заголовке lambda)
5. Функции без параметров без возврата используют `name: () -> Void = { ... }`, унифицировано с RFC-010
6. Старый синтаксис выводится из эксплуатации, предоставляются инструменты миграции

**Примеры вывода типов**:
```yaoxiang
# Обобщённые функции: явные параметры типа (используется унифицированный синтаксис RFC-010)
identity: (T: Type) -> ((x: T) -> T) = x
map: (T: Type, R: Type) -> ((f: (T) -> R, list: List(T)) -> List(R)) = {
    result = List(R)()
    for item in list { result.push(f(item)) }
    return result
}

# Полиморфные функции: определение через явные обобщённые ограничения (RFC-010/011)
add: (T: Add) -> ((a: T, b: T) -> T) = a + b
print_sum: (a: Int, b: Int) -> Void = { println(a + b) }  # выводится как (Int, Int) -> Void

# Полиморфизм высших порядков: реализация поддержки HM через обобщённые аннотации типов
call_twice: (T: Type) -> ((f: (T) -> T, x: T) -> T) = { return f(f(x)) }
compose: (A: Type, B: Type, C: Type) -> ((f: (B) -> C, g: (A) -> B, x: A) -> C) = { return f(g(x)) }
```

```yaoxiang
# === Определение функций: вывод типов по алгоритму HM ===

# Стандартные функции: HM выводит тип возврата (типы параметров требуют явного указания)
add = (a: Int, b: Int) => a + b            # выводится как (a: Int, b: Int) -> Int
main = { println("Hello") }                # выводится как () -> Void

# Частично явные параметры: HM выводит оставшуюся часть
print_sum: (a: Int, b: Int) -> Void = { println(a + b) }  # выводится как (Int, Int) -> Void
greet: (name: String) -> Void = { println("Hello " + name) }  # выводится как (String) -> Void

# Обобщённые функции: явное ограничение параметров полиморфного типа (используется унифицированный синтаксис RFC-010)
identity: (T: Type) -> ((x: T) -> T) = x
map: (T: Type, R: Type) -> ((f: (T) -> R, list: List(T)) -> List(R)) = {
    # реализация функции map
    return List(R)()
}

# Рекурсивные функции: вывод через алгоритм HM и рекурсивные ограничения
factorial: (n: Int) -> Int = {
    if n <= 1 { 1 } else { n * factorial(n - 1) }
}

# === Присваивание переменных: вывод типов по алгоритму HM ===

# Явный тип
x: Int = 42

# HM автоматически выводит Int
y = 42                               # выводится как Int

# HM автоматически выводит String
name = "YaoXiang"                    # выводится как String

# HM автоматически выводит Float
pi = 3.14159                         # выводится как Float
```

**Правила вывода типов по HM**:

| Сценарий | Синтаксис | Опускаемые части | Пример |
|------|------|----------|------|
| **Полная форма** | `name: (a: Type, b: Type) -> Ret = (a, b) => ...` | нет | сигнатура + полный заголовок Lambda |
| **Опущен заголовок Lambda** | `name: (a: Type, b: Type) -> Ret = ...` | заголовок Lambda | сигнатура объявляет параметры |
| **Опущена сигнатура** | `name = (a: Type, b: Type) => ...` | сигнатура | заголовок lambda предоставляет типы параметров |
| **Опущен возвращаемый тип Ret** | `name: (a: Type, b: Type) -> = ...` | тип возврата | HM выводит тип возврата |
| **Без параметров, полная** | `name: () -> Void = () => { ... }` | нет | полная форма для функции без параметров |
| **Без параметров, сокращённая** | `name: () -> Void = { ... }` | заголовок Lambda | опущено `() =>` |
| **Без параметров, минимальная** | `name = { ... }` | всё | минимальная форма без параметров и возврата |
| **Присваивание переменной** | `name = value` | тип | HM выводит тип |
| **Явная переменная** | `name: Type = value` | нет | явная аннотация типа |

**Основные принципы**:
- `->` является маркером типа функции и не может быть опущен (иначе будет распознано как кортеж)
- Тип возврата `Ret` может быть опущен, HM выводит его из тела функции
- При наличии входных параметров тип параметра должен быть явно указан хотя бы в одном месте — в сигнатуре или заголовке lambda
- Остальные части могут быть опущены при возможности вывода и отсутствии неоднозначности
- Нет неявных преобразований типов, избегая хаоса как в JavaScript

## Детальное проектирование

### Раскрытие синтаксического сахара

Независимо от степени сокращения, всё в итоге нормализуется к унифицированному промежуточному представлению:

```rust
// Полная форма
add: (a: Int, b: Int) -> Int = (a, b) => a + b

// Раскрытое IR
let add: (Int, Int) -> Int = |a: Int, b: Int| -> Int {
    return a + b
};

// Опущен заголовок Lambda
add: (a: Int, b: Int) -> Int = a + b

// Раскрытое IR (идентично полной форме)
let add: (Int, Int) -> Int = |a: Int, b: Int| -> Int {
    return a + b
};

// Опущена сигнатура (заголовок lambda указывает типы параметров)
add = (a: Int, b: Int) => a + b

// Раскрытое IR
let add: (Int, Int) -> Int = |a: Int, b: Int| -> Int {
    a + b
};
```

### Определение синтаксиса

```bnf
function_def ::= identifier ':' type_expr '=' expression
               | identifier '=' expression
               | identifier '=' block                    # минимальная форма: без параметров и возврата

identifier ::= [a-zA-Z_][a-zA-Z0-9_]*

type_expr ::= identifier                     # ссылка на тип
       | '()'                          # пустой тип
       | '(' parameters ')' '->' type_expr   # функциональный тип (имена параметров в сигнатуре)
       | type_expr '->' type_expr            # простой функциональный тип
       | identifier '(' type_expr (',' type_expr)* ')'  # применение типа

expression ::= '(' parameters ')' '=>' block
             | '(' ')' '=>' block
             | '(' parameters ')' '=>' expression

parameters ::= parameter (',' parameter)*
parameter ::= identifier                # вывод типа
            | identifier ':' type_expr      # частично явный тип

block ::= '{' statement (',' statement)* '}'
        | expression

statement ::= identifier ':' expression  # оператор присваивания
           | expression                  # выражение (выполняется, но не возвращает)
           | 'return' expression         # оператор возврата (возвращает указанное значение)

# Примечание: блок кода возвращает значение последнего выражения; пустой блок {} выводится как Void
# Например: { 1 + 1 } возвращает Int; { println("Hello") } возвращает Void
# Примечание: обобщённые параметры используют синтаксис (T: Type), являясь частью функционального типа,
# не требуют отдельного BNF-правила
```

### Обработка ошибок

```yaoxiang
# === Примеры ошибок компиляции ===

# Ошибка 1: несоответствие типа возврата из блока кода
add: (a: Int, b: Int) -> Int = { println(a + b) }
// Ошибка: последнее выражение блока — println(...), возвращает Void, но сигнатура ожидает Int
// Правильно: add: (a: Int, b: Int) -> Int = a + b
// или: add: (a: Int, b: Int) -> Int = { a + b }

# Ошибка 2: использование необъявленного параметра типа
identity: (x: T) -> T = x
// Ошибка: T не объявлен; требуется явный обобщённый параметр (RFC-010)
// Правильно: identity: (T: Type) -> ((x: T) -> T) = x

# Правильно: HM выводит тип возврата
double = (x: Int) => x + x

# Полная форма (постепенное сокращение)
double: (x: Int) -> Int = (x) => x + x                # полная
double: (x: Int) -> Int = x + x                       # опущен заголовок Lambda
double = (x: Int) => x + x                            # опущен тип возврата (HM выводит возвращаемый)
// double = (x) => x + x                               # ❌ Тип параметра не может быть опущен с обеих сторон
```

## Компромиссы

### Преимущества

- **Унификация синтаксиса**: модель `name: Signature = LambdaBody` охватывает все сценарии
- **Гибкие сокращения**: любая часть может быть опущена при возможности вывода по HM
- **Типовая безопасность**: алгоритм HM гарантирует типобезопасность, отсутствие неявных преобразований типов
- **Поддержка рекурсии**: алгоритм HM и рекурсивные ограничения автоматически выводят типы
- **Нулевая нагрузка**: плавный переход от полной к минимальной форме

### Недостатки

- **Стоимость миграции**: старый код требует инструмента преобразования
- **Затраты на обучение**: необходимо понимать модель «полная форма + произвольные сокращения»

## Альтернативные решения

| Решение | Описание | Почему не выбрано |
|------|------|-----------|
| Вывод типов по алгоритму HM | Использование алгоритма Hindley-Milner для вывода типов | ✅ **Принято**, стандарт современных функциональных языков |
| Явные объявления типов | Все типы должны быть явно написаны | Противоречит принципу упрощения синтаксиса, увеличивает шаблонный код |
| Сохранение старого синтаксиса | Поддержка старого и нового синтаксиса одновременно | Раскол синтаксиса, высокая стоимость поддержки |
| Ключевое слово fn | Введение fn для различия функций и переменных | Противоречит принципу «функция — это lambda» |

## Стратегия реализации

### Фазы

1. **Фаза 1: Синтаксический анализ и алгоритм HM** (v0.3)
   - Реализация нового синтаксиса `name = lambda` + вывод типов по алгоритму HM
   - Реализация заполнения значений по умолчанию для функций без параметров и возврата

2. **Фаза 2: Инструмент миграции** (v0.3)
   - Разработка инструмента `yaoxiang-migrate --old-to-new`
   - Автоматическое преобразование кода со старым синтаксисом

3. **Фаза 3: Верификация и документация** (v0.3)
   - Завершение верификации миграции старого кода
   - Обновление документации

### Инструмент миграции

```bash
# Миграция одного файла
yaoxiang-migrate --old-to-new src/main.yaoxiang

# Миграция всего проекта
yaoxiang-migrate --old-to-new --recursive src/

# Предпросмотр миграции (без изменения файлов)
yaoxiang-migrate --old-to-new --dry-run src/main.yaoxiang
```

Правила миграции:
```yaoxiang
# Старый синтаксис
add(Int, Int) -> Int = (a, b) => { a + b }
main() -> Int = { println("Hello"); 0 }
main() = { println("Hello") }

# === Новый синтаксис: полная форма (полная сигнатура + полный заголовок Lambda) ===
add: (a: Int, b: Int) -> Int = (a, b) => a + b
main: () -> Void = () => { println("Hello") }

# === Сокращение: опущен заголовок Lambda ===
add: (a: Int, b: Int) -> Int = a + b
main: () -> Void = { println("Hello") }

# === Сокращение: вывод по HM ===
add = (a: Int, b: Int) => a + b              # выводится как (a: Int, b: Int) -> Int
main = { println("Hello") }                  # выводится как () -> Void

# === Минимальная форма ===
main = {                                      # эквивалентно main: () -> Void = { ... }
    println("Hello")
}
```

### Зависимости

- Нет внешних зависимостей
- Может быть реализовано независимо

### Риски

| Риск | Влияние | Меры снижения |
|------|------|---------|
| Пропуск при миграции | Ошибка компиляции старого кода | Предоставление инструмента миграции, покрытие всех паттернов старого синтаксиса |
| Ошибки парсера | Нестабильный синтаксический анализ | Достаточное тестовое покрытие |

## Открытые вопросы

> Следующие вопросы уже решены в процессе проектирования, записаны в Приложении A.

- ~~Q1: Следует ли сохранить сверхкраткую запись `main() = body`?~~ → Решено: сохранена как `main = { ... }`
- ~~Q2: Сохраняется ли `:` после имени функции?~~ → Решено: опционально; но для функций с параметрами тип параметра всё ещё должен быть указан в сигнатуре или заголовке lambda
- ~~Q3: Поддерживает ли алгоритм HM вывод типов параметров?~~ → Решено: возвращаемое значение/локальные переменные выводимы; типы параметров функций с параметрами требуют явной аннотации
- ~~Q4: Вводится ли ключевое слово `fn`?~~ → Решено: не вводится, функция — это lambda
- ~~Q5: Какова стратегия миграции старого кода?~~ → Решено: предоставляется инструмент `yaoxiang-migrate`
- ~~Q6: Как использовать обобщённые функции?~~ → Решено: используется унифицированный синтаксис RFC-010 `(T: Type)`

---

## Приложения

### Приложение A: Справочник синтаксиса определения функций различных языков

| Язык | Стиль синтаксиса | Особенности |
|------|---------|------|
| Rust | `fn add(a: i32, b: i32) -> i32 { ... }` | Ключевое слово + аннотации типов |
| Haskell | `add a b = ...` / `add :: Int -> Int -> Int` | Типовая сигнатура отдельно |
| OCaml | `let add a b = ...` | Типы параметров можно опускать |
| MoonBit | `fn add(a: Int, b: Int): Int { ... }` | Краткие аннотации типов |
| TypeScript | `const add = (a: number, b: number): number => ...` | Стиль Lambda |
| Scala | `def add(a: Int, b: Int): Int = { ... }` | Ключевое слово def |
| **YaoXiang** | `name = (a: Int, b: Int) => a + b` | **Функция = lambda, HM выводит возвращаемое значение** |

### Приложение B: Журнал проектных решений

| Решение | Решено | Дата | Записал |
|------|------|------|--------|
| Стиль синтаксиса | Новый синтаксис `name: (params) -> Return = body` + вывод по HM | 2026-02-03 | @沫郁酱 |
| Расположение параметров | Имена параметров объявляются в сигнатуре, унифицировано с RFC-010 | 2026-02-03 | @沫郁酱 |
| Заполнение по умолчанию | Функции без параметров можно опускать сигнатуру, пустой блок `{}` выводится как `Void` | 2026-02-03 | @沫郁酱 |
| Вывод типов | Алгоритм HM автоматически выводит, явная аннотация при невозможности | 2026-01-06 | @沫郁酱 |
| Старый синтаксис | Выводится из эксплуатации, предоставляется инструмент миграции | 2026-01-06 | @沫郁酱 |
| Ключевое слово fn | Не вводится | 2026-01-06 | @沫郁酱 |
| Объявление рекурсии | Алгоритм HM и рекурсивные ограничения автоматически выводят | 2026-01-06 | @沫郁酱 |

### Приложение C: Глоссарий

| Термин | Определение |
|------|------|
| Алгоритм HM | Алгоритм вывода типов Hindley-Milner, автоматически выводит типы функций и переменных |
| Обобщения | Использование параметров типа `(T: Type)` для ограничения полиморфных функций, например `identity: (T: Type) -> ((x: T) -> T) = x` (RFC-010) |
| Заполнение типов по умолчанию | Функции без параметров и возврата опускают `-> Void`, компилятор заполняет автоматически |
| Синтаксический сахар | Синтаксическое упрощение для улучшения читаемости кода |
| Нормализация | Преобразование синтаксических форм в унифицированное внутреннее представление |
| Функция — это lambda | Функция по сути является переменной lambda, тип автоматически выводится по алгоритму HM |

---

## Ссылки

- [Дизайн языка MoonBit](https://moonbitlang.com/)
- [Синтаксис функций Rust](https://doc.rust-lang.org/book/ch03-03-how-functions-work.html)
- [Типовая система Haskell](https://www.haskell.org/tutorial/patterns.html)
- [Вывод типов OCaml](https://v2.ocaml.org/manual/)
```