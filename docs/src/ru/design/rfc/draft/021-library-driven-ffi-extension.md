```markdown
---
title: "RFC-021: Расширение FFI на основе библиотек и поддержка межъязыковых вызовов"
---

# RFC-021: Расширение FFI на основе библиотек и поддержка межъязыковых вызовов

> **Статус**: Черновик
> **Автор**: Чэнь Сюй
> **Дата создания**: 2026-03-14
> **Последнее обновление**: 2026-05-29

> **Связанные документы**:
> - [RFC-001: Модель 并作 и система обработки ошибок](./001-concurrent-model-error-handling.md)
> - [RFC-008: Модель 并发 Runtime и развязка планировщика](./008-runtime-concurrency-model.md)
> - [План реализации FFI](../reference/plan/completed/FFI.md)

## Аннотация

В данном документе предлагается схема расширения FFI (Foreign Function Interface — внешний функциональный интерфейс), управляемая **библиотеками**. Единственной точкой входа в FFI является объявление `native("symbol")` + среда выполнения `FfiRegistry`, при этом в ядре не вводится второй механизм. Поверх этого стандартная библиотека обеспечивает возможности динамической загрузки библиотек, межъязыковых вызовов и привязок. Привязки вызовов для конкретных языков (таких как C, Python, JavaScript) автоматически генерируются официальным инструментарием или создаются проектами по мере необходимости.

## Мотивация

### Недостатки текущей реализации

Текущая реализация FFI уже обладает следующими возможностями:
- Синтаксис объявления внешних функций `native("symbol")`
- Таблица регистрации функций `FfiRegistry`

Однако функциональность относительно ограничена:
- Отсутствует поддержка динамической загрузки библиотек
- Нет инфраструктуры для межъязыковых вызовов
- Отсутствуют инструменты автоматической генерации привязок

### Философия дизайна

YaoXiang придерживается принципа **«Ядро простое, сложность опускается в библиотеки»**:

> **Хороший вкус (Good Taste)**: Язык должен предоставлять атомарные возможности, а не полный набор функций. Сложность должна решаться через библиотеки, а не накапливаться в компиляторе.

Therefore, this approach:
- ✅ **Нулевые изменения синтаксиса** — полная обратная совместимость, точка входа в FFI — только `native("symbol")`
- ✅ **Библиотека — это язык** — функциональность расширяется через стандартную библиотеку
- ✅ **Автоматизация инструментарием** — привязки генерируются автоматически с помощью `yx-bindgen`, без ручного сопровождения
- ✅ **Постепенное улучшение** — разработчики добавляют функциональность по необходимости

## Предложение

### 1. Расширение основной FFI-библиотеки

Расширение модуля `std.ffi`. Примечание: все вызовы внешних функций по-прежнему осуществляются через объявление `native("symbol")`, `std.ffi` предоставляет только вспомогательные возможности.

#### 1.1 Динамическая загрузка библиотек

```yaoxiang
import ffi

# Загрузка динамической библиотеки (.so/.dll/.dylib)
lib = ffi.load_library("./libmyext.so")

# Получение символов функций из библиотеки, возврат имени symbol для native
ffi.register_library_symbols(lib, [
    "my_function",
    "another_func",
])
```

`load_library` возвращает дескриптор `DynamicLibrary`, `register_library_symbols` регистрирует имена символов в известной таблице `FfiRegistry`. After that, пользователь по-прежнему использует объявление `native`:

```yaoxiang
my_func: (a: Int, b: Int) -> Int = native("my_function")
```

Никаких вторых механизмов вызова, никаких обёрток `try_call`.

#### 1.2 Управление библиотеками

```yaoxiang
# Список загруженных библиотек
loaded = ffi.loaded_libraries()

# Выгрузка библиотеки
ffi.unload_library(lib)

# Проверка версии библиотеки
ffi.check_version(lib, "1.0.0")
```

#### 1.3 Разрешение символов

```yaoxiang
# Поиск символа по имени (возврат структуры Symbol)
sin_sym = ffi.dlsym("libm.so", "sin")
```

Соглашения о межъязыковых вызовах и преобразование типов обрабатываются не через универсальные обёртки во время выполнения, а генерируются `yx-bindgen` на этапе компиляции.

### 2. Реализация динамической загрузки библиотек

#### 2.1 Основные структуры данных

```rust
pub struct DynamicLibrary {
    handle: *mut std::ffi::c_void,
    path: String,
}

impl DynamicLibrary {
    pub fn load(path: &str) -> Result<Self, FfiError>;
    pub fn get_symbol(&self, name: &str) -> Result<*mut std::ffi::c_void, FfiError>;
    pub fn unload(self) -> Result<(), FfiError>;
}
```

#### 2.2 Типы ошибок

```rust
pub enum FfiError {
    LibraryNotFound { name: String, os_error: Option<OsError> },
    SymbolNotFound { name: String, os_error: Option<OsError> },
    CallFailed { message: String, os_error: Option<OsError> },
    Timeout,
}

pub struct OsError {
    pub code: i32,
    pub message: String,
}
```

`OsError` содержит нативные коды ошибок платформы (`dlerror()` в Linux, `GetLastError()` в Windows), обеспечивая отлаживаемость.

### 3. Привязки для множества языков: инструментарий

Отказ от «фантазии» о том, что «сообщество языков поддерживает библиотеки привязок». Вместо этого — автоматическая генерация официальным инструментарием.

#### 3.1 Архитектурный дизайн

```
┌───────────────────────────────────────────────┐
│  YaoXiang код                                 │
│                                               │
│  // Пользователь пишет только объявление native │
│  my_func: (a: Int) -> Int = native("my_func") │
└───────────────────────────────────────────────┘
         ↑                          ↑
         |  Время компиляции         |  Время выполнения
┌──────────────────┐   ┌────────────────────────┐
│  yx-bindgen       │   │  std.ffi + FfiRegistry  │
│  (C заголовочные → .yx) │   │  - dlopen/dlsym         │
│                   │   │  - LoadLibrary/GetProc  │
└──────────────────┘   └────────────────────────┘
```

#### 3.2 Генератор привязок (`yx-bindgen`)

`yx-bindgen` — это автономный CLI-инструмент, который генерирует код привязок YaoXiang FFI из заголовочных файлов C:

```bash
yx-bindgen --header /usr/include/sqlite3.h --output sqlite3.yx
```

Пример сгенерированного результата:

```yaoxiang
# Автогенерация, не редактировать вручную
# Источник: /usr/include/sqlite3.h

sqlite3_open: (filename: *const u8, ppDb: *mut *mut opaque) -> Int
    = native("sqlite3_open")

sqlite3_close: (db: *mut opaque) -> Int
    = native("sqlite3_close")

sqlite3_exec: (
    db: *mut opaque,
    sql: *const u8,
    callback: *mut opaque,
    arg: *mut opaque,
    errmsg: *mut *mut u8,
) -> Int
    = native("sqlite3_exec")
```

`yx-bindgen` поддерживается официально, гарантируя:
- Полноту映射 типов (`int` → `Int`, `char*` → `*const u8`, `void*` → `*mut opaque`)
- Выравнивание layout структур (автоматическая `#[repr(C)]` эквивалентность)
- Преобразование сигнатур callback

#### 3.3 Официально поддерживаемые пакеты привязок

Основная команда YaoXiang не обязуется поддерживать универсальные библиотеки привязок для всех языков, но предоставляет официальный пакет привязок `libc` (подмножество POSIX + Windows API) в качестве примера лучших практик FFI и базовых возможностей.

Привязки для других языков и библиотек:
- Генерируются самостоятельно с помощью `yx-bindgen`
- Могут быть опубликованы как пакеты YaoXiang (например, `libsqlite3`, `libcurl`, `libsdl2`)
- Основная команда не несёт ответственности за поддержку, но предоставляет механизм публикации и управления версиями пакетов

### 4. Уровень преобразования типов

#### 4.1 Преобразование типов во время компиляции

Преобразование типов осуществляется не через обёртки времени выполнения, а статически определяется при генерации `yx-bindgen`:

| Тип C | Тип YaoXiang | Способ преобразования |
|--------|---------------|----------|
| `int` | `Int` | Прямая передача значения |
| `char*` | `*const u8` | Передача указателя |
| `void*` | `*mut opaque` | Непрозрачный указатель |
| `struct T` | `extern struct T` | Соответствие layout памяти |
| `int*` | `*mut Int` | Передача указателя (изменяемый) |
| `const int*` | `*const Int` | Передача указателя (только чтение) |

#### 4.2 Ручное преобразование (вспомогательные функции стандартной библиотеки)

```yaoxiang
# Явное преобразование
raw_ptr = ffi.to_pointer(my_bytes)
c_string = ffi.to_c_string(my_string)
```

### 5. Модель владения памятью

#### 5.1 Основные принципы

При каждом выделении памяти через границу FFI необходимо дать однозначный ответ на два вопроса:
1. **Кто выделяет?** (С-сторона `malloc` / сторона YaoXiang runtime)
2. **Кто освобождает?** (С-сторона `free` / сторона YaoXiang runtime)

При генерации `yx-bindgen` добавляет аннотации для распространённых паттернов:

```yaoxiang
# Выделено C, вызывающая сторона должна освободить
sqlite3_exec: (...) -> Int
    = native("sqlite3_exec")
    # memory: C-allocated, caller must free errmsg via sqlite3_free

# Вызывающая сторона выделяет указатель
read: (fd: Int, buf: *mut u8, count: Int) -> Int
    = native("read")
    # memory: caller-allocated buf
```

Среда выполнения не выполняет автоматическое управление памятью для указателей, пересекающих границу FFI — ответственность за владение лежит на вызывающей стороне.

#### 5.2 Обработка строк

`char*`, возвращённый функциями C, при преобразовании в YaoXiang `String` немедленно копируется. Владение исходным указателем определяется соглашением функции C (декларируется через аннотации), автоматическое освобождение не выполняется.

### 6. Вопросы безопасности

#### 6.1 Параллельная безопасность

Вызовы FFI-функций **по умолчанию не участвуют в планировании DAG**, рассматриваются как блокирующие операции. Функции C, подтверждённо являющиеся реентерабельными, могут быть помечены как `@concurrent`:

```yaoxiang
# Чистая функция, без глобального состояния, может быть并发
sin_safe: (x: Float) -> Float = native("sin")
    # reentrant: true

# Имеет глобальное состояние, не может быть并发
strtok: (s: *const u8, delim: *const u8) -> *const u8 = native("strtok")
    # reentrant: false
```

`yx-bindgen` по возможности аннотирует информацию о реентерабельности для стандартных функций C (`_r` варианты `strtok` и т.д.).

**Требования к асинхронным вызывающим сторонам:** перед вызовом FFI-функции вызывающая сторона должна убедиться, что целевая функция является реентерабельной. Среда выполнения не выполняет автоматическое обнаружение — это неразрешимая задача для статического анализа.

#### 6.2 Изоляция ошибок

- Ошибки FFI-вызовов распространяются через тип `Result` (если функция объявила тип возврата Result)
- Механизм таймаута предотвращает взаимоблокировку внешних функций

```yaoxiang
# Вызов с таймаутом (реализуется на уровне FfiRegistry)
result = ffi.call_with_timeout("blocking_func", 5000)  # Таймаут 5 секунд
```

#### 6.3 Безопасность указателей

- Параметры-указатели требуют маркера `unsafe` со стороны YaoXiang
- Время жизни указателей, пересекающих границу FFI, гарантируется вызывающей стороной

### 7. Изменения в компиляторе

**Нулевые изменения синтаксиса** — требуется только объявление `native("symbol")`, которое уже реализовано в текущем компиляторе.

В интерпретаторе/среде выполнения необходимо добавить:
- Инструкции динамической загрузки библиотек (привязки FFI для `DynamicLibrary`)
- Механизм таймаута

### 8. Функции, отклонённые к рассмотрению

Следующие функции после проверки явно исключены и более не включены в RFC:

- **`ffi.try_call`**: Избыточно, уже есть `native` + тип возврата `Result`
- **`ffi.verify_signature`**: Среда выполнения делает работу компилятора — неправильный уровень абстракции
- **`ffi.async_call`**: Требует уточнения модели контракта реентерабельности перед рассмотрением
- **Таблица привязок, поддерживаемая сообществом**: Неосуществимо, переход к схеме инструментария `yx-bindgen`

## Компромиссы

### Преимущества

- ✅ **Нулевые изменения синтаксиса** — точка входа в FFI только `native("symbol")`, полная обратная совместимость
- ✅ **Библиотека — это язык** — функциональность вводится через стандартную библиотеку постепенно
- ✅ **Инструментарий как основа** — `yx-bindgen` автоматически обрабатывает генерацию привязок
- ✅ **Безопасность памяти** — чёткая модель владения, отсутствие use-after-free из-за автоматической сборки мусора
- ✅ **Отлаживаемость** — ошибки содержат нативные коды ошибок ОС

### Недостатки

- ⚠️ Безопасность типов ограничена выразительностью заголовочных файлов C (`void*` невозможно статически различить)
- ⚠️ `yx-bindgen` требует постоянной поддержки для отслеживания эволюции стандарта C
- ⚠️ Привязки для не-C языков (Python/JS/Java) требуют индивидуальной обработки проектами, унифицированного решения нет

## Стратегия реализации

### Этап 1: Основная библиотека (v0.7)

- [ ] Расширение модуля `std.ffi`
- [ ] Реализация структуры `DynamicLibrary`
- [ ] Поддержка Linux/macOS (`dlopen`/`dlsym`)
- [ ] Поддержка Windows (`LoadLibrary`/`GetProcAddress`)
- [ ] Добавление механизма таймаута в среду выполнения
- [ ] Модульные тесты

### Этап 2: yx-bindgen (v0.8)

- [ ] Реализация парсера заголовочных файлов C (на основе существующих привязок Clang или самописного парсера)
- [ ] Система映射 типов
- [ ] Генерация объявлений `native("symbol")`
- [ ] Генерация layout структур
- [ ] Интеграционные тесты: генерация привязок для реальных C-библиотек SQLite3, libcurl и др.

### Этап 3: Основа экосистемы (v0.9)

- [ ] Публикация официального пакета привязок `libc` (подмножество POSIX + Windows API)
- [ ] Разработка спецификации публикации пакетов привязок
- [ ] Документация: лучшие практики FFI, владение памятью, контракты параллельной безопасности

## Связь с другими RFC

- **RFC-001**: Вызовы FFI как внешние функции, по умолчанию `@block` (не участвуют в планировании DAG)
- **RFC-008**: Дизайн развязки планировщика, вызовы FFI как независимые задачи
- **RFC-020**: Интеграция динамических модулей и FFI

## Открытые вопросы

- [ ] Должен ли `yx-bindgen` быть интегрирован в систему сборки (`yaoxiang build`)?
- [ ] Как спроектировать поддержку FFI для платформы WASM? (Механизм импорта WASM полностью отличается от dlopen)
- [ ] Нужна ли поддержка `cxx-bindgen` для обработки name mangling C++ (опционально, после v1.0)?

---

## Приложение A: Журнал решений по дизайну

| Решение | Вердикт | Причина | Дата | Записал |
|------|------|------|------|--------|
| Унификация точки входа FFI | Только `native("symbol")` | Избежать расщепления API | 2026-05-29 | Чэнь Сюй |
| Исключить `try_call` | Не реализовывать | Избыточно, есть тип Result | 2026-05-29 | Чэнь Сюй |
| Исключить `verify_signature` | Не реализовывать | Среда выполнения делает работу компилятора | 2026-05-29 | Чэнь Сюй |
| Таблица привязок сообщества → Инструментарий | `yx-bindgen` автогенерация | Невыполнимая фантазия | 2026-05-29 | Чэнь Сюй |
| Коды ошибок ОС | `FfiError` обязательно содержит `os_error` | Неотлаживаемый API бесполезен | 2026-05-29 | Чэнь Сюй |
| Нулевые изменения синтаксиса | Реализация через библиотеки | Принцип простоты ядра | 2026-03-14 | Чэнь Сюй |
| Динамическая загрузка библиотек | Использовать dlopen/dlsym | Стандартный интерфейс ОС | 2026-03-14 | Чэнь Сюй |
| Обработка ошибок | Использовать тип Result | Согласованность | 2026-03-14 | Чэнь Сюй |

## Приложение B: Примеры кода

### Полный пример: использование C-библиотеки

```yaoxiang
# Загрузка C математической библиотеки
libm = ffi.load_library("libm.so")

# Регистрация C символов в таблице среды выполнения (yx-bindgen сделает это во время компиляции)
ffi.register_library_symbols(libm, ["sin", "cos", "sqrt"])

# Использование через объявление native
sin_f: (x: Float) -> Float = native("sin")
cos_f: (x: Float) -> Float = native("cos")

# Прямой вызов
result = sin_f(3.14159 / 2)

# Использование Result при вызове C-функций, которые могут завершиться неудачей
file_open: (path: *const u8, mode: *const u8) -> Result(*mut opaque, Int)
    = native("fopen")
```

### Использование yx-bindgen

```bash
# Автогенерация всех объявлений, ручное написание не требуется
yx-bindgen --header /usr/include/math.h --output math_bindings.yx

# Подключение в YaoXiang
import "math_bindings.yx"
# sin_f / cos_f и др. автоматически объявлены как native("sin") / native("cos")
```

---

## Библиография

- [RFC-001: 并作 модель и система обработки ошибок](./001-concurrent-model-error-handling.md)
- [RFC-008: 并发 модель Runtime и развязка планировщика](./008-runtime-concurrency-model.md)
- [План реализации FFI](../reference/plan/completed/FFI.md)
- [Документация Python ctypes](https://docs.python.org/3/library/ctypes.html)
- [Rust libloading crate](https://docs.rs/libloading/latest/libloading/)
```