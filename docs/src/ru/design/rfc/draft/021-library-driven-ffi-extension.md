# RFC-021: Расширение FFI на основе библиотек и поддержка межъязыковых вызовов

> **Статус**: Черновик
> **Автор**: Чэнь Сюй
> **Дата создания**: 2026-03-14
> **Последнее обновление**: 2026-05-29

> **См. также**:
> - [RFC-001: Модель 并作 и система обработки ошибок](./001-concurrent-model-error-handling.md)
> - [RFC-008: Модель 并作 Runtime и проектирование разделения планировщика](./008-runtime-concurrency-model.md)
> - [План реализации FFI](../reference/plan/completed/FFI.md)

## Аннотация

В данном документе представлен план расширения FFI (Foreign Function Interface — внешний функциональный интерфейс), управляемого **библиотеками**. Единственной точкой входа в FFI является объявление `native("symbol")` + среда выполнения `FfiRegistry`. В ядре не вводится второй набор механизмов. На этой основе стандартная библиотека обеспечивает динамическую загрузку библиотек, межъязыковые вызовы и связывание и т. д. Привязки вызовов для конкретных языков (таких как C, Python, JavaScript) автоматически генерируются официальным инструментарием или разрабатываются проектами по мере необходимости.

## Мотивация

### Недостатки текущей реализации

Текущая реализация FFI уже обладает следующими возможностями:

- Синтаксис `native("symbol")` для объявления внешних функций
- Функциональная регистратура `FfiRegistry`

Однако функциональность относительно ограничена:

- Отсутствует поддержка динамической загрузки библиотек
- Нет инфраструктуры для межъязыковых вызовов
- Отсутствуют инструменты автоматической генерации привязок

### Философия проектирования

YaoXiang следует принципу **«Ядро просто, сложность спускается в библиотеки»**:

> **Хороший вкус (Good Taste)**: задача языка — предоставлять атомарные возможности, а не универсальный функциональный набор. Сложность следует решать через библиотеки, а не нагромождать в компиляторе.

Поэтому данный план:

- ✅ **Нулевые синтаксические изменения** — полная обратная совместимость, единственная точка входа в FFI — это `native("symbol")`
- ✅ **Библиотека как язык** — функциональность расширяется через стандартную библиотеку
- ✅ **Автоматизация инструментарием** — привязки автоматически генерируются `yx-bindgen`, без ручного сопровождения
- ✅ **Постепенное улучшение** — разработчики подключают функциональность по необходимости

## Предложение

### 1. Расширение основной FFI-библиотеки

Расширение модуля `std.ffi`. Примечание: все вызовы внешних функций по-прежнему осуществляются через объявление `native("symbol")`, `std.ffi` предоставляет только вспомогательные возможности.

#### 1.1 Динамическая загрузка библиотек

```yaoxiang
import ffi

# Загрузка динамической библиотеки (.so/.dll/.dylib)
lib = ffi.load_library("./libmyext.so")

# Получение символов функций из библиотеки, возврат имени symbol,
# доступного для native
ffi.register_library_symbols(lib, [
    "my_function",
    "another_func",
])
```

`load_library` возвращает дескриптор `DynamicLibrary`, `register_library_symbols` регистрирует имена символов в таблице известных значений FfiRegistry. После этого пользователь по-прежнему использует объявление `native`:

```yaoxiang
my_func: (a: Int, b: Int) -> Int = native("my_function")
```

Нет второго набора синтаксиса вызова, нет обёрток `try_call`.

#### 1.2 Управление библиотеками

```yaoxiang
# Перечисление загруженных библиотек
loaded = ffi.loaded_libraries()

# Выгрузка библиотеки
ffi.unload_library(lib)

# Проверка версии библиотеки
ffi.check_version(lib, "1.0.0")
```

#### 1.3 Разрешение символов

```yaoxiang
# Поиск символа по имени (возврат структуры Symbol)
sin_sym = ffi.dlsym("libm.so", "sin")
```

Соглашения о межъязыковых вызовах и преобразование типов не обрабатываются во время выполнения через универсальные обёртки, а генерируются `yx-bindgen` на этапе компиляции.

### 2. Реализация динамической загрузки библиотек

#### 2.1 Основные структуры данных

```rust
pub struct DynamicLibrary {
    handle: *mut std::ffi::c_void,
    path: String,
}

impl DynamicLibrary {
    pub fn load(path: &str) -> Result<Self, FfiError>;
    pub fn get_symbol(&self, name: &str) -> Result<*mut std::ffi::c_void, FfiError>;
    pub fn unload(self) -> Result<(), FfiError>;
}
```

#### 2.2 Типы ошибок

```rust
pub enum FfiError {
    LibraryNotFound { name: String, os_error: Option<OsError> },
    SymbolNotFound { name: String, os_error: Option<OsError> },
    CallFailed { message: String, os_error: Option<OsError> },
    Timeout,
}

pub struct OsError {
    pub code: i32,
    pub message: String,
}
```

`OsError` содержит нативные коды ошибок платформы (`dlerror()` в Linux, `GetLastError()` в Windows), что обеспечивает отлаживаемость.

### 3. Привязки для нескольких языков: решение на основе инструментария

Отказ от «фантазии» о том, что «сообщество языков будет поддерживать библиотеки привязок». Вместо этого — автоматическая генерация официальным инструментарием.

#### 3.1 Архитектурный проект

```
┌───────────────────────────────────────────────┐
│  Код YaoXiang                                 │
│                                               │
│  // Пользователь пишет только объявления native│
│  my_func: (a: Int) -> Int = native("my_func")  │
└───────────────────────────────────────────────┘
         ↑                          ↑
         |  Время компиляции        |  Время выполнения
┌──────────────────┐   ┌────────────────────────┐
│  yx-bindgen       │   │  std.ffi + FfiRegistry  │
│  (C headers → .yx)│   │  - dlopen/dlsym         │
│                   │   │  - LoadLibrary/GetProc  │
└──────────────────┘   └────────────────────────┘
```

#### 3.2 Генератор привязок (`yx-bindgen`)

`yx-bindgen` — это автономный инструмент CLI, который генерирует код привязок YaoXiang FFI из заголовочных файлов C:

```bash
yx-bindgen --header /usr/include/sqlite3.h --output sqlite3.yx
```

Пример сгенерированного результата:

```yaoxiang
# Автоматически сгенерировано, не редактировать вручную
# Source: /usr/include/sqlite3.h

sqlite3_open: (filename: *const u8, ppDb: *mut *mut opaque) -> Int
    = native("sqlite3_open")

sqlite3_close: (db: *mut opaque) -> Int
    = native("sqlite3_close")

sqlite3_exec: (
    db: *mut opaque,
    sql: *const u8,
    callback: *mut opaque,
    arg: *mut opaque,
    errmsg: *mut *mut u8,
) -> Int
    = native("sqlite3_exec")
```

`yx-bindgen` поддерживается официально, что гарантирует:

- Полноту映射 типов (`int` → `Int`, `char*` → `*const u8`, `void*` → `*mut opaque`)
- Выравнивание layout структур (автоматический эквивалент `#[repr(C)]` и т. д.)
- Преобразование сигнатур callback-функций

#### 3.3 Официально поддерживаемые пакеты привязок

Основная команда YaoXiang не берёт на себя обязательств по поддержке универсальных библиотек привязок для всех языков, но предоставляет официальный пакет привязок `libc` (подмножество POSIX + Windows API) в качестве примера лучших практик FFI и базовых возможностей.

Привязки для других языков и библиотек:

- Генерируются самостоятельно с помощью `yx-bindgen`
- Могут публиковаться как пакеты YaoXiang (например, `libsqlite3`, `libcurl`, `libsdl2`)
- Основная команда не несёт ответственности за поддержку, но предоставляет механизм публикации пакетов и управления версиями

### 4. Уровень преобразования типов

#### 4.1 Преобразование типов во время компиляции

Преобразование типов не выполняется через обёртки во время выполнения, а статически определяется при генерации `yx-bindgen`:

| Тип C | Тип YaoXiang | Способ преобразования |
|--------|---------------|----------|
| `int` | `Int` | Прямая передача значения |
| `char*` | `*const u8` | Передача указателя |
| `void*` | `*mut opaque` | Непрозрачный указатель |
| `struct T` | `extern struct T` | Соответствие layout в памяти |
| `int*` | `*mut Int` | Передача указателя (изменяемый) |
| `const int*` | `*const Int` | Передача указателя (только для чтения) |

#### 4.2 Ручное преобразование (вспомогательные средства стандартной библиотеки)

```yaoxiang
# Явное преобразование
raw_ptr = ffi.to_pointer(my_bytes)
c_string = ffi.to_c_string(my_string)
```

### 5. Модель владения памятью

#### 5.1 Базовые принципы

При каждом выделении памяти через границу FFI необходимо дать чёткий ответ на два вопроса:

1. **Кто выделил?** (C-сторона `malloc` / среда выполнения YaoXiang)
2. **Кто освобождает?** (C-сторона `free` / среда выполнения YaoXiang)

При генерации `yx-bindgen` добавляет аннотации для распространённых паттернов:

```yaoxiang
# Выделено C, вызывающая сторона должна освободить
sqlite3_exec: (...) -> Int
    = native("sqlite3_exec")
    # memory: C-allocated, caller must free errmsg via sqlite3_free

# Указатель выделяется вызывающей стороной
read: (fd: Int, buf: *mut u8, count: Int) -> Int
    = native("read")
    # memory: caller-allocated buf
```

Среда выполнения не выполняет автоматическое управление памятью для ссылок на указатели через границу FFI — владение чётко возлагается на вызывающую сторону.

#### 5.2 Обработка строк

`char*`, возвращённый функцией C, немедленно копируется при преобразовании в `String` YaoXiang. Владение исходным указателем определяется соглашением C-функции (объявляется через аннотацию), автоматическое освобождение не выполняется.

### 6. Вопросы безопасности

#### 6.1 Безопасность 并作

Вызовы FFI-функций **по умолчанию не участвуют в планировании DAG**, рассматриваются как блокирующие операции. C-функции, подтверждённые как reentrant, могут быть помечены как `@concurrent`:

```yaoxiang
# Чистая функция, без глобального состояния, может быть并发
sin_safe: (x: Float) -> Float = native("sin")
    # reentrant: true

# Имеет глобальное состояние, не может быть并发
strtok: (s: *const u8, delim: *const u8) -> *const u8 = native("strtok")
    # reentrant: false
```

`yx-bindgen` по возможности аннотирует информацию о reentrancy для стандартных функций C (варианты `_r` для `strtok` и т. д.).

**Требования к асинхронным вызывающим сторонам:** перед вызовом FFI-функции вызывающая сторона должна убедиться, что целевая функция является reentrant. Среда выполнения не выполняет автоматическое обнаружение — это статически неразрешимая задача.

#### 6.2 Изоляция ошибок

- Ошибки FFI-вызовов распространяются через тип `Result` (если функция объявила тип возврата Result)
- Механизм тайм-аута предотвращает взаимоблокировку внешних функций

```yaoxiang
# Вызов с тайм-аутом (реализуется на уровне FfiRegistry)
result = ffi.call_with_timeout("blocking_func", 5000)  # Тайм-аут 5 секунд
```

#### 6.3 Безопасность указателей

- Параметры-указатели требуют маркера `unsafe` со стороны YaoXiang
- Время жизни указателей через границу FFI гарантируется вызывающей стороной

### 7. Изменения в компиляторе

**Нулевые синтаксические изменения** — требуется только объявление `native("symbol")`, уже реализованное в текущем компиляторе.

В интерпретаторе/среде выполнения добавляется:

- Инструкции динамической загрузки библиотек (привязки FFI для `DynamicLibrary`)
- Механизм тайм-аута

### 8. Функции, не прошедшие проверку

Следующие функции после аудита явно исключены и больше не рассматриваются в RFC:

- **`ffi.try_call`**: Избыточно, уже есть `native` + тип возврата `Result`
- **`ffi.verify_signature`**: Среда выполнения делает работу компилятора — неправильный уровень абстракции
- **`ffi.async_call`**: Требует уточнения модели контракта reentrancy перед рассмотрением
- **Таблица привязок, поддерживаемая сообществом**: Неосуществима, заменена решением на основе инструментария `yx-bindgen`

## Компромиссы

### Преимущества

- ✅ **Нулевые синтаксические изменения** — точка входа в FFI только `native("symbol")`, полная обратная совместимость
- ✅ **Библиотека как язык** — функциональность постепенно вводится через стандартную библиотеку
- ✅ **Инструментарий** — `yx-bindgen` автоматически обрабатывает генерацию привязок
- ✅ **Безопасность памяти** — чёткая модель владения, отсутствие use-after-free из-за автоматической сборки мусора
- ✅ **Отлаживаемость** — ошибки содержат нативные коды ошибок ОС

### Недостатки

- ⚠️ Безопасность типов ограничена выразительностью C-заголовков (`void*` невозможно статически различить)
- ⚠️ `yx-bindgen` требует постоянной поддержки для отслеживания эволюции стандарта C
- ⚠️ Привязки для языков, отличных от C (Python/JS/Java), требуют индивидуальной обработки проектами, унифицированного решения нет

## Стратегия реализации

### Этап 1: Основная библиотека (v0.7)

- [ ] Расширение модуля `std.ffi`
- [ ] Реализация структуры `DynamicLibrary`
- [ ] Поддержка Linux/macOS (`dlopen`/`dlsym`)
- [ ] Поддержка Windows (`LoadLibrary`/`GetProcAddress`)
- [ ] Добавление механизма тайм-аута в среду выполнения
- [ ] Модульные тесты

### Этап 2: yx-bindgen (v0.8)

- [ ] Реализация парсера заголовочных файлов C (на основе существующих привязок Clang или самописного парсера)
- [ ] Система映射 типов
- [ ] Генерация объявлений `native("symbol")`
- [ ] Генерация layout структур
- [ ] Интеграционные тесты: генерация привязок для реальных C-библиотек SQLite3, libcurl и др.

### Этап 3: Основа экосистемы (v0.9)

- [ ] Публикация официального пакета привязок `libc` (подмножество POSIX + Windows API)
- [ ] Разработка спецификации публикации пакетов привязок
- [ ] Документация: лучшие практики FFI, владение памятью, контракты безопасности 并作

## Связь с другими RFC

- **RFC-001**: FFI-вызовы как внешние функции, по умолчанию `@block` (не участвуют в планировании DAG)
- **RFC-008**: Проектирование разделения планировщика, FFI-вызовы как независимые задачи
- **RFC-020**: Динамические модули и интеграция FFI

## Открытые вопросы

- [ ] Должен ли `yx-bindgen` быть интегрирован в систему сборки (`yaoxiang build`)?
- [ ] Как спроектировать поддержку FFI для платформы WASM? (Механизм импорта WASM полностью отличается от dlopen)
- [ ] Нужно ли предоставить `cxx-bindgen` для обработки name mangling C++ (опционально, рассмотреть после v1.0)?

---

## Приложение A: Журнал решений по проектированию

| Решение | Решение | Причина | Дата | Записал |
|------|------|------|------|--------|
| Унификация точки входа FFI | Только `native("symbol")` | Избежать расщепления API | 2026-05-29 | Чэнь Сюй |
| Исключение `try_call` | Не реализовывать | Избыточно, уже есть тип Result | 2026-05-29 | Чэнь Сюй |
| Исключение `verify_signature` | Не реализовывать | Среда выполнения делает работу компилятора | 2026-05-29 | Чэнь Сюй |
| Привязки сообщества → Инструментарий | Автогенерация `yx-bindgen` | Неосуществимая фантазия | 2026-05-29 | Чэнь Сюй |
| Коды ошибок ОС | `FfiError` обязательно содержит `os_error` | Неотлаживаемый API бесполезен | 2026-05-29 | Чэнь Сюй |
| Нулевые синтаксические изменения | Реализация через библиотеку | Принцип простого ядра | 2026-03-14 | Чэнь Сюй |
| Динамическая загрузка библиотек | Использование dlopen/dlsym | Стандартный интерфейс ОС | 2026-03-14 | Чэнь Сюй |
| Обработка ошибок | Использование типа Result | Согласованность | 2026-03-14 | Чэнь Сюй |

## Приложение B: Примеры кода

### Полный пример: использование C-библиотеки

```yaoxiang
# Загрузка C математической библиотеки
libm = ffi.load_library("libm.so")

# Регистрация C-символов в таблице среды выполнения
# (yx-bindgen сделает это во время компиляции)
ffi.register_library_symbols(libm, ["sin", "cos", "sqrt"])

# Использование через объявление native
sin_f: (x: Float) -> Float = native("sin")
cos_f: (x: Float) -> Float = native("cos")

# Прямой вызов
result = sin_f(3.14159 / 2)

# Для C-функций, которые могут завершиться неудачей, используется Result
file_open: (path: *const u8, mode: *const u8) -> Result(*mut opaque, Int)
    = native("fopen")
```

### Использование yx-bindgen

```bash
# Автоматическая генерация всех объявлений, ручная запись не требуется
yx-bindgen --header /usr/include/math.h --output math_bindings.yx

# Подключение в YaoXiang
import "math_bindings.yx"
# sin_f / cos_f и другие автоматически объявлены как native("sin") / native("cos")
```

---

## Список литературы

- [RFC-001: Модель 并作 и система обработки ошибок](./001-concurrent-model-error-handling.md)
- [RFC-008: Модель 并作 Runtime и проектирование разделения планировщика](./008-runtime-concurrency-model.md)
- [План реализации FFI](../reference/plan/completed/FFI.md)
- [Документация Python ctypes](https://docs.python.org/3/library/ctypes.html)
- [Документация Rust libloading crate](https://docs.rs/libloading/latest/libloading/)