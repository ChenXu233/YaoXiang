```md
---
title: "RFC-019：Типизированная гомоиконность (Typed Homoiconicity)"
---

# RFC-019: Типизированная гомоиконность (Typed Homoiconicity) — синтаксис как тип

> **Статус**: Черновик
>
> **Автор**: Чэнь Сюй
>
> **Дата создания**: 2026-02-20
>
> **⚠️ Постоянное экспериментальное уведомление**: Это **исследовательский эксперимент**, направленный на проверку осуществимости языковой конструкции «синтаксис как тип». **Данный RFC никогда не будет влит**, независимо от результата он не попадёт в ветку dev/main. Экспериментальная ветка будет удалена или архивирована после завершения.
>
> - **Цель эксперимента**: проверить сложность реализации и потенциальную ценность типизированной гомоиконности
> - **Критическая точка**: отказ при отсутствии прогресса в течение 6 месяцев
> - **Критерий успеха**: возможность запустить хотя бы одно пользовательское ключевое слово (полный цикл: разбор → компиляция → выполнение)
>
> **Нет гарантии влития в main-ветку**, проект может быть отклонён или заброшен в будущем по различным причинам. Не используйте эту функциональность в производственной среде.
>
> **⚠️ Пояснение к позиционированию**: Данный RFC является мысленным экспериментом в области дизайна языка и не предоставляет инженерного решения. Для практических расширяемых паттернов парсинга см. Rust `syn::Parse` или Haskell `parsec`.

---

## Аннотация

В данном RFC предлагается радикальный эксперимент в дизайне языка: **сделать синтаксическую структуру языка частью системы типов**.

Ключевая идея заимствована из Lisp — «код как данные» (гомоиконность), но реализуется через **статическую систему типов**:
- Синтаксическое дерево (AST) является типом
- Ключевые слова — это предопределённые экземпляры типов
- Пользователи могут расширять синтаксис языка через определение типов

Это означает: язык сам становится набором composable, расширяемых «строительных блоков».

---

## Мотивация

### Зачем нужен этот эксперимент?

1. **Стремление к унификации**: устранение «ключевых слов» как особого синтаксического элемента — всё становится типами и функциями
2. **Расширяемость языка**: пользователи могут определять новые синтаксические конструкции так же, как определяют функции
3. **Типобезопасные макросы**: традиционные макросы (текстовые подстановки) опасны; типизированная гомоиконность обеспечивает проверку на этапе компиляции
4. **Учебные цели**: глубокое понимание сущности дизайна языков

### Связь с Lisp

Lisp давно реализует «код как данные»:
```lisp
; Код на Lisp — это S-expression
(if (> x 0) "positive" "negative")
```

Отличие данного эксперимента: **статическая система типов усиливает эту идею**.

---

## Предложение

### Основные концепции

#### 1. Синтаксическое дерево как тип (AST as Type)

```yaoxiang
// Узлы синтаксического дерева — это типы
If: Type = { condition: Expr, then: Block, else: Block }
While: Type = { condition: Expr, body: Block }
Return: Type = { value: Expr }
Block: Type = { statements: Array[Expr] }
Let: Type = { name: String, value: Expr, body: Expr }
Function: Type = { params: Array[Param], body: Expr }
Call: Type = { func: Expr, args: Array[Expr] }

// Базовые типы
Literal: Type = { value: Int }
StringLiteral: Type = { value: String }
Variable: Type = { name: String }
```

#### 2. Ключевые слова = функции, обрабатывающие типы

```yaoxiang
// Вычислитель — это функция, обрабатывающая эти типы
eval_if: (node: If, env: Env) -> Value = ...
eval_while: (node: While, env: Env) -> Value = ...
eval_return: (node: Return, env: Env) -> Value = ...
eval_block: (node: Block, env: Env) -> Value = ...

// Компилятор тоже может быть функцией
compile_if: (node: If, ctx: CompileContext) -> IR = ...
compile_while: (node: While, ctx: CompileContext) -> IR = ...
```

#### 3. Типы несут правила разбора (ключевая инновация)

Это ключевой момент эксперимента: **тип не только описывает данные, но и несёт правила разбора кода**.

```yaoxiang
// Тип правил синтаксиса
SyntaxRule: Type = {
    // Как разобрать код этого типа
    parse: (token_stream: TokenStream) -> (Self, remaining_tokens)

    // Как скомпилировать/вычислить экземпляр типа
    compile: (node: Self, ctx: CompileContext) -> IR
    eval: (node: Self, env: Env) -> Value
}

// Правила синтаксиса для типа IF
IF: SyntaxRule = {
    // Разбор "if (cond) { then } else { else }"
    parse: (tokens: TokenStream) -> (If, remaining) = {
        consume("if")
        cond = parse_expression(tokens)
        consume("{")
        then_block = parse_block(tokens)
        consume("}")
        consume("else")
        consume("{")
        else_block = parse_block(tokens)
        consume("}")
        return If(cond, then_block, else_block), tokens
    }

    eval: (node: If, env: Env) -> Value = {
        if eval(node.condition, env) != 0 {
            return eval(node.then, env)
        } else {
            return eval(node.else, env)
        }
    }
}
```

#### 4. Пользовательские синтаксические расширения

Пользователи могут определять свои «ключевые слова»:

```yaoxiang
// Пользователь определяет новую синтаксическую конструкцию: unless
Unless: SyntaxRule = {
    parse: (tokens: TokenStream) -> (If, remaining) = {
        consume("unless")
        cond = parse_expression(tokens)
        consume("{")
        body = parse_block(tokens)
        consume("}")
        // unless эквивалентен if (!cond) { body }
        return If(Not(cond), body, Block([])), tokens
    }
}

// Использование
unless x > 0 {
    print("x is not positive")
}

// Раскрывается в
if !(x > 0) {
    print("x is not positive")
}
```

### Примеры

#### Полный пример: пользовательский синтаксис цикла

```yaoxiang
// Определяем цикл "times": n.times { ... } выполняется n раз
TimesLoop: SyntaxRule = {
    parse: (tokens: TokenStream) -> (While, remaining) = {
        receiver = parse_expression(tokens)  // Получаем число
        consume(".times")
        consume("{")
        body = parse_block(tokens)
        consume("}")
        // Преобразуем в цикл while
        counter_var = gensym("i")
        return While(
            Less(Variable(counter_var), receiver),
            Block([
                body,
                Assign(counter_var, Add(Variable(counter_var), Literal(1)))
            ])
        ), tokens
    }
}

// Использование
5.times {
    print("Hello!")
}

// Раскрывается в
i = 0
while i < 5 {
    print("Hello!")
    i = i + 1
}
```

#### Пример: синтаксис сопоставления с образцом

```yaoxiang
// Пользовательское сопоставление с образцом
Match: SyntaxRule = {
    parse: (tokens: TokenStream) -> (MatchNode, remaining) = {
        subject = parse_expression(tokens)
        consume("{")
        cases = []
        while !check("}") {
            pattern = parse_pattern(tokens)
            consume("=>")
            body = parse_expression(tokens)
            cases.push((pattern, body))
        }
        consume("}")
        return MatchNode(subject, cases), tokens
    }
}

// Использование
match x {
    0 => "zero",
    1 => "one",
    n if n > 10 => "big",
    _ => "other"
}
```

---

## Детальное проектирование

### Архитектура системы

```
┌─────────────────────────────────────────────────────┐
│                    Исходный код                     │
└─────────────────┬───────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────────┐
│              Синтаксический анализатор              │
│  - Распознавание ключевых слов                      │
│  - Поиск соответствующего типа SyntaxRule           │
│  - Вызов метода parse типа                         │
└─────────────────┬───────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────────┐
│              AST (экземпляры типов)                 │
│  If, While, Match, TimesLoop...                     │
└─────────────────┬───────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────────┐
│              Компилятор / Интерпретатор             │
│  - Вызов методов compile/eval типа                 │
│  - Генерация целевого кода или выполнение          │
└─────────────────────────────────────────────────────┘
```

### Ключевые технические проблемы

#### 1. Функционализация потока управления

Проблема: `if` должен вычислять только одну ветку, нельзя использовать обычный вызов функции.

Решение: передача thunk (отложенное вычисление)

```yaoxiang
// Внутреннее представление после компиляции
If: Type = {
    condition: Expr,
    then: () -> Value,  // thunk, отложенное вычисление
    else: () -> Value
}
```

#### 2. Нелокальный возврат return

Проблема: `return` должен выходить из нескольких уровней функций.

Решения:
- Вариант A: CPS-преобразование на этапе компиляции
- Вариант B: использование монады Result/Either
- Вариант C: ограничение области видимости return

#### 3. Синтаксическая неоднозначность

Проблема: как отличить `if(x > 0) { 1 }` как вызов функции от ключевого слова?

Решение:
- Ключевые слова используют специальный синтаксис (например, `if ... { } else { }`)
- Или ограничения через систему типов

#### 4. Бесконечная рекурсия

Проблема: пользователь может определить самоссылающиеся синтаксические правила.

Решение: обнаружение циклических зависимостей на этапе компиляции

---

## Связь с существующими системами

### Связь с RFC-010 (Унифицированный синтаксис типов)

RFC-010 реализует унифицированный синтаксис `name: type = value`, данный RFC является его развитием:

| RFC-010 | Данный RFC |
|----------|-----------|
| Переменные, функции, типы — всё `name: type = value` | Ключевые слова тоже `name: type = value` |
| Типы являются значениями | Синтаксические правила тоже значения |
| `Type` — метатип | `SyntaxRule` — метатип для синтаксиса |

### Сравнение с Lisp/макросами

| Характеристика | Макросы Lisp | Данный эксперимент |
|------|---------|--------|
| Представление кода | S-expression (списки) | Экземпляры типов |
| Способ расширения | defmacro | Определение типа SyntaxRule |
| Типобезопасность | Слабая (текстовая подстановка) | Сильная (проверка типов) |
| Время разбора | Runtime/compile-time | Этап компиляции |
| Поддержка IDE | Слабая | Сильная (информация о типах) |

---

## План ветвления

### Экспериментальная ветка

```
Имя ветки: exp/typed-homoiconicity
Создаётся от ветки dev
```

**Важно**:
- Это **экспериментальная ветка**, не будет часто сливаться с dev
- Может длительно разрабатываться независимо
- **Нет гарантии влития в main**
- В случае неудачи эксперимента ветка будет удалена

### Этапы разработки

> **⚠️ Временной лимит эксперимента: 6 месяцев**

| Этап | Цель | Ожидаемое время | Примечания |
|------|------|----------|------------|
| Phase 1 | Проверка концепции: реализация типов AST с использованием существующего синтаксиса | 2 недели | |
| Phase 2 | Реализация базового вычислителя | 2 недели | Ключевая сложность: управление потоком if/return |
| Phase 3 | Реализация правил разбора для типа SyntaxRule | 3 недели | |
| Phase 4 | Расширение пользовательского синтаксиса | 3 недели | Основная цель: запустить хотя бы одно пользовательское ключевое слово |
| Phase 5 | Оптимизация и документация | 2 недели | Завершение эксперимента |

**Обработка таймаута**: если Phase 2 (реализация управления потоком) не показывает прогресса в течение 4 недель, следует рассмотреть отказ от эксперимента.

---

## Компромиссы

### Преимущества

- **Предельная унификация**: устранение границы между ключевыми словами и обычным кодом
- **Расширяемость языка**: пользователи могут определять собственный синтаксис
- **Типобезопасность**: безопаснее традиционных макросов
- **Образовательная ценность**: глубокое понимание сущности языков

### Недостатки

- **Сложность реализации**: требуется значительная модификация компилятора
- **Опасения по производительности**: интерпретация на runtime может быть медленной
- **Крутая кривая обучения**: абстрактные концепции, требуется понимание системы типов
- **Сомнительная практичность**: возможен overengineering

### Риски

- Эксперимент может провалиться, не найдя практического применения
- Сложность реализации может превысить ожидания
- Возможны конфликты с существующими возможностями

---

## Открытые вопросы

- [ ] Как обрабатывать синтаксические конфликты (конфликты пользовательских правил со встроенными)?
- [ ] Какие есть варианты оптимизации производительности?
- [ ] Нужен ли механизм импорта/экспорта синтаксиса?
- [ ] Как интегрировать с существующей модульной системой?

---

## Приложение

### Глоссарий терминов

| Термин | Определение |
|------|------|
| Гомоиконность (Homoiconicity) | Код и данные используют одинаковое представление |
| Синтаксическое дерево (AST) | Абстрактное представление синтаксического дерева программы |
| SyntaxRule | Тип, несущий правила синтаксического разбора |
| Thunk | Обёртка функции с отложенным вычислением |
| CPS | Continuation Passing Style, стиль передачи продолжений |

### Ссылки

- [Lisp Wiki: Homoiconicity](https://en.wikipedia.org/wiki/Homoiconicity)
- [Julia Метапрограммирование](https://docs.julialang.org/en/v1/manual/metaprogramming/)
- [Rust Процедурные макросы](https://doc.rust-lang.org/book/ch19-06-macros.html)

---

## Жизненный цикл и судьба

```
┌─────────────┐
│   Черновик  │  ← Текущий статус
└──────┬──────┘
       │
       ▼
       ⚠️ Постоянная экспериментальная ветка (exp/typed-homoiconicity)

       Возможные результаты:
       ├─► Успешная проверка → Архивирование, никогда не вливается
       ├─► Неудача → Удаление ветки
       └─► Таймаут → Отказ и удаление

       ⚠️ Независимо от результата, данный RFC никогда не вливается
```

> **⚠️ Важное напоминание**: Это исследовательский эксперимент, **никогда не будет влит**. Пожалуйста, не полагайтесь на эту функциональность в производственном коде.
```