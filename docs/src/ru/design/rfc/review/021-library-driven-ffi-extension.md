```yaml
---
title: "RFC-021：库驱动 FFI расширение и поддержка кросс-языковых вызовов"
---

# RFC-021: Библиотечно-управляемое FFI расширение и поддержка кросс-языковых вызовов

> **Статус**: Черновик
> **Автор**: Чэнь Сюй (晨煦)
> **Дата создания**: 2026-03-14
> **Последнее обновление**: 2026-05-29

> **См. также**:
> - [RFC-001: 并作 модель и система обработки ошибок](./001-concurrent-model-error-handling.md)
> - [RFC-008: Runtime модель 并作 и развязка планировщика](./008-runtime-concurrency-model.md)
> - [План реализации FFI](../reference/plan/completed/FFI.md)

## Аннотация

В данном документе предлагается **библиотечно-управляемая** схема расширения FFI (Foreign Function Interface — внешний интерфейс функций). Единственной точкой входа в FFI является объявление `native("symbol")` + среда выполнения `FfiRegistry`, при этом в ядре не вводится второй набор механизмов. Поверх этого через стандартную библиотеку предоставляются возможности динамической загрузки библиотек, кросс-языковых вызовов и привязок. Привязки вызовов для конкретных языков (таких как C, Python, JavaScript) автоматически генерируются официальным инструментарием или разрабатываются проектами по необходимости.

## Мотивация

### Недостатки текущей реализации

Текущая реализация FFI уже обладает следующими возможностями:
- Синтаксис `native("symbol")` для объявления внешних функций
- Реестр функций `FfiRegistry`

Однако функциональность относительно ограничена:
- Отсутствует поддержка динамической загрузки библиотек
- Нет инфраструктуры для кросс-языковых вызовов
- Отсутствуют инструменты автоматической генерации привязок

### Философия проектирования

YaoXiang придерживается принципа **"Ядро простое, сложность опускается в библиотеки"**:

> **Хороший вкус (Good Taste)**: Задача языка — предоставлять атомарные возможности, а не полный набор функций. Сложность должна решаться через библиотеки, а не накапливаться в компиляторе.

Поэтому данная схема:
- ✅ **Нулевые изменения синтаксиса** — полная обратная совместимость, единственная точка входа в FFI — это `native("symbol")`
- ✅ **Библиотека как язык** — функциональность расширяется через стандартную библиотеку
- ✅ **Автоматизация инструментарием** — привязки генерируются `yx-bindgen`, а не поддерживаются вручную
- ✅ **Постепенное улучшение** — разработчики подключают функциональность по необходимости

## Предложение

### 1. Расширение базовой FFI библиотеки

Расширяется модуль `std.ffi`. Внимание: все вызовы внешних функций по-прежнему осуществляются через объявление `native("symbol")`, `std.ffi` предоставляет только вспомогательные возможности.

#### 1.1 Динамическая загрузка библиотек

```yaoxiang
import ffi

# Загрузка динамической библиотеки (.so/.dll/.dylib)
lib = ffi.load_library("./libmyext.so")

# Получение символов функций из библиотеки, возврат имени symbol, доступного для native
ffi.register_library_symbols(lib, [
    "my_function",
    "another_func",
])
```

`load_library` возвращает дескриптор `DynamicLibrary`, а `register_library_symbols` регистрирует имена символов в известной таблице `FfiRegistry`. После этого пользователь по-прежнему использует объявление `native`:

```yaoxiang
my_func: (a: Int, b: Int) -> Int = native("my_function")
```

Никакого второго набора синтаксиса вызовов, никаких обёрток `try_call`.

#### 1.2 Управление библиотеками

```yaoxiang
# Список загруженных библиотек
loaded = ffi.loaded_libraries()

# Выгрузка библиотеки
ffi.unload_library(lib)

# Проверка версии библиотеки
ffi.check_version(lib, "1.0.0")
```

#### 1.3 Разрешение символов

```yaoxiang
# Поиск символа по имени (возвращает структуру Symbol)
sin_sym = ffi.dlsym("libm.so", "sin")
```

Соглашения о кросс-языковых вызовах и преобразование типов не обрабатываются во время выполнения через универсальные обёртки, а генерируются `yx-bindgen` на этапе компиляции.

### 2. Реализация динамической загрузки библиотек

#### 2.1 Основные структуры данных

```rust
pub struct DynamicLibrary {
    handle: *mut std::ffi::c_void,
    path: String,
}

impl DynamicLibrary {
    pub fn load(path: &str) -> Result<Self, FfiError>;
    pub fn get_symbol(&self, name: &str) -> Result<*mut std::ffi::c_void, FfiError>;
    pub fn unload(self) -> Result<(), FfiError>;
}
```

#### 2.2 Типы ошибок

```rust
pub enum FfiError {
    LibraryNotFound { name: String, os_error: Option<OsError> },
    SymbolNotFound { name: String, os_error: Option<OsError> },
    CallFailed { message: String, os_error: Option<OsError> },
    Timeout,
}

pub struct OsError {
    pub code: i32,
    pub message: String,
}
```

`OsError` содержит нативный код ошибки платформы (`dlerror()` в Linux, `GetLastError()` в Windows), что обеспечивает отлаживаемость.

### 3. Привязки для нескольких языков: решение на основе инструментария

Отказываемся от «фантазии» о том, что представители сообщества для каждого языка будут поддерживать библиотеки привязок. Вместо этого — автоматическая генерация официальным инструментарием.

#### 3.1 Архитектурный дизайн

```
┌───────────────────────────────────────────────┐
│  YaoXiang код                                │
│                                               │
│  // Пользователь пишет только объявление native │
│  my_func: (a: Int) -> Int = native("my_func") │
└───────────────────────────────────────────────┘
         ↑                          ↑
         |  Время компиляции         |  Время выполнения
┌──────────────────┐   ┌────────────────────────┐
│  yx-bindgen       │   │  std.ffi + FfiRegistry  │
│  (C заголовочные  │   │  - dlopen/dlsym         │
│   файлы → .yx)    │   │  - LoadLibrary/GetProc  │
└──────────────────┘   └────────────────────────┘
```

#### 3.2 Генератор привязок (`yx-bindgen`)

`yx-bindgen` — это автономный CLI-инструмент, который генерирует код привязок YaoXiang FFI из C заголовочных файлов:

```bash
yx-bindgen --header /usr/include/sqlite3.h --output sqlite3.yx
```

Пример сгенерированного результата:

```yaoxiang
# Автогенерировано, не редактировать вручную
# Источник: /usr/include/sqlite3.h

sqlite3_open: (filename: *const u8, ppDb: *mut *mut opaque) -> Int
    = native("sqlite3_open")

sqlite3_close: (db: *mut opaque) -> Int
    = native("sqlite3_close")

sqlite3_exec: (
    db: *mut opaque,
    sql: *const u8,
    callback: *mut opaque,
    arg: *mut opaque,
    errmsg: *mut *mut u8,
) -> Int
    = native("sqlite3_exec")
```

`yx-bindgen` поддерживается официально, что гарантирует:
- Полноту маппинга типов (`int` → `Int`, `char*` → `*const u8`, `void*` → `*mut opaque`)
- Выравнивание layout структур (автоматический эквивалент `#[repr(C)]`)
- Преобразование сигнатур callback-функций

#### 3.3 Официально поддерживаемые пакеты привязок

Основная команда YaoXiang не обязуется поддерживать универсальные привязки для всех языков, но предоставляет официальный пакет привязок `libc` (подмножество POSIX + Windows API) в качестве примера лучших практик FFI и базовых возможностей.

Привязки для других языков и библиотек:
- Генерируются самостоятельно с помощью `yx-bindgen`
- Могут публиковаться как пакеты YaoXiang (например, `libsqlite3`, `libcurl`, `libsdl2`)
- Основная команда не несёт ответственности за поддержку, но предоставляет механизм публикации и управления версиями пакетов

### 4. Уровень преобразования типов

#### 4.1 Компиляционный маппинг типов

Преобразование типов выполняется не через обёртки времени выполнения, а статически определяется при генерации `yx-bindgen`:

| Тип C   | Тип YaoXiang   | Способ преобразования       |
|----------|-----------------|------------------------------|
| `int`    | `Int`           | Прямая передача значения     |
| `char*`  | `*const u8`     | Передача указателя           |
| `void*`  | `*mut opaque`   | Непрозрачный указатель       |
| `struct T` | `extern struct T` | Соответствие layout памяти |
| `int*`   | `*mut Int`      | Передача указателя (изменяемый) |
| `const int*` | `*const Int` | Передача указателя (только чтение) |

#### 4.2 Ручное преобразование (вспомогательные средства стандартной библиотеки)

```yaoxiang
# Явное преобразование
raw_ptr = ffi.to_pointer(my_bytes)
c_string = ffi.to_c_string(my_string)
```

### 5. Модель владения памятью

#### 5.1 Базовые принципы

При каждом выделении памяти через границу FFI должны быть даны однозначные ответы на два вопроса:
1. **Кто выделил?** (С-сторона `malloc` / сторона runtime YaoXiang)
2. **Кто освободит?** (С-сторона `free` / сторона runtime YaoXiang)

При генерации `yx-bindgen` добавляет аннотации для типичных паттернов:

```yaoxiang
# Выделено C, вызывающая сторона должна освободить
sqlite3_exec: (...) -> Int
    = native("sqlite3_exec")
    # memory: C-allocated, caller must free errmsg via sqlite3_free

# Вызывающая сторона выделяет указатель
read: (fd: Int, buf: *mut u8, count: Int) -> Int
    = native("read")
    # memory: caller-allocated buf
```

Среда выполнения не выполняет автоматическое управление памятью для ссылок на указатели, пересекающих границу FFI — владение чётко возлагается на вызывающую сторону.

#### 5.2 Обработка строк

`char*`, возвращённый из C-функции, немедленно копируется при преобразовании в YaoXiang `String`. Владение исходным указателем определяется C-функцией (объявляется через аннотацию), автоматическое освобождение не выполняется.

### 6. Вопросы безопасности

#### 6.1 Безопасность в условиях 并作

Вызовы FFI-функций **по умолчанию не участвуют в планировании DAG**, рассматриваются как блокирующие операции. C-функции, подтверждённые как reentrant, могут быть помечены как `@concurrent`:

```yaoxiang
# Чистая функция, без глобального состояния, может быть 并作
sin_safe: (x: Float) -> Float = native("sin")
    # reentrant: true

# Есть глобальное состояние, не может быть 并作
strtok: (s: *const u8, delim: *const u8) -> *const u8 = native("strtok")
    # reentrant: false
```

`yx-bindgen` по возможности аннотирует reentrancy-информацию для стандартных C-библиотек (`_r` варианты `strtok` и т.д.).

**Требования к асинхронным вызывающим сторонам:** перед вызовом FFI-функции вызывающая сторона должна убедиться, что целевая функция является reentrant. Среда выполнения не выполняет автоматическую проверку — это невозможно решить статически.

#### 6.2 Изоляция ошибок

- Ошибки FFI-вызовов распространяются через тип `Result` (если функция объявила возвращаемый тип Result)
- Механизм тайм-аута предотвращает deadlock внешних функций

```yaoxiang
# Вызов с тайм-аутом (реализуется на уровне FfiRegistry)
result = ffi.call_with_timeout("blocking_func", 5000)  # Тайм-аут 5 секунд
```

#### 6.3 Безопасность указателей

- Параметры-указатели требуют пометки `unsafe` на стороне YaoXiang
- Время жизни указателей, пересекающих границу FFI, гарантируется вызывающей стороной

### 7. Изменения в компиляторе

**Нулевые изменения синтаксиса** — требуется только объявление `native("symbol")`, которое уже реализовано в текущем компиляторе.

В интерпретаторе/среде выполнения добавляются:
- Инструкции динамической загрузки библиотек (FFI-привязки для `DynamicLibrary`)
- Механизм тайм-аута

### 8. Функции, отклонённые к принятию

Следующие функции после аудита явно исключены и более не рассматриваются в RFC:

- **`ffi.try_call`**: Избыточно, уже есть `native` + возвращаемый тип `Result`
- **`ffi.verify_signature`**: Среда выполнения делает работу компилятора — неправильный уровень абстракции
- **`ffi.async_call`**: Требует уточнения модели контракта reentrancy перед рассмотрением
- **Таблица привязок, поддерживаемая сообществом**: Неосуществимо, переходим к схеме инструментария `yx-bindgen`

## Компромиссы

### Преимущества

- ✅ **Нулевые изменения синтаксиса** — точка входа в FFI только `native("symbol")`, полная обратная совместимость
- ✅ **Библиотека как язык** — функциональность постепенно вводится через стандартную библиотеку
- ✅ **Инструментарий как драйвер** — `yx-bindgen` автоматически обрабатывает генерацию привязок
- ✅ **Безопасность памяти** — модель владения однозначна, нет автоматической утилизации, ведущей к use-after-free
- ✅ **Отлаживаемость** — ошибки несут нативный код ошибки ОС

### Недостатки

- ⚠️ Безопасность типов ограничена выразительностью C заголовочных файлов (`void*` невозможно статически различить)
- ⚠️ `yx-bindgen` требует постоянной поддержки для отслеживания эволюции C стандарта
- ⚠️ Привязки для не-C языков (Python/JS/Java) требуют индивидуальной обработки проектами, нет единого решения

## Стратегия реализации

### Этап 1: Базовая библиотека (v0.7)

- [ ] Расширение модуля `std.ffi`
- [ ] Реализация структуры `DynamicLibrary`
- [ ] Поддержка Linux/macOS (`dlopen`/`dlsym`)
- [ ] Поддержка Windows (`LoadLibrary`/`GetProcAddress`)
- [ ] Добавление механизма тайм-аута в среду выполнения
- [ ] Модульное тестирование

### Этап 2: yx-bindgen (v0.8)

- [ ] Реализация парсера C заголовочных файлов (на основе существующих привязок к Clang или собственного парсера)
- [ ] Система маппинга типов
- [ ] Генерация объявлений `native("symbol")`
- [ ] Генерация layout структур
- [ ] Интеграционное тестирование: генерация привязок для реальных C-библиотек SQLite3, libcurl и др.

### Этап 3: Экосистемные основы (v0.9)

- [ ] Публикация официального пакета привязок `libc` (подмножество POSIX + Windows API)
- [ ] Разработка спецификации публикации пакетов привязок
- [ ] Документация: лучшие практики FFI, владение памятью, контракты безопасности 并作

## Связь с другими RFC

- **RFC-001**: FFI-вызовы как внешние функции, по умолчанию `@block` (не участвуют в планировании DAG)
- **RFC-008**: Развязка дизайна планировщика, FFI-вызовы как независимые задачи
- **RFC-020**: Семантика планирования FFI-узлов в DAG, Phi-узлы, разворот циклов и другие аспекты планирования

## Открытые вопросы

- [ ] Должен ли `yx-bindgen` быть интегрирован в систему сборки (`yaoxiang build`)?
- [ ] Как спроектировать поддержку FFI для платформы WASM? (Механизм импорта WASM кардинально отличается от dlopen)
- [ ] Нужна ли поддержка `cxx-bindgen` для C++ name mangling (опционально, после v1.0)?

---

## Приложение A: Журнал архитектурных решений

| Решение | Решение | Причина | Дата | Записал |
|---------|---------|---------|------|---------|
| Унификация точки входа FFI | Только `native("symbol")` | Избежать расщепления API | 2026-05-29 | Чэнь Сюй |
| Исключение `try_call` | Не реализуется | Избыточно, уже есть тип Result | 2026-05-29 | Чэнь Сюй |
| Исключение `verify_signature` | Не реализуется | Среда выполнения делает работу компилятора | 2026-05-29 | Чэнь Сюй |
| Привязки сообщества → Инструментарий | Автогенерация `yx-bindgen` | Неосуществимая фантазия | 2026-05-29 | Чэнь Сюй |
| Коды ошибок ОС | `FfiError` обязательно содержит `os_error` | Неотлаживаемый API бесполезен | 2026-05-29 | Чэнь Сюй |
| Нулевые изменения синтаксиса | Реализация через библиотеки | Принцип простоты ядра | 2026-03-14 | Чэнь Сюй |
| Динамическая загрузка библиотек | Использование dlopen/dlsym | Стандартный интерфейс ОС | 2026-03-14 | Чэнь Сюй |
| Обработка ошибок | Использование типа Result | Согласованность | 2026-03-14 | Чэнь Сюй |

## Приложение B: Примеры кода

### Полный пример: использование C-библиотеки

```yaoxiang
# Загрузка C математической библиотеки
libm = ffi.load_library("libm.so")

# Регистрация C символов в таблице времени выполнения
# (yx-bindgen сделает это во время компиляции)
ffi.register_library_symbols(libm, ["sin", "cos", "sqrt"])

# Объявление через native
sin_f: (x: Float) -> Float = native("sin")
cos_f: (x: Float) -> Float = native("cos")

# Прямой вызов
result = sin_f(3.14159 / 2)

# Для C функций, которые могут завершиться неудачей, используется Result
file_open: (path: *const u8, mode: *const u8) -> Result(*mut opaque, Int)
    = native("fopen")
```

### Использование yx-bindgen

```bash
# Автогенерация всех объявлений, ручное написание не требуется
yx-bindgen --header /usr/include/math.h --output math_bindings.yx

# Подключение в YaoXiang
import "math_bindings.yx"
# sin_f / cos_f и др. автоматически объявлены как native("sin") / native("cos")
```

---

## Список литературы

- [RFC-001: 并作 модель и система обработки ошибок](./001-concurrent-model-error-handling.md)
- [RFC-008: Runtime 并作 модель и развязка планировщика](./008-runtime-concurrency-model.md)
- [План реализации FFI](../reference/plan/completed/FFI.md)
- [Документация Python ctypes](https://docs.python.org/3/library/ctypes.html)
- [Rust libloading crate](https://docs.rs/libloading/latest/libloading/)