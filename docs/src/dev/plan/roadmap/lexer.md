---
title: "词法分析器状态"
---

# 词法分析器（Lexer）

> **模块状态**：稳定（3 项待改进）
> **位置**：`src/frontend/core/lexer/`
> **最后更新**：2026-06-01

---

## 模块概述

词法分析器负责将源代码字符串转换为 Token 流。采用经典的字符流驱动架构，支持完整的 YaoXiang 语言词法规范。

**代码量**：约 800 行（7 个源文件）

---

## 功能清单

### 已实现功能

| 功能 | 状态 | 说明 |
|------|------|------|
| **关键字** | ✅ | 17 个：pub, use, spawn, ref, mut, if, elif, else, match, while, for, in, return, break, continue, as, unsafe |
| **整数字面量** | ✅ | 十进制、十六进制(0x)、八进制(0o)、二进制(0b)，支持下划线分隔符，溢出检测 |
| **浮点字面量** | ✅ | 小数点、指数(e/E)、前导小数点(.5)，下划线分隔符 |
| **字符串字面量** | ✅ | 单行字符串，三引号多行字符串(`"""`)，转义序列 |
| **字符字面量** | ✅ | 单引号，支持与字符串相同的转义 |
| **布尔字面量** | ✅ | `true`, `false` |
| **Void 字面量** | ✅ | `void` |
| **F-String** | ✅ | `f"..."`，支持 `{expression}` 插值、`\{\{`/`\}\}` 转义（RFC-012） |
| **运算符** | ✅ | `+`, `-`, `*`, `/`, `%`, `=`, `==`, `!=`, `<`, `<=`, `>`, `>=`, `&&`, `||`, `!`, `&`, `&mut`, `::`, `...`, `..`, `->`, `=>`, `?` |
| **分隔符** | ✅ | `(`, `)`, `[`, `]`, `{`, `}`, `@`, `,`, `:`, `;`, `|`, `.` |
| **注释** | ✅ | 单行 `//`、嵌套多行 `/* /* */ */` |
| **绑定语法** | ✅ | `[` `]` 作为绑定位置标记（RFC-004） |
| **泛型语法** | ✅ | `(` `)` 作为泛型参数容器（RFC-010） |
| **符号表** | ✅ | SymbolTable / SymbolIndex，支持按名称/文件查找 |
| **验证器** | ✅ | BindingValidator、GenericValidator、TypeSystemValidator |

---

## 测试覆盖

| 测试类型 | 数量 | 状态 |
|----------|------|------|
| 内联测试 | 18 | ✅ 通过 |
| F-String 测试 | 10 | ✅ 通过 |
| 符号表测试 | 3 | ✅ 通过 |
| **tests/ 目录测试** | 150+ | ⚠️ 未编译 |

**关键问题**：`tests/` 目录下 11 个文件共约 150+ 个测试未被编译，处于死代码状态。

---

## RFC 对比

| RFC | 实现状态 | 说明 |
|-----|----------|------|
| RFC-004 绑定语法 | ✅ 已实现 | `[` `]` 正确识别，BindingValidator 实现完整 |
| RFC-010 统一类型语法 | ⚠️ 部分实现 | 泛型用 `()` 而非 `<>`，但缺少 `where`, `trait`, `interface`, `impl`, `forall`, `exists` 关键字 |
| RFC-011 泛型系统 | ⚠️ 验证器实现 | `TypeSystemValidator` 已实现，但缺少高阶类型相关的关键字支持 |
| RFC-012 F-String | ✅ 已实现 | 完整的 `f"..."` 词法分析 |

---

## 代码质量评估

| 维度 | 评分 | 说明 |
|------|------|------|
| 未完成事项 | 3 | 激活测试、补充关键字、重构转义处理 |
| 测试覆盖 | 中等 | 31 个通过测试，150+ 未编译 |
| 文档质量 | 良好 | 每个文件都有模块级 `//!` 注释 |
| 代码架构 | 良好 | 职责分离清晰 |

---

## 待改进项

1. **激活 tests/ 目录测试**：约 150+ 测试处于死代码状态
2. **补充缺失关键字**：`where`, `trait`, `interface`, `impl`, `forall`, `exists`
3. **抽取公共转义处理函数**：`literals.rs` 中有大量重复的转义处理代码
