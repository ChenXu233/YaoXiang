# RFC-001：并作模型与错误处理系统

> **状态**: 审核中
> **作者**: 晨煦
> **创建日期**: 2025-01-05
> **最后更新**: 2025-01-05

## 设计来源与参考

本文档的设计基于以下文档，并作为 language-spec 的详细设计来源：

| 文档 | 关系 | 说明 |
|------|------|------|
| [async-whitepaper](../async-whitepaper.md) | **设计源头** | 并作模型的理论基础和核心概念 |
| [language-spec](../language-spec.md) | **规范目标** | 本RFC的设计将整合到语言规范中 |

> **说明**：本RFC是对 [async-whitepaper](../async-whitepaper.md) 中提出的并作模型的具体化和规范化，将其转化为可实现的语言设计。

## 摘要

提出YaoXiang的并作模型（三层并发架构）、副作用处理机制、DAG依赖分析、Result类型系统和错误图可视化。核心设计理念源自《易经》"万物并作，吾以观复"——以同步语法描述逻辑，运行时自动并发执行。

## 动机

### 为什么需要并作模型？

当前主流语言的并发模型存在明显缺陷：

| 语言 | 并发模型 | 问题 |
|------|----------|------|
| Rust | async/await + tokio | 异步传染、学习曲线陡峭 |
| Go | goroutine | 无类型安全、逃逸分析困难 |
| Python | asyncio | GIL限制、性能差 |
| JavaScript | Promise/async | 回调地狱虽缓解但仍复杂 |

### 核心矛盾

1. **透明性 vs 可控性**：完全透明但不可控 vs 完全可控但不透明
2. **并发 vs 可调试**：并发程序难调试 vs 可调试程序难并发
3. **安全 vs 性能**：GC安全但有开销 vs 手动管理高效但危险

### 并作模型的解决思路

```
传统困境 ────────────── 并作模型 ────────────── 解决方案
完全透明但不可控 ────→ 三层模型 ────→ 从L1到L3渐进式采用
并发程序难调试 ──────→ 图调试 ──────→ 错误图可视化
GC安全但有开销 ──────→ 所有权 ──────→ Rust式编译时检查
```

## 提案

### 1. 并作模型：三层并发架构

并作模型允许开发者以同步、顺序的思维描述逻辑，而语言运行时令其中的计算单元如万物并作般自动、高效地并发执行。

#### 三层抽象

| 层级 | 模式 | 语法标记 | 执行方式 | 适用场景 |
|------|------|----------|----------|----------|
| **L1** | `@block`同步 | `@block` | 完全顺序 | 调试、新手、关键代码段 |
| **L2** | 显式spawn | `spawn` | 开发者可控 | 中级用户、需要控制并发 |
| **L3** | 完全透明 | 无（默认） | 自动最优 | 专家、自动并行优化 |

#### L1: `@block`同步模式

完全顺序执行，禁用所有并发优化，便于调试和理解。

```yaoxiang
# L1: @block 同步模式（注解放在返回类型后）
fetch_sync: (String) -> JSON @block = (url) => {
    HTTP.get(url).json()
}

main: () -> Void @block = () => {
    # 严格顺序执行，无任何并发
    data1 = fetch_sync("https://api.example.com/data1")
    data2 = fetch_sync("https://api.example.com/data2")
    process(data1, data2)
}
```

> **📌 开放问题**: `@block`注解位置详见[附录A](#附录a-block注解位置讨论)

#### L2: 显式spawn并发

开发者显式标记可并发单元，保持可控性同时获得并发收益。

```yaoxiang
# L2: 显式 spawn 并发
fetch_data: (String) -> JSON spawn = (url) => {
    HTTP.get(url).json()
}

process_users_and_posts: () -> Void spawn = () => {
    users = fetch_data("https://api.example.com/users")
    posts = fetch_data("https://api.example.com/posts")

    # users 和 posts 自动并行执行
    print("Users: " + users.length.to_string())
    print("Posts: " + posts.length.to_string())
}

# 显式并发块
compute_all: () -> (Int, Int, Int) spawn = () => {
    (a, b, c) = spawn {
        heavy_calc(1),
        heavy_calc(2),
        heavy_calc(3)
    }
    (a, b, c)
}
```

#### L3: 完全透明（默认）

无需任何标记，编译器自动分析依赖并生成最优并行执行计划。

```yaoxiang
# L3: 完全透明（默认模式）
heavy_calc: (Int) -> Int = (n) => {
    fibonacci(n)  # 默认惰性求值
}

auto_parallel: (Int) -> Int = (n) => {
    # 系统自动分析：a, b, c 无依赖，可完全并行
    a = heavy_calc(1)
    b = heavy_calc(2)
    c = heavy_calc(3)

    a + b + c  # 在此处等待所有结果
}

# 数据并行循环（自动并行化）
parallel_sum: (Int) -> Int spawn = (n) => {
    total = spawn for i in 0..n {
        fibonacci(i)  # 每次迭代并行执行
    }
    total
}
```

### 2. 副作用处理与DAG依赖分析

#### 2.1 核心原则：同步语法，异步本质

```
程序 → 编译时分析 → DAG（有向无环图）
           ↓
       运行时调度 → 自动并行
```

#### 2.2 Void函数急切规则

返回`Void`的函数自动急切求值，保证副作用必然执行。

```yaoxiang
# Void 函数自动急切执行
log_message: (String) -> Void = (msg) => {
    print(msg)  # 副作用，立即执行
}

side_effect_demo: () -> Void = () => {
    msg = get_message()  # 可能返回惰性
    log_message(msg)     # 这里的 print 必定执行
}
```

#### 2.3 资源冲突检测

编译时分析资源访问模式，自动串行化冲突操作。

```rust
// 编译时分析资源访问
struct ResourceAccess {
    reads: Set<ResourceId>,   // 读取的资源
    writes: Set<ResourceId>,  // 写入的资源
}

// 冲突规则矩阵
// ╔═══════════╦══════════╦══════════╗
// ║   访问    ║   读     ║    写    ║
// ╠═══════════╬══════════╬══════════╣
// ║   读      ║  可并行  ║  串行化  ║
// ║   写      ║  串行化  ║  串行化  ║
// ╚═══════════╩══════════╩══════════╝

// 示例
file1 = open("a.txt")  # 资源1
file2 = open("b.txt")  # 资源2
// file1 读和 file2 写 → 串行化
// file1 读和 file2 读 → 可并行
```

#### 2.4 手动控制注解

| 注解 | 行为 | 使用场景 |
|------|------|----------|
| `@eager` | 强制急切求值 | 需要立即获取结果的计算 |
| `@ordered` | 强制顺序执行 | 有序副作用、调试 |
| `@concurrent` | 强制并发（默认） | 显式声明并发意图 |

```yaoxiang
# 强制急切求值
@eager
expensive_computation: (Int) -> Int = (n) => {
    fibonacci(n)
}

# 强制顺序执行（即使在spawn块内）
@ordered
log_sequential: (String) -> Void = (msg) => {
    print("[Order] " + msg)
}

# 数据并行的有序收集
ordered_collect: (Int) -> [Int] = (n) => {
    result = spawn for i in 0..n {
        heavy_calc(i)
    }
    @ordered(result)  # 保持原始顺序
}
```

#### 2.5 并作图可视化

```
源代码                     并作图（DAG）
┌─────────────────┐       ┌─────────────┐
│ a = calc(1)     │       │   calc(1)   │
├─────────────────┤       │      │      │
│ b = calc(2)     │  →    │   calc(2)   │  ← 可并行
├─────────────────┤       │      │      │
│ c = calc(3)     │       │   calc(3)   │  ← 可并行
├─────────────────┤       └───────┬─────┘
│ d = a + b + c   │               │ 依赖汇聚
└─────────────────┘       ┌───────▼──────┐
                          │   add.add    │
                          └─────────────┘
```

#### 2.6 DAG 详细设计

根据 RFC-008 三层运行时架构，DAG 是 Standard 和 Full 运行时的核心组件。

##### 2.6.1 节点类型

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              DAG 节点类型                                    │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐       │
│  │ TaskNode    │  │ ValueNode   │  │ ControlNode │  │ SpawnNode   │       │
│  │ 计算任务    │  │ 值节点      │  │ 控制流节点  │  │ 并发入口    │       │
│  └─────────────┘  └─────────────┘  └─────────────┘  └─────────────┘       │
│                                                                             │
│  TaskNode 详细：                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  字段                    │  类型              │  说明                │   │
│  ├─────────────────────────────────────────────────────────────────────┤   │
│  │  id                      │  NodeId            │  唯一标识            │   │
│  │  kind                    │  TaskKind          │  计算/IO/控制        │   │
│  │  inputs                  │  [ValueNodeId]     │  输入值依赖          │   │
│  │  outputs                 │  [ValueNodeId]     │  输出值              │   │
│  │  span                    │  Span              │  源码位置            │   │
│  │  eager                   │  bool              │  是否急切求值        │   │
│  │  blocking                │  bool              │  是否阻塞调度        │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

##### 2.6.2 边类型

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              DAG 边类型                                      │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  边类型              │  符号    │  语义                          │   │
│  ├─────────────────────────────────────────────────────────────────────┤   │
│  │  DataEdge            │  ──►     │  数据依赖（值流动）            │   │
│  │  ControlEdge         │  ──●     │  控制依赖（顺序执行）          │   │
│  │  SpawnEdge           │  ──◎     │  并发入口（可并行起点）        │   │
│  │  JoinEdge            │  ──✦     │  汇聚点（等待所有前置）        │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
│  边冲突规则：                                                                │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  边A \ 边B    │   DataEdge    │  ControlEdge  │  SpawnEdge         │   │
│  ├─────────────────────────────────────────────────────────────────────┤   │
│  │  DataEdge     │   可并行      │   串行化     │   可并行           │   │
│  │  ControlEdge  │   串行化      │   串行化     │   串行化           │   │
│  │  SpawnEdge    │   可并行      │   串行化     │   可并行           │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

##### 2.6.3 DAG 构建算法

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           DAG 构建算法流程                                   │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  输入：AST（抽象语法树）                                                    │
│  输出：DAG（有向无环图）                                                    │
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  算法：DAGBuilder::build(ast)                                        │   │
│  │                                                                     │   │
│  │  1. 遍历所有函数声明，收集 TaskNode                                  │   │
│  │     for fn in ast.functions {                                       │   │
│  │         task_id = create_task_node(fn)                              │   │
│  │     }                                                               │   │
│  │                                                                     │   │
│  │  2. 分析数据依赖（Def-Use 链）                                       │   │
│  │     for (def, uses) in def_use_chains {                             │   │
│  │         for use in uses {                                           │   │
│  │             add_data_edge(def, use)                                 │   │
│  │         }                                                           │   │
│  │     }                                                               │   │
│  │                                                                     │   │
│  │  3. 分析控制依赖（Statement 顺序）                                   │   │
│  │     for i in 0..statements.len() - 1 {                              │   │
│  │         add_control_edge(i, i + 1)                                  │   │
│  │     }                                                               │   │
│  │                                                                     │   │
│  │  4. 识别 spawn 点（并发入口）                                        │   │
│  │     for spawn in ast.spawns {                                       │   │
│  │         add_spawn_edge(spawn)                                        │   │
│  │     }                                                               │   │
│  │                                                                     │   │
│  │  5. 验证 DAG 无环（拓扑排序）                                        │   │
│  │     if !is_acyclic() { error("循环依赖") }                          │   │
│  │                                                                     │   │
│  │  6. 优化边（合并冗余边）                                             │   │
│  │     dag = optimize_edges(dag)                                       │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
│  时间复杂度：O(n + m) 其中 n=节点数, m=边数                                 │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

##### 2.6.4 拓扑排序与调度

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                        拓扑排序（Kahn 算法）                                 │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  输入：DAG                                                               │
│  输出：拓扑序（执行顺序）                                                  │
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  fn topological_sort(dag: DAG) -> Vec[NodeId] {                     │   │
│  │                                                                     │   │
│  │      // 1. 计算入度                                                 │   │
│  │      in_degree = dag.nodes.map(|n| n.incoming_edges.len())          │   │
│  │                                                                     │   │
│  │      // 2. 收集入度为 0 的节点（可并行执行）                        │   │
│  │      ready_queue: Vec[NodeId] = in_degree.filter(|d| d == 0)        │   │
│  │                                                                     │   │
│  │      // 3. 广度优先遍历                                             │   │
│  │      result = []                                                    │   │
│  │      while ready_queue not empty {                                  │   │
│  │          node = ready_queue.pop_front()                             │   │
│  │          result.push(node)                                          │   │
│  │                                                                     │   │
│  │          // 减少后继节点的入度                                       │   │
│  │          for succ in node.successors {                              │   │
│  │              in_degree[succ] -= 1                                   │   │
│  │              if in_degree[succ] == 0 {                              │   │
│  │                  ready_queue.push_back(succ)                        │   │
│  │              }                                                       │   │
│  │          }                                                           │   │
│  │      }                                                               │   │
│  │                                                                     │   │
│  │      // 4. 检测循环依赖                                             │   │
│  │      if result.len() != dag.nodes.len() {                           │   │
│  │          error("循环依赖检测")                                      │   │
│  │      }                                                               │   │
│  │                                                                     │   │
│  │      result                                                         │   │
│  │  }                                                                   │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
│  调度优先级（Work Stealing 适用）：                                         │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  优先级  │  条件                              │  说明                │   │
│  ├─────────────────────────────────────────────────────────────────────┤   │
│  │  P0      │  入度 == 0 && 依赖最少            │  立即执行            │   │
│  │  P1      │  入度 == 0 && 已准备好            │  正常调度            │   │
│  │  P2      │  阻塞任务（@blocking）            │  单独队列            │   │
│  │  P3      │  IO 密集型                        │  异步处理            │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

##### 2.6.5 DAG 可视化格式

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                         DOT 语言导出格式                                     │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  用于 Graphviz 可视化：                                                     │
│                                                                             │
│  digraph YaoXiang_DAG {                                                    │
│      rankdir=TB;                                                           │
│      node [shape=box, style=rounded];                                      │
│                                                                             │
│      // 节点定义                                                           │
│      "calc_1" [label="calc(1)\n(line 10)", fillcolor=lightblue];          │
│      "calc_2" [label="calc(2)\n(line 11)", fillcolor=lightblue];          │
│      "calc_3" [label="calc(3)\n(line 12)", fillcolor=lightblue];          │
│      "add"   [label="a + b + c\n(line 13)", fillcolor=lightgreen];        │
│                                                                             │
│      // 边定义（数据依赖）                                                 │
│      "calc_1" -> "add" [label="a"];                                        │
│      "calc_2" -> "add" [label="b"];                                        │
│      "calc_3" -> "add" [label="c"];                                        │
│                                                                             │
│      // 并发分组                                                           │
│      { rank=same; "calc_1"; "calc_2"; "calc_3" }                           │
│  }                                                                          │
│                                                                             │
│  生成图像：                                                                 │
│                                                                             │
│         ┌─────────┐                                                        │
│         │ calc(1) │                                                        │
│            │                                                           │
│            ▼                                                           │
│         ┌─────────┐     ┌─────────┐                                        │
│         │ calc(2) │ ──▶ │  add    │                                        │
│            │           └─────────┘                                        │
│            ▼                                                           │
│         ┌─────────┐                                                        │
│         │ calc(3) │                                                        │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

##### 2.6.6 DAG 与 RFC-008 运行时的关系

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                      DAG 在三层运行时中的位置                                │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  🟢 Embedded Runtime                                                │   │
│  │                                                                     │   │
│  │  无 DAG：即时执行器直接执行                                          │   │
│  │  路径：AST ──▶ 即时解释执行                                         │   │
│  │  对应 Phase：P8 + P12                                               │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                    │                                        │
│                                    ▼                                        │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  🔵 Standard Runtime（核心）                                        │   │
│  │                                                                     │   │
│  │  DAG 核心：                                                         │   │
│  │  ┌───────────────────────────────────────────────────────────────┐ │   │
│  │  │                     AST                                        │ │   │
│  │  │                       │                                        │ │   │
│  │  │                       ▼                                        │ │   │
│  │  │               ┌───────────────┐                                │ │   │
│  │  │               │ DAG Builder    │ ← P9（核心）                  │ │   │
│  │  │               └───────┬───────┘                                │ │   │
│  │  │                       │                                        │ │   │
│  │  │                       ▼                                        │ │   │
│  │  │               ┌───────────────┐                                │ │   │
│  │  │               │ Topological    │ ← 拓扑排序                    │ │   │
│  │  │               │ Sort           │                                │ │   │
│  │  │               └───────┬───────┘                                │ │   │
│  │  │                       │                                        │ │   │
│  │  │                       ▼                                        │ │   │
│  │  │               ┌───────────────┐                                │ │   │
│  │  │               │ Scheduler      │ ← P10（调度器）               │ │   │
│  │  │               └───────┬───────┘                                │ │   │
│  │  │                       │                                        │ │   │
│  │  │                       ▼                                        │ │   │
│  │  │               ┌───────────────┐                                │ │   │
│  │  │               │ VM Executor   │ ← P11（解释器）                │ │   │
│  │  │               └───────────────┘                                │ │   │
│  │  └───────────────────────────────────────────────────────────────┘ │   │
│  │  对应 Phase：P8 → P9 → P10 → P11                                  │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                    │                                        │
│                                    ▼                                        │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  🟣 Full Runtime                                                    │   │
│  │                                                                     │   │
│  │  Standard + Work Stealing + @blocking：                            │   │
│  │  ┌───────────────────────────────────────────────────────────────┐ │   │
│  │  │  Standard Runtime（全部）                                      │ │   │
│  │  │         │                                                      │ │   │
│  │  │         ▼                                                      │ │   │
│  │  │  ┌───────────────────────────┐                                │ │   │
│  │  │  │ Work Stealer (P13)        │ ← 负载均衡                     │ │   │
│  │  │  └─────────────┬─────────────┘                                │ │   │
│  │  │                │                                              │ │   │
│  │  │                ▼                                              │ │   │
│  │  │  ┌───────────────────────────┐                                │ │   │
│  │  │  │ @blocking Handler (P14)   │ ← 同步执行保证                 │ │   │
│  │  │  └───────────────────────────┘                                │ │   │
│  │  └───────────────────────────────────────────────────────────────┘ │   │
│  │  对应 Phase：P8 → P9 → P10 → P11 → P13 → P14                      │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
│  详见：[RFC-008: Runtime 并发模型与调度器脱耦设计](./008-runtime-concurrency-model.md) |
│  详见：[Phase 实现方案（RFC-008 对齐版）](../../plan/PHASE-IMPLEMENTATION-PLAN.md)    │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 3. Result类型与错误图系统

#### 3.1 Result类型定义

```yaoxiang
# 标准Result类型（统一构造器语法）
type Result[T, E] = ok(T) | err(E)

# 使用示例
type ParseError = invalid_format | unexpected_eof | position(Int)

parse_config: (String) -> Result[Config, ParseError] = (content) => {
    if content.is_empty() {
        err(invalid_format)
    } else {
        ok(parse(content))
    }
}
```

#### 3.2 错误传播语法

采用Rust式`?`运算符，实现透明错误传播。

```yaoxiang
# Rust式 ? 运算符
process() -> Result[Data, Error] = {
    data = fetch_data()?      # 自动等待并检查错误
    processed = transform(data)?
    save(processed)?          # 错误自动向上传播
}

# 模式匹配处理错误
handle_result: (Result[Int, Error]) -> String = (result) => {
    match result {
        ok(value) => "Success: " + value.to_string()
        err(e) => match e {
            network_error => "Network failed"
            parse_error => "Parse failed"
            _ => "Unknown error"
        }
    }
}
```

#### 3.3 错误图可视化

错误图类似调用栈，但显示DAG中的错误传播路径。

```
┌─────────────────────────────────────────────────────────────┐
│ Error: Division by zero                                     │
├─────────────────────────────────────────────────────────────┤
│ Error Graph:                                                │
│                                                             │
│   main()                                                   │
│     │                                                       │
│     ├──► calculate()                                        │
│     │         │                                             │
│     │         └──► divide(100, 0)  ✗ [Division by zero]     │
│     │                                                       │
│     └──► fallback()  ✓                                      │
│                                                             │
│ 因果链: main → calculate → divide                           │
│ 捕获位置: calculate (第42行)                                │
└─────────────────────────────────────────────────────────────┘
```

##### 3.3.1 错误图数据结构

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                            错误图数据结构                                    │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  ErrorNode（错误节点）                                               │   │
│  │  ┌─────────────────────────────────────────────────────────────────┐ │   │
│  │  │  字段              │  类型              │  说明                 │ │   │
│  │  ├─────────────────────────────────────────────────────────────────┤ │   │
│  │  │  id                │  ErrorNodeId       │  唯一标识             │ │   │
│  │  │  kind              │  ErrorKind         │  错误类型             │ │   │
│  │  │  message           │  String            │  错误消息             │ │   │
│  │  │  span              │  Span              │  源码位置             │ │   │
│  │  │  task_id           │  NodeId            │  对应 DAG 节点        │ │   │
│  │  │  cause             │  Option[ErrorNodeId] │  直接原因           │ │   │
│  │  │  effects           │  [ErrorNodeId]     │  影响的节点           │ │   │
│  │  └─────────────────────────────────────────────────────────────────┘ │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  ErrorKind（错误类型）                                               │   │
│  │                                                                     │   │
│  │  enum ErrorKind {                                                   │   │
│  │      division_by_zero,                                              │   │
│  │      null_pointer,                                                  │   │
│  │      out_of_bounds,                                                 │   │
│  │      type_mismatch,                                                 │   │
│  │      resource_busy,                                                 │   │
│  │      io_error(String),      // 变体携带信息                         │   │
│  │      custom(String),                                                   │   │
│  │  }                                                                   │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

##### 3.3.2 错误传播算法

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          错误传播算法                                        │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  当 DAG 节点执行出错时，错误沿依赖边向上游传播：                            │
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  fn propagate_error(error: Error, node: NodeId) {                   │   │
│  │                                                                     │   │
│  │      // 1. 创建错误节点                                              │   │
│  │      error_node = ErrorNode {                                       │   │
│  │          id: generate_id(),                                         │   │
│  │          kind: error.kind,                                          │   │
│  │          message: error.message,                                    │   │
│  │          span: node.span,                                           │   │
│  │          task_id: node,                                             │   │
│  │          cause: None,                                               │   │
│  │          effects: [],                                               │   │
│  │      }                                                               │   │
│  │                                                                     │   │
│  │      // 2. 查找所有依赖此节点的上游节点                              │   │
│  │      dependents = dag.get_dependents(node)                          │   │
│  │                                                                     │   │
│  │      // 3. 向上游传播错误                                           │   │
│  │      for dep in dependents {                                        │   │
│  │          if dep.is_blocking() {                                     │   │
│  │              // @blocking 节点：立即终止                             │   │
│  │              error_node.effects.push(dep)                           │   │
│  │              return error_node                                      │   │
│  │          } else {                                                   │   │
│  │              // 普通节点：错误继续传播                               │   │
│  │              error_node.effects.push(dep)                           │   │
│  │              propagate_error_to(dep, error_node)                    │   │
│  │          }                                                           │   │
│  │      }                                                               │   │
│  │                                                                     │   │
│  │      error_node                                                     │   │
│  │  }                                                                   │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
│  错误影响范围示例：                                                          │
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  DAG 结构                          错误传播                         │   │
│  │                                                                     │   │
│  │      [A]                             [A]                            │   │
│  │       │                               │                              │   │
│  │       ▼                               ▼                              │   │
│  │  [B]──▶[C]          ✗            [B]──▶[C] ✗                        │   │
│  │   │    │                        │      │                            │   │
│  │   ▼    ▼                        ▼      ▼                            │   │
│  │  [D]  [E]                      [D]    [E]                            │   │
│  │                                                                     │   │
│  │  C 出错后，B 被标记为受影响（A 继续执行）                           │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

##### 3.3.3 错误图可视化格式

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                       错误图可视化输出格式                                   │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  1. 终端文本格式（默认）：                                                  │
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  × Error: Division by zero                                          │   │
│  │                                                                     │   │
│  │    ┌───────────────────────────────────────────────────────────┐    │   │
│  │    │ Error Graph:                                               │    │   │
│  │    │                                                             │    │   │
│  │    │   main()                                                    │    │   │
│  │    │     │                                                       │    │   │
│  │    │     ├──► calculate()  ✓                                     │    │   │
│  │    │     │         │                                             │    │   │
│  │    │     │         └──► divide(100, 0)  ✗ [Division by zero]    │    │   │
│  │    │     │                                                       │    │   │
│  │    │     └──► fallback()  ✓                                      │    │   │
│  │    │                                                             │    │   │
│  │    │   因果链: main → calculate → divide                          │    │   │
│  │    │   捕获位置: calculate (第42行)                               │    │   │
│  │    └───────────────────────────────────────────────────────────┘    │   │
│  │                                                                     │   │
│  │    ┌───────────────────────────────────────────────────────────┐    │   │
│  │    │ Stack Trace:                                               │    │   │
│  │    │                                                             │    │   │
│  │    │   × divide at example.yx:42:15                             │    │   │
│  │    │   └─ calculate at example.yx:38:8                          │    │   │
│  │    │      └─ main at example.yx:10:1                            │    │   │
│  │    └───────────────────────────────────────────────────────────┘    │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
│  2. JSON 格式（用于 IDE 集成）：                                            │
│                                                                             │
│  {                                                                           │
│    "error": {                                                               │
│      "kind": "division_by_zero",                                           │
│      "message": "division by zero",                                        │
│      "span": {                                                              │
│        "file": "example.yx",                                               │
│        "line": 42,                                                         │
│        "column": 15                                                        │
│      }                                                                     │
│    },                                                                       │
│    "error_graph": {                                                         │
│      "root": "main",                                                       │
│      "nodes": [                                                            │
│        {"id": "main", "status": "completed", "children": ["calculate"]},   │
│        {"id": "calculate", "status": "failed",                             │
│         "children": ["divide"], "cause": "divide"},                       │
│        {"id": "divide", "status": "error",                                │
│         "error": {"kind": "division_by_zero"}},                           │
│        {"id": "fallback", "status": "completed"}                          │
│      ]                                                                     │
│    },                                                                       │
│    "stack_trace": [                                                         │
│      {"function": "divide", "file": "example.yx", "line": 42},             │
│      {"function": "calculate", "file": "example.yx", "line": 38},          │
│      {"function": "main", "file": "example.yx", "line": 10}                │
│    ]                                                                         │
│  }                                                                           │
│                                                                             │
│  3. DOT 格式（用于 Graphviz）：                                             │
│                                                                             │
│  digraph ErrorGraph {                                                       │
│      node [shape=box, style="rounded,filled"];                             │
│      edge [color=red];                                                      │
│                                                                             │
│      "main" [fillcolor=lightgreen];                                        │
│      "calculate" [fillcolor=lightyellow];                                  │
│      "divide" [fillcolor=lightcoral];                                      │
│      "fallback" [fillcolor=lightgreen];                                    │
│                                                                             │
│      "main" -> "calculate";                                                │
│      "main" -> "fallback";                                                 │
│      "calculate" -> "divide" [color=red, label="error"];                   │
│  }                                                                          │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

#### 3.4 错误处理最佳实践

```yaoxiang
# 组合多个可能出错的操作
batch_process: ([String]) -> Result[[String], Error] = (items) => {
    results = items.map(item => {
        process_item(item)?
    })
    ok(results)
}

# with? 语法糖（未来特性）
validate_user: (User) -> Result[ValidatedUser, ValidationError] = (user) => {
    name = user.name.with?(validate_name)?
    email = user.email.with?(validate_email)?
    ok(ValidatedUser(name, email))
}
```

## 详细设计

### 1. 编译器改动

| 组件 | 改动 |
|------|------|
| lexer | 新增 `@block`、`@eager`、`@ordered`、`spawn` 关键字/注解 |
| parser | 支持 `spawn` 标记函数、`spawn { }` 并发块 |
| typecheck | Send/Sync 约束检查、资源访问分析 |
| codegen | DAG构建、依赖图生成 |

### 2. 运行时改动

| 组件 | 改动 |
|------|------|
| 调度器 | 工作窃取调度器、优先级队列 |
| DAG执行器 | 并作图遍历、依赖解析 |
| 错误传播 | 错误图生成、栈追踪增强 |
| 线程池 | 并发任务管理、资源隔离 |

### 3. 类型系统影响

```
┌─────────────────────────────────────────────────────────────┐
│ 类型约束层次                                                 │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│   Send ──► 可安全跨线程传输                                 │
│     │                                                       │
│     └──► Sync ──► 可安全跨线程共享                          │
│          │                                                  │
│          └──► 满足 Send + Sync 的类型可自动并发              │
│                                                             │
│ Arc[T] 实现 Send + Sync（线程安全引用计数）                  │
│ Mutex[T] 提供内部可变性                                     │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 4. 语法BNF

```
FunctionDecl    ::= Identifier ':' Type ('spawn' | '@block')? '=' Expr
ConcurrentBlock ::= 'spawn' '{' Expr (',' Expr)* '}' | 'spawn' 'for' ...
Pattern         ::= 'ok' '(' Pattern ')' | 'err' '(' Pattern ')' | '_'
ErrorPropagate  ::= Expr '?'
```

### 5. 向后兼容性

- ✅ 完全向后兼容
- 仅新增语法，原有代码无需修改
- L3模式（完全透明）为默认，需考虑渐进启用策略

## 权衡

### 优点

1. **渐进式采用**：三层模型适应不同技能水平
2. **自然语法**：同步代码获得并行性能
3. **编译时安全**：Send/Sync约束消除数据竞争
4. **可调试性**：错误图提供清晰的错误传播视图
5. **资源安全**：编译时资源冲突检测防止死锁

### 缺点

1. **学习曲线**：需要理解DAG依赖概念
2. **编译时间**：全程序DAG分析可能较慢
3. **工具链复杂度**：需要全新的调试和可视化工具
4. **调试挑战**：L3模式的调试需要特殊工具支持

## 替代方案

| 方案 | 为什么不选择 |
|------|--------------|
| 仅支持显式async/await | 无法实现真正的透明并发 |
| 仅支持完全透明并发 | 用户失去控制权，无法调试 |
| Go式goroutine | 无类型安全、无法编译时检查 |
| 仅L1模式 | 放弃并作模型的核心价值 |

## 实现策略

### 阶段划分

1. **阶段1 (v0.1)**: `@block`同步模式、基础类型
2. **阶段2 (v0.3)**: `spawn`块、显式并发
3. **阶段3 (v0.5)**: L3完全透明、DAG自动分析
4. **阶段4 (v1.0)**: 错误图、图调试器

### 依赖关系

- RFC-001 无外部依赖（基础核心）
- RFC-002（libuv集成）可并行开发
- RFC-003（版本规划）可并行开发

### 风险

1. **DAG分析性能**: 全程序分析可能O(n²)，需要优化算法
2. **工具链缺失**: 调试器需要从零开发
3. **用户接受度**: 透明并发违反直觉，需要良好文档

## 开放问题

> **说明**：以下问题需要在 RFC 进入"已接受"状态前确定

- [x] @ordered在循环中的语义定义（详见讨论）
- [x] @block注解位置选择（详见附录A，已决议）

### 已删除问题（不属于RFC-001设计范围）

以下问题已移至其他文档或实现阶段处理：

| 问题 | 移至 |
|------|------|
| L3模式回退机制 | RFC-008 或实现规范 |
| 嵌套spawn依赖分析 | P9-DAG 实现规范 |
| 错误图交互式体验 | 工具链设计文档 |
| 性能基准测试 | 测试规范 |

---

## 附录

### 附录A：@block注解位置讨论

> **讨论状态**: 开放中
> **发起者**: @晨煦
> **日期**: 2025-01-05

#### 问题描述

`@block`注解放在函数签名的哪个位置？需要平衡**可读性**和**一致性**。

#### 备选方案

| 方案 | 语法示例 | 优点 | 缺点 |
|------|----------|------|------|
| **A（当前）** | `() -> Void @block` | 与`spawn`位置一致 | 类型后紧跟注解，稍显拥挤 |
| **B** | `@block () -> Void` | 注解醒目、一眼可见 | 与`spawn`不一致 |
| **C** | `() @block -> Void` | 视觉分割清晰 | 破坏类型语法结构 |
| **D** | `fn @block name() -> Void { }` | 关键字开头 | 关键字+注解组合冗长 |

#### 各方案分析

**方案A（推荐）**：
- 与`spawn`保持同一位置，降低学习成本
- 可通过IDE高亮和代码风格规范弥补可读性

**方案B**：
- 注解更醒目，但与`spawn`位置不一致

**方案C**：
- 破坏`参数列表 -> 返回类型`的结构完整性

**方案D**：
- 冗长，与YaoXiang简洁语法风格不符

#### 推荐代码风格（缓解方案A的可读性问题）

```yaoxiang
# 将注解放在单独一行
main:
    () -> Void @block
= () => {
    ...
}
```

#### 社区讨论记录

- [待添加]

#### 决议

> **状态**: ✅ 已决定
> **日期**: 2025-01-05

采用 **方案A**：注解放在返回类型后，与 `spawn` 位置一致。

理由：
1. 与 `spawn` 保持同一位置，降低学习成本
2. 可通过 IDE 高亮和代码风格规范弥补可读性

#### 讨论：@ordered 在循环中的语义

> **状态**: ✅ 已决定
> **日期**: 2025-01-05

**问题**：`@ordered` 在循环中的语义是什么？

**决策**：`@ordered` 只影响结果收集顺序，不影响并行执行。

```yaoxiang
# @ordered 不影响循环内部的并行执行
@ordered
results = spawn for i in 0..100 {
    heavy_calc(i)  # 仍然并行执行
}
# results 按原始顺序 [0, 1, 2, ..., 99] 收集
```

**理由**：
1. 保持循环内部的并行性能
2. 只在最终结果收集时按顺序排列
3. 类似于 JavaScript `Promise.all()` + `Array.map()` 的行为

---

### 附录B：设计决策记录

> 本附录记录RFC中已确定的设计决策及其理由。

| 决策 | 决定 | 日期 | 记录人 |
|------|------|------|--------|
| 采用三层并发架构 | L1/L2/L3 渐进式 | 2025-01-05 | @晨煦 |
| @block与spawn位置一致 | 方案A | 2025-01-05 | @晨煦 |
| @ordered 循环语义 | 只影响结果收集顺序 | 2025-01-05 | @晨煦 |

---

### 附录C：实现问题分析（重要）

> 本附录记录 RFC-001 设计中可能影响实现的问题及其缓解建议。

#### C.1 严重问题（高优先级）

| 问题 | 严重程度 | 影响 | 缓解建议 |
|------|----------|------|----------|
| **L3 透明模式回退机制缺失** | 高 | 用户无法从 L3 降级到 L2/L1 | 实现编译时 feature flag：`#[mode(standard)]` |
| **嵌套 spawn 依赖分析复杂度** | 高 | O(n²) 或更高，可能导致编译卡死 | 限制嵌套深度或使用增量分析 |
| **DAG 构建与执行顺序冲突** | 高 | 错误传播可能与并行执行顺序不一致 | 错误沿依赖边向上游传播，不考虑执行顺序 |

#### C.2 中等问题（中优先级）

| 问题 | 严重程度 | 影响 | 缓解建议 |
|------|----------|------|----------|
| **DAG 构建时间复杂度** | 中 | 大型项目编译缓慢 | 使用增量构建 + 缓存 |
| **资源冲突检测规则不完整** | 中 | 可能遗漏资源竞争 | 明确资源类型 + 静态分析 |
| **Send/Sync 约束检查不完整** | 中 | 数据竞争风险 | 编译时强制检查 |

#### C.3 低优先级问题

| 问题 | 严重程度 | 影响 | 缓解建议 |
|------|----------|------|----------|
| **@ordered 性能开销** | 低 | 顺序收集可能降低并行收益 | 只在必要时使用 |
| **错误图内存占用** | 低 | 大型 DAG 可能产生大量错误节点 | 错误节点惰性创建 |

#### C.4 与 RFC-008 的交互问题

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                    RFC-001 与 RFC-008 交互问题                              │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  问题1：三层运行时如何选择？                                                 │
│  解答：通过编译时 feature flag：                                            │
│  ```yaoxiang                                                                   │
│  # Embedded 模式（无 DAG）                                                   │
│  #[runtime(embedded)]                                                         │
│  main: () -> Void = () => { ... }                                            │
│                                                                             │
│  # Standard 模式（DAG + 调度器）                                             │
│  #[runtime(standard)]                                                        │
│  main: () -> Void = () => { ... }                                            │
│                                                                             │
│  # Full 模式（Work Stealing + @blocking）                                    │
│  #[runtime(full)]                                                            │
│  main: () -> Void = () => { ... }                                            │
│  ```                                                                           │
│                                                                             │
│  问题2：DAG 节点与运行时的接口是什么？                                       │
│  解答：统一的 Task trait：                                                   │
│  ```rust                                                                           │
│  trait Task {                                                                │
│      fn inputs(&self) -> &[NodeId];                                         │
│      fn outputs(&self) -> &[NodeId];                                        │
│      fn execute(&self, ctx: &mut Context) -> Result<(), Error>;             │
│  }                                                                               │
│  ```                                                                           │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

#### C.5 实现优先级建议

根据 MVP 路线（P1-P4 → P8 → P11），建议实现顺序：

1. **第一阶段（P1-P4）**：编译前端
   - 实现 L1 `@block` 注解
   - 实现基础的 spawn 语法解析

2. **第二阶段（P8-P11）**：标准运行时
   - 实现 DAG Builder（P9）
   - 实现 Scheduler（P10）
   - 实现 VM 解释器（P11）

3. **第三阶段（P13-P14）**：Full 运行时
   - 实现 Work Stealing（P13）
   - 实现 @blocking（P14）

---

### 附录D：术语表

| 术语 | 定义 |
|------|------|
| 并作模型 | YaoXiang的并发范式，同步语法、异步本质 |
| DAG | 有向无环图，描述计算依赖关系 |
| spawn | 并作函数标记，表示可并发执行 |
| @block | 同步注解，禁用并发优化 |
| 错误图 | 可视化的错误传播路径 |

## 参考文献

- [Rust async book](https://rust-lang.github.io/async-book/)
- [Go并发模式](https://golang.org/doc/effective_go#concurrency)
- [工作窃取调度](https://en.wikipedia.org/wiki/Work_stealing)
- [《易经》并作思想](https://en.wikipedia.org/wiki/I_Ching)
- [并作模型白皮书](../async-whitepaper.md)
- [YaoXiang 指南](../YaoXiang-book.md)
- [YaoXiang 语言规范](../language-spec.md)