use std.io

add: (Int, Int) -> Int = (a, b) => {
    return a + b
}

greet: (String) -> String = (name) => {
    return "Hello, ${name}!"
}

type Point = {
    x: Float,
    y: Float
}

main: () -> Void = () => {
    p: Point = Point(1.0, 2.0);

    sum: Int = add(10, 20);
    print(sum);

    message: String = greet("World");

    return
}
