# YaoXiang 前端架构激进重构方案

> 版本: 2.0 | 日期: 2026-01-27 | 状态: 规划完成

## 📋 重构目标

### 核心目标
- **降低耦合度**: 消除模块间强依赖，实现松耦合架构
- **文件分层优化**: 清晰的分层架构，每层职责单一
- **可维护性提升**: 大文件拆分，职责清晰
- **可扩展性增强**: 为未来特性（RFC-012等）预留扩展空间

### 成功指标
- [ ] 所有文件控制在500行以内
- [ ] 模块依赖关系清晰，无循环依赖
- [ ] 公共API简化，隐藏内部实现
- [ ] 测试覆盖率达到85%以上
- [ ] 编译时间减少20%（通过更好的模块化）

---

## 🏗️ 新架构设计

### 1. 分层架构图

```
┌─────────────────────────────────────┐
│           Frontend API              │  ← 公共接口层
│        (frontend/mod.rs)            │
├─────────────────────────────────────┤
│  Lexer → Parser → TypeCheck → Const│  ← 流水线层
│     │        │         │       │   │
│     ▼        ▼         ▼       ▼   │
├─────────────────────────────────────┤
│          Shared Utilities           │  ← 共享工具层
│    (error, span, diagnostic)        │
├─────────────────────────────────────┤
│        Core Algorithm Layer         │  ← 核心算法层
│  (type_system, const_eval, parse)   │
└─────────────────────────────────────┘
```

### 2. 模块重新组织

#### **第一层: 核心算法层 (Core Algorithm Layer)**

```
src/frontend/core/
├── mod.rs                    # 核心模块入口
├── lexer/
│   ├── mod.rs               # 词法分析器接口
│   ├── tokenizer.rs         # Tokenizer实现 (从1270行拆分)
│   ├── state.rs            # 词法状态管理 (新建)
│   └── literals.rs         # 字面量处理 (拆分)
├── parser/
│   ├── mod.rs              # 解析器接口
│   ├── ast.rs              # AST定义 (保持305行)
│   ├── Pratt/              # Pratt解析器核心 (新建目录)
│   │   ├── mod.rs
│   │   ├── nud.rs          # 前缀解析 (从896行拆分)
│   │   ├── led.rs          # 中缀解析 (保持380行)
│   │   └── precedence.rs   # 优先级处理 (拆分)
│   ├── statements/         # 语句解析 (新建目录)
│   │   ├── mod.rs
│   │   ├── declarations.rs  # 声明语句 (从1399行拆分)
│   │   ├── expressions.rs   # 表达式语句 (拆分)
│   │   └── control_flow.rs  # 控制流 (拆分)
│   └── types/              # 类型解析 (新建目录)
│       ├── mod.rs
│       ├── parser.rs       # 类型解析器 (从614行拆分)
│       └── constraints.rs  # 类型约束解析 (新建)
├── type_system/            # 类型系统 (激进拆分types.rs)
│   ├── mod.rs
│   ├── vars.rs            # TypeVar, ConstVar (拆分)
│   ├── mono_poly.rs       # MonoType, PolyType (拆分)
│   ├── constraints.rs      # TypeConstraint (拆分)
│   ├── unify.rs           # Unify算法 (拆分)
│   └── pretty_print.rs    # 类型打印 (新建)
└── const_eval/            # 常量求值
    ├── mod.rs
    ├── evaluator.rs       # 常量求值器 (从677行重命名)
    ├── functions.rs      # Const函数 (从536行拆分)
    └── static_assert.rs  # 静态断言 (保持490行)
```

#### **第二层: 共享工具层 (Shared Utilities)**

```
src/frontend/shared/
├── mod.rs
├── error/
│   ├── mod.rs
│   ├── diagnostic.rs       # 统一诊断信息
│   ├── span.rs            # Span处理
│   ├── result.rs          # 统一Result类型
│   └── conversion.rs      # 错误转换
├── diagnostics/
│   ├── mod.rs
│   ├── formatter.rs       # 诊断格式化
│   ├── severity.rs        # 严重级别
│   └── code.rs            # 错误码定义
└── utils/
    ├── mod.rs
    ├── mem.rs             # 内存管理工具
    ├── debug.rs           # 调试工具
    └── panic.rs           # panic处理
```

#### **第三层: 类型检查层 (Type Checking Layer)**

```
src/frontend/typecheck/
├── mod.rs                 # 类型检查入口
├── inference/             # 类型推断 (拆分infer.rs)
│   ├── mod.rs
│   ├── expressions.rs    # 表达式推断 (拆分)
│   ├── statements.rs     # 语句推断 (拆分)
│   └── patterns.rs       # 模式匹配推断 (新建)
├── checking/             # 类型检查 (拆分check.rs)
│   ├── mod.rs
│   ├── subtyping.rs      # 子类型检查 (拆分)
│   ├── assignment.rs     # 赋值检查 (拆分)
│   └── compatibility.rs # 兼容性检查 (拆分)
├── specialization/       # 泛型特化
│   ├── mod.rs
│   ├── algorithm.rs      # 特化算法 (从488行拆分)
│   └── substitution.rs  # 替换逻辑 (新建)
├── traits/              # 特质系统
│   ├── mod.rs
│   ├── solver.rs        # 特质求解器 (从274行拆分)
│   ├── coherence.rs     # 一致性检查 (新建)
│   └── object_safety.rs # 对象安全 (新建)
└── gat/                # GAT支持 (保持529行，优化结构)
    ├── mod.rs
    ├── checker.rs       # GAT检查器
    └── higher_rank.rs   # 高阶类型
```

#### **第四层: 高级类型层 (Advanced Type Level)**

```
src/frontend/type_level/
├── mod.rs               # 类型级计算入口
├── conditional_types.rs  # 条件类型 (保持)
├── dependent_types.rs    # 依赖类型 (保持)
├── evaluation/          # 类型级计算 (新建目录)
│   ├── mod.rs
│   ├── normalize.rs     # 范式化
│   ├── reduce.rs        # 归约
│   └── unify.rs         # 类型级统一
├── operations/          # 类型级操作 (新建目录)
│   ├── mod.rs
│   ├── arithmetic.rs    # 算术运算
│   ├── comparison.rs   # 比较运算
│   └── logic.rs        # 逻辑运算
└── tests.rs            # 测试 (保持)
```

#### **第五层: 公共接口层 (Public API Layer)**

```
src/frontend/
├── mod.rs               # 编译器公共接口 (简化)
├── compiler.rs          # 编译器核心逻辑 (从235行拆分)
├── pipeline.rs          # 编译流水线 (新建)
└── config.rs            # 编译配置 (新建)
```

---

## 📅 分阶段实施计划

### 🚀 阶段 1: 紧急拆分 (Week 1-3)

#### **Day 1-2: 准备阶段**

**步骤 1.1: 创建新目录结构**
- **子任务 1.1.1**: 在 `src/frontend/` 下创建 `type_system/` 主目录
  - 预期耗时: 5分钟
  - 验收标准: 目录创建成功，可通过 `ls src/frontend/type_system/` 验证

- **子任务 1.1.2**: 创建 `type_system/` 的4个子目录
  - 创建 `vars/`, `mono_poly/`, `constraints/`, `unify/`
  - 预期耗时: 5分钟
  - 验收标准: 所有子目录存在

- **子任务 1.1.3**: 创建 `core/` 主目录及其子结构
  - 创建 `core/lexer/`, `core/parser/`, `core/type_system/`, `core/const_eval/`
  - 预期耗时: 10分钟
  - 验收标准: 目录结构完整

- **子任务 1.1.4**: 创建 `shared/` 主目录
  - 创建 `shared/error/`, `shared/diagnostics/`, `shared/utils/`
  - 预期耗时: 5分钟
  - 验收标准: 目录结构完整

**步骤 1.2: 运行现有测试基准**
- **子任务 1.2.1**: 记录当前编译性能
  - 运行 `time cargo build --release` 记录基准时间
  - 保存结果到 `metrics/pre_refactor_build_time.txt`

- **子任务 1.2.2**: 运行完整测试套件
  - 运行 `cargo test --all` 确保当前测试全部通过
  - 记录测试通过数量: ___/___
  - 保存到 `metrics/pre_refactor_test_results.txt`

- **子任务 1.2.3**: 记录代码统计
  - 运行 `cloc src/frontend/typecheck/types.rs` 记录原始行数
  - 记录: 总行数 ___ 行，代码行数 ___ 行
  - 保存到 `metrics/pre_refactor_loc.txt`

---

#### **Day 3-7: 拆分 typecheck/types.rs**

**目标**: 将1948行的巨无霸文件拆分为职责清晰的模块
**预期总耗时**: 5天 (每天1天)

**Day 3: 分析与拆解**

- **子任务 1.3.1: 文件结构分析**
  - **1.3.1.1**: 使用工具分析文件结构 (30分钟)
    ```bash
    rg "^pub struct|^pub enum|^impl" src/frontend/typecheck/types.rs
    ```
  - **1.3.1.2**: 手动标注可分离的逻辑块 (60分钟)
    - 标记 TypeVar/ConstVar 定义 (行1-200)
    - 标记 MonoType/PolyType 定义 (行201-600)
    - 标记 TypeConstraint 定义 (行601-1000)
    - 标记 Unify 算法实现 (行1001-1948)
  - **1.3.1.3**: 确定模块边界和依赖关系 (30分钟)
    - 绘制依赖关系图
    - 确定公共接口

- **子任务 1.3.2: 创建第一个模块 `vars.rs` (2小时)**
  - **1.3.2.1**: 复制 TypeVar/ConstVar 相关代码到新文件
  - **1.3.2.2**: 调整导入路径，修复编译错误
  - **1.3.2.3**: 运行 `cargo check` 验证编译通过
  - **验收标准**: vars.rs 独立编译成功

**Day 4: 完成基础模块**

- **子任务 1.3.3: 创建 `mono_poly.rs` (2小时)**
  - **1.3.3.1**: 复制 MonoType/PolyType 代码
  - **1.3.3.2**: 修复与 vars.rs 的依赖关系
  - **1.3.3.3**: 运行测试验证功能正常

- **子任务 1.3.4: 创建 `constraints.rs` (2小时)**
  - **1.3.4.1**: 复制 TypeConstraint/ConstraintSet 代码
  - **1.3.4.2**: 实现 UnionFind 结构
  - **1.3.4.3**: 验证约束求解逻辑

**Day 5: 核心算法模块**

- **子任务 1.3.5: 创建 `unify.rs` (3小时)**
  - **1.3.5.1**: 复制 Unify 算法和 Substitution
  - **1.3.5.2**: 实现 Unifier 结构
  - **1.3.5.3**: 添加类型扩展方法
  - **验收标准**: unify.rs 编译通过，算法逻辑正确

**Day 6: 整合与依赖修复**

- **子任务 1.3.6: 创建 `type_system/mod.rs` (1小时)**
  - **1.3.6.1**: 定义模块入口文件
  - **1.3.6.2**: 统一导出所有公共接口
  - **1.3.6.3**: 定义 TypeSystemError 类型

- **子任务 1.3.7: 更新依赖关系 (2小时)**
  - **1.3.7.1**: 修改 `src/frontend/typecheck/mod.rs` 的导入
    ```rust
    // 从
    pub use types::*;
    // 改为
    pub use crate::type_system::{MonoType, PolyType, ...};
    ```
  - **1.3.7.2**: 使用搜索替换工具批量更新引用路径
    ```bash
    rg "use.*types::" src/frontend/typecheck/ --replace 'use crate::type_system::$1'
    ```
  - **1.3.7.3**: 逐个文件修复编译错误

**Day 7: 验证与清理**

- **子任务 1.3.8: 全面验证 (2小时)**
  - **1.3.8.1**: 运行 `cargo check --all` 确保无编译错误
  - **1.3.8.2**: 运行 `cargo test type_system` 运行类型系统测试
  - **1.3.8.3**: 运行 `cargo test --all` 确保所有测试通过
  - **1.3.8.4**: 性能对比: 编译时间变化 < 10%

- **子任务 1.3.9: 清理旧文件 (1小时)**
  - **1.3.9.1**: 确认新模块功能正常后，删除原 `types.rs`
  - **1.3.9.2**: 更新 git 并提交
  - **1.3.9.3**: 创建标签 `refactor/types-complete`

**验收标准**:
- [ ] `types.rs` 完全删除
- [ ] 新模块编译通过: `cargo check --all`
- [ ] 所有测试绿色: `cargo test --all` (测试数量对比基准)
- [ ] 性能无明显下降: 编译时间变化 < 10%
- [ ] 最大文件行数检查: `find src/frontend -name "*.rs" -exec wc -l {} + | sort -nr | head -1` < 1000行

---

#### **Day 8-14: 拆分 lexer/mod.rs**

**目标**: 将1270行的词法分析器拆分为独立模块
**预期总耗时**: 7天

**Day 8-9: 分析与拆解 lexer**

- **子任务 1.4.1: 分析 lexer 结构 (2小时)**
  - **1.4.1.1**: 运行分析工具
    ```bash
    rg "^pub struct|^impl.*Tokenizer" src/frontend/lexer/mod.rs
    ```
  - **1.4.1.2**: 识别核心逻辑
    - Tokenizer 主结构 (行1-300)
    - 状态管理代码 (行301-600)
    - 字面量处理逻辑 (行601-900)
    - 辅助方法 (行901-1270)
  - **1.4.1.3**: 设计新模块接口

- **子任务 1.4.2: 创建 `tokenizer.rs` (3小时)**
  - **1.4.2.1**: 提取 Tokenizer 结构体和主要方法
  - **1.4.2.2**: 保留核心 tokenization 逻辑
  - **1.4.2.3**: 委托状态和字面量处理到专门模块

- **子任务 1.4.3: 创建 `state.rs` (2小时)**
  - **1.4.3.1**: 提取 LexerState 结构
  - **1.4.3.2**: 实现关键字查找等状态相关方法

**Day 10-11: 完成拆分**

- **子任务 1.4.4: 创建 `literals.rs` (2小时)**
  - **1.4.4.1**: 提取所有字面量处理方法
  - **1.4.4.2**: 数字、字符串、字符处理逻辑
  - **1.4.4.3**: 转义序列处理

- **子任务 1.4.5: 创建模块入口 (1小时)**
  - **1.4.5.1**: 创建 `core/lexer/mod.rs`
  - **1.4.5.2**: 统一导出接口
  - **1.4.5.3**: 更新对外接口

**Day 12-13: 迁移测试**

- **子任务 1.4.6: 迁移测试文件 (3小时)**
  - **1.4.6.1**: 创建测试目录结构
    ```bash
    mkdir -p src/frontend/core/lexer/tests
    ```
  - **1.4.6.2**: 复制所有测试文件
    ```bash
    cp src/frontend/lexer/tests/* src/frontend/core/lexer/tests/
    ```
  - **1.4.6.3**: 批量更新导入路径
    ```bash
    find src/frontend/core/lexer/tests -name "*.rs" -exec sed -i \
      's/use crate::lexer::/use crate::core::lexer::/g' {} \;
    ```

- **子任务 1.4.7: 验证测试 (2小时)**
  - **1.4.7.1**: 运行词法测试
    ```bash
    cargo test core::lexer
    ```
  - **1.4.7.2**: 修复测试中的编译错误
  - **1.4.7.3**: 确保测试覆盖率不下降

**Day 14: 整合验证**

- **子任务 1.4.8: 更新上层依赖 (2小时)**
  - **1.4.8.1**: 更新 parser 模块的导入路径
  - **1.4.8.2**: 更新 frontend 主模块的导出
  - **1.4.8.3**: 运行集成测试

- **子任务 1.4.9: 全面验证 (2小时)**
  - **1.4.9.1**: 编译检查
    ```bash
    cargo check --all
    ```
  - **1.4.9.2**: 运行相关测试
    ```bash
    cargo test lexer
    cargo test parser
    ```
  - **1.4.9.3**: 清理旧文件
  - **1.4.9.4**: 提交更改，创建标签 `refactor/lexer-complete`

**验收标准**:
- [ ] lexer/mod.rs 拆分完成
- [ ] 新模块编译通过
- [ ] 词法测试全部通过: `cargo test lexer`
- [ ] 解析器测试正常: `cargo test parser`
- [ ] 性能无明显退化

---

#### **Day 15-21: 拆分 parser/stmt.rs**

**目标**: 重新组织parser结构，消除1399行的stmt.rs
**预期总耗时**: 7天

**Day 15-16: 分析 parser 结构**

- **子任务 1.5.1: 分析 stmt.rs 结构 (3小时)**
  - **1.5.1.1**: 分析文件内容分布
    ```bash
    rg "^//.*声明|^//.*表达式|^//.*控制流" src/frontend/parser/stmt.rs
    ```
  - **1.5.1.2**: 识别逻辑分组
    - 声明相关代码 (行1-500): 函数、结构体、枚举、变量声明
    - 表达式语句 (行501-900): 赋值、表达式语句
    - 控制流代码 (行901-1399): if、while、for、match
  - **1.5.1.3**: 识别 Pratt 解析器部分
    - nud.rs (前缀解析)
    - led.rs (中缀解析)
    - precedence.rs (优先级)

- **子任务 1.5.2: 创建目录结构 (1小时)**
  ```bash
  mkdir -p src/frontend/core/parser/{statements,Pratt,types}
  mkdir -p src/frontend/core/parser/tests/{declarations,expressions,control_flow}
  ```

**Day 17-18: 拆分语句解析**

- **子任务 1.5.3: 创建 `statements/declarations.rs` (3小时)**
  - **1.5.3.1**: 提取函数声明解析
    - `parse_function_decl()`
    - `parse_function_param()`
    - `parse_generic_params()`
  - **1.5.3.2**: 提取结构体和枚举声明
    - `parse_struct_decl()`
    - `parse_enum_decl()`
  - **1.5.3.3**: 提取变量声明
    - `parse_variable_decl()`
    - `parse_use_decl()`

- **子任务 1.5.4: 创建 `statements/expressions.rs` (2小时)**
  - **1.5.4.1**: 提取表达式语句解析
  - **1.5.4.2**: 提取赋值语句解析
  - **1.5.4.3**: 提取块语句解析

**Day 19: 拆分控制流**

- **子任务 1.5.5: 创建 `statements/control_flow.rs` (3小时)**
  - **1.5.5.1**: 提取 if-else 解析
  - **1.5.5.2**: 提取循环解析 (while, for)
  - **1.5.5.3**: 提取 match 解析
  - **1.5.5.4**: 提取 break/continue/return 解析

**Day 20: 处理 Pratt 解析器**

- **子任务 1.5.6: 拆分 Pratt 模块 (2小时)**
  - **1.5.6.1**: 优化 nud.rs，提取前缀解析
  - **1.5.6.2**: 优化 led.rs，保持中缀解析
  - **1.5.6.3**: 提取 precedence.rs

- **子任务 1.5.7: 拆分类型解析 (1小时)**
  - **1.5.7.1**: 创建 `types/parser.rs`
  - **1.5.7.2**: 提取类型解析逻辑

**Day 21: 整合与验证**

- **子任务 1.5.8: 创建模块入口 (1小时)**
  - **1.5.8.1**: 创建 `core/parser/mod.rs`
  - **1.5.8.2**: 创建 `core/parser/statements/mod.rs`
  - **1.5.8.3**: 统一导出接口

- **子任务 1.5.9: 迁移测试 (3小时)**
  - **1.5.9.1**: 迁移解析器测试文件
    ```bash
    # 分类迁移
    mv src/frontend/parser/tests/decl_tests.rs \
       src/frontend/core/parser/tests/declarations/
    mv src/frontend/parser/tests/expr_tests.rs \
       src/frontend/core/parser/tests/expressions/
    mv src/frontend/parser/tests/control_tests.rs \
       src/frontend/core/parser/tests/control_flow/
    ```
  - **1.5.9.2**: 批量更新导入路径
  - **1.5.9.3**: 修复测试编译错误

- **子任务 1.5.10: 全面验证 (2小时)**
  - **1.5.10.1**: 编译检查
    ```bash
    cargo check --all
    ```
  - **1.5.10.2**: 运行解析器测试
    ```bash
    cargo test core::parser
    ```
  - **1.5.10.3**: 运行完整测试套件
    ```bash
    cargo test --all
    ```
  - **1.5.10.4**: 提交更改，创建标签 `refactor/parser-complete`

**验收标准**:
- [ ] stmt.rs 完全拆分
- [ ] 新模块编译通过
- [ ] 解析器测试全部通过: `cargo test parser`
- [ ] 集成测试正常: `cargo test --all`
- [ ] 最大文件行数 < 500行

---

### ⚡ 阶段 2: 抽象提取 (Week 4-6)

#### **Week 4: 统一错误处理系统**
**目标**: 消除20+文件中的重复错误处理代码
**预期总耗时**: 5天

**Day 22: 设计错误处理系统**

- **子任务 2.1.1: 分析现有错误处理 (2小时)**
  - **2.1.1.1**: 搜索所有错误处理模式
    ```bash
    rg "return Err\(" src/frontend/ --type rust | head -20
    ```
  - **2.1.1.2**: 识别重复模式
    - `if condition { return Err(...) }`
    - `ensure!(condition, error)`
    - 自定义错误类型
  - **2.1.1.3**: 设计统一接口

- **子任务 2.1.2: 创建错误处理宏 (2小时)**
  - **2.1.2.1**: 创建 `shared/error/macros.rs`
    ```rust
    #[macro_export]
    macro_rules! ensure {
        ($condition:expr, $error:expr) => {
            if !$condition {
                return Err($error.into());
            }
        };
    }
    ```
  - **2.1.2.2**: 创建 `ensure_index!`, `ensure_some!` 等宏
  - **2.1.2.3**: 创建 `ErrorContext` trait

**Day 23-24: 在 lexer 中应用**

- **子任务 2.2.1: 重构词法错误处理 (3小时)**
  - **2.2.1.1**: 更新 `core/lexer/tokenizer.rs`
    ```rust
    // 从
    if self.pos >= self.source.len() {
        return Err(LexicalError::UnexpectedEOF);
    }
    // 改为
    ensure!(self.pos < self.source.len(),
            LexicalError::UnexpectedEOF);
    ```
  - **2.2.2**: 简化数字解析错误处理
  - **2.2.3**: 简化字符串解析错误处理

- **子任务 2.2.2: 验证 lexer 重构 (2小时)**
  - **2.2.2.1**: 编译检查
    ```bash
    cargo check -p core-lexer
    ```
  - **2.2.2.2**: 运行测试
    ```bash
    cargo test core::lexer
    ```

**Day 25-26: 推广到 parser**

- **子任务 2.3.1: 重构解析器错误处理 (4小时)**
  - **2.3.1.1**: 更新 `core/parser/statements/declarations.rs`
  - **2.3.1.2**: 更新 `core/parser/statements/expressions.rs`
  - **2.3.1.3**: 更新 `core/parser/statements/control_flow.rs`
  - **2.3.1.4**: 更新 Pratt 解析器

- **子任务 2.3.2: 验证 parser 重构 (2小时)**
  - **2.3.2.1**: 编译检查
    ```bash
    cargo check -p core-parser
    ```
  - **2.3.2.2**: 运行解析器测试
    ```bash
    cargo test core::parser
    ```

**Day 27-28: 推广到 typecheck**

- **子任务 2.4.1: 重构类型检查错误处理 (4小时)**
  - **2.4.1.1**: 更新类型系统模块
  - **2.4.1.2**: 更新类型检查模块
  - **2.4.1.3**: 更新特质求解模块

- **子任务 2.4.2: 验证 typecheck 重构 (2小时)**
  - **2.4.2.1**: 编译检查
    ```bash
    cargo check -p typecheck
    ```
  - **2.4.2.2**: 运行类型检查测试
    ```bash
    cargo test typecheck
    ```

**Day 29: 验证与度量**

- **子任务 2.5.1: 全面验证 (2小时)**
  - **2.5.1.1**: 运行完整测试套件
    ```bash
    cargo test --all
    ```
  - **2.5.1.2**: 检查代码重复率变化
    ```bash
    # 使用工具检查重复错误处理代码
    cpd --minimum-tokens 20 --files src/frontend/shared/error/
    ```

- **子任务 2.5.2: 度量改进 (1小时)**
  - **2.5.2.1**: 统计消除的重复代码行数
  - **2.5.2.2**: 对比重构前后错误处理一致性
  - **2.5.2.3**: 提交更改，创建标签 `refactor/error-handling-complete`

**验收标准**:
- [ ] 错误处理宏在所有模块中应用
- [ ] 编译通过: `cargo check --all`
- [ ] 所有测试通过: `cargo test --all`
- [ ] 代码重复率检查: 使用工具验证重复代码 < 200行
- [ ] 错误处理一致性: 100% 模块使用统一宏

#### **Week 5: 类型推断抽象**
**目标**: 创建可重用的类型推断接口，消除重复逻辑
**预期总耗时**: 5天

**Day 30-31: 分析类型推断逻辑**

- **子任务 2.6.1: 分析 infer.rs (3小时)**
  - **2.6.1.1**: 搜索类型推断相关代码
    ```bash
    rg "fn infer_" src/frontend/typecheck/infer.rs
    ```
  - **2.6.1.2**: 识别重复模式
    - 表达式类型推断
    - 语句类型推断
    - 模式类型推断
  - **2.6.1.3**: 绘制推断流程图

- **子任务 2.6.2: 设计 TypeInferrer trait (2小时)**
  - **2.6.2.1**: 定义通用接口
    ```rust
    pub trait TypeInferrer {
        type Expr;
        type Stmt;
        type Pattern;

        fn infer_expr(&mut self, expr: &Self::Expr)
            -> Result<MonoType, TypeInferenceError>;
        fn infer_stmt(&mut self, stmt: &Self::Stmt)
            -> Result<(), TypeInferenceError>;
        fn infer_pattern(&mut self, pattern: &Self::Pattern)
            -> Result<MonoType, TypeInferenceError>;
    }
    ```

**Day 32-33: 实现抽象**

- **子任务 2.6.3: 创建推断器实现 (4小时)**
  - **2.6.3.1**: 实现 `ExprInferrer`
    -  Literal 推断
    -  Identifier 推断
    -  BinaryOp 推断
    -  UnaryOp 推断
  - **2.6.3.2**: 实现 `StmtInferrer`
  - **2.6.3.3**: 实现 `PatternInferrer`

- **子任务 2.6.4: 重构现有代码 (3小时)**
  - **2.6.4.1**: 更新 `typecheck/infer.rs` 使用 trait
  - **2.6.4.2**: 消除重复的推断逻辑
  - **2.6.4.3**: 简化类型检查器

**Day 34-35: 验证与优化**

- **子任务 2.6.5: 验证抽象效果 (3小时)**
  - **2.6.5.1**: 编译检查
    ```bash
    cargo check --all
    ```
  - **2.6.5.2**: 运行类型推断测试
    ```bash
    cargo test typecheck::infer
    ```
  - **2.6.5.3**: 检查代码重复减少量

- **子任务 2.6.6: 性能测试 (2小时)**
  - **2.6.6.1**: 运行性能基准测试
    ```bash
    cargo bench --features type_inference
    ```
  - **2.6.6.2**: 对比抽象前后性能

**验收标准**:
- [ ] TypeInferrer trait 完整实现
- [ ] 编译通过: `cargo check --all`
- [ ] 类型推断测试通过: `cargo test infer`
- [ ] 代码重复率降低 > 50%
- [ ] 性能无明显退化 (变化 < 10%)

#### **Week 6: 完成抽象提取**
**目标**: 全面优化抽象后的代码，提升整体质量
**预期总耗时**: 5天

**Day 36-37: 代码审查与优化**

- **子任务 2.7.1: 代码质量审查 (4小时)**
  - **2.7.1.1**: 运行 clippy 检查
    ```bash
    cargo clippy --all
    ```
  - **2.7.1.2**: 修复所有警告
  - **2.7.1.3**: 优化代码风格

- **子任务 2.7.2: API 优化 (3小时)**
  - **2.7.2.1**: 审查公共接口
  - **2.7.2.2**: 简化复杂接口
  - **2.7.2.3**: 添加必要文档

**Day 38-39: 测试完善**

- **子任务 2.7.3: 增加测试覆盖 (4小时)**
  - **2.7.3.1**: 识别测试盲点
    ```bash
    cargo tarpaulin --out Xml
    ```
  - **2.7.3.2**: 添加缺失的单元测试
  - **2.7.3.3**: 添加集成测试

- **子任务 2.7.4: 性能基准测试 (2小时)**
  - **2.7.4.1**: 创建基准测试
    ```rust
    #[bench]
    fn bench_type_inference(b: &mut Bencher) {
        // 基准测试代码
    }
    ```
  - **2.7.4.2**: 运行并记录结果

**Day 40: 文档与总结**

- **子任务 2.7.5: 文档更新 (2小时)**
  - **2.7.5.1**: 更新 API 文档
    ```bash
    cargo doc --all --no-deps
    ```
  - **2.7.5.2**: 编写迁移指南
  - **2.7.5.3**: 更新 CHANGELOG

- **子任务 2.7.6: 阶段总结 (1小时)**
  - **2.7.6.1**: 统计改进指标
  - **2.7.6.2**: 对比阶段1和阶段2成果
  - **2.7.6.3**: 提交阶段2成果

**验收标准**:
- [ ] 代码质量: clippy 无警告
- [ ] 测试覆盖: 覆盖率 > 85%
- [ ] 文档完整: API 文档生成成功
- [ ] 性能稳定: 基准测试无退化
- [ ] 阶段验收: 提交 `refactor/phase2-complete`

---

### 🎯 阶段 3: 架构优化 (Week 7-10)

#### **Week 7-8: 洋葱架构改造**
**目标**: 实现依赖倒置，建立清晰的分层架构
**预期总耗时**: 10天

**Day 41-42: 设计核心 Trait**

- **子任务 3.1.1: 分析依赖关系 (3小时)**
  - **3.1.1.1**: 绘制当前依赖图
    ```bash
    cargo dep-graph --all > current_deps.dot
    ```
  - **3.1.1.2**: 识别循环依赖
  - **3.1.1.3**: 设计目标依赖图

- **子任务 3.1.2: 创建核心 Trait (4小时)**
  - **3.1.2.1**: 创建 `core/type_system/traits.rs`
    ```rust
    pub trait TypeDisplay {
        fn fmt(&self, f: &mut Formatter<'_>) -> Result;
    }

    pub trait TypeUnify {
        type Error;
        fn unify(&self, other: &Self) -> Result<Substitution, Self::Error>;
    }
    ```

- **子任务 3.1.3: 实现 Trait (2小时)**
  - **3.1.3.1**: 为 MonoType 实现 TypeDisplay
  - **3.1.3.2**: 为 MonoType 实现 TypeUnify

**Day 43-45: 重构类型检查器**

- **子任务 3.2.1: 实现依赖注入 (4小时)**
  - **3.2.1.1**: 修改 TypeChecker 使用泛型
    ```rust
    pub struct TypeChecker<T: TypeEnvironment, S: SymbolTable> {
        type_env: T,
        symbol_table: S,
        // ...
    }
    ```
  - **3.2.1.2**: 消除硬编码依赖
  - **3.2.1.3**: 提高可测试性

- **子任务 3.2.2: 重构实现 (4小时)**
  - **3.2.2.1**: 注入具体实现
    ```rust
    let checker = TypeChecker::new(
        Box::new(DefaultTypeEnvironment::new()),
        Box::new(DefaultSymbolTable::new()),
    );
    ```
  - **3.2.2.2**: 测试替换实现

**Day 46-48: 实现事件系统**

- **子任务 3.3.1: 设计事件系统 (3小时)**
  - **3.3.1.1**: 定义事件接口
    ```rust
    pub trait EventSubscriber {
        fn on_typecheck_progress(&self, progress: TypecheckProgress);
        fn on_error(&self, error: &Diagnostic);
    }
    ```

- **子任务 3.3.2: 实现事件发布 (4小时)**
  - **3.3.2.1**: 修改 Compiler 结构
    ```rust
    pub struct Compiler {
        subscribers: Vec<Box<dyn EventSubscriber>>,
        // ...
    }
    ```
  - **3.3.2.2**: 在关键点发布事件

**Day 49-50: 验证架构改进**

- **子任务 3.4.1: 依赖分析 (2小时)**
  - **3.4.1.1**: 重新绘制依赖图
    ```bash
    cargo dep-graph --all > refactored_deps.dot
    ```
  - **3.4.1.2**: 确认循环依赖消除

- **子任务 3.4.2: 功能验证 (3小时)**
  - **3.4.2.1**: 编译检查
    ```bash
    cargo check --all
    ```
  - **3.4.2.2**: 运行测试
    ```bash
    cargo test --all
    ```

#### **Week 9-10: 性能优化**
**目标**: 通过缓存和增量编译提升性能
**预期总耗时**: 10天

**Day 51-53: 实现编译缓存**

- **子任务 3.5.1: 设计缓存结构 (3小时)**
  - **3.5.1.1**: 创建 `shared/cache/mod.rs`
    ```rust
    pub struct CompilationCache {
        inference_cache: FxHashMap<(ExprId, TypeEnvId), MonoType>,
        unify_cache: LruCache<(TypeId, TypeId), Substitution>,
        // ...
    }
    ```

- **子任务 3.5.2: 实现缓存逻辑 (4小时)**
  - **3.5.2.1**: 实现类型推断缓存
    ```rust
    pub fn get_inferred_type(&self, expr_id: ExprId, env_id: TypeEnvId)
        -> Option<&MonoType> {
        self.inference_cache.get(&(expr_id, env_id))
    }
    ```
  - **3.5.2.2**: 实现统一算法缓存

- **子任务 3.5.3: 集成缓存 (2小时)**
  - **3.5.3.1**: 修改类型推断器使用缓存
  - **3.5.3.2**: 修改类型统一器使用缓存

**Day 54-56: 实现增量编译**

- **子任务 3.6.1: 设计变更跟踪 (3小时)**
  - **3.6.1.1**: 创建 `shared/change_tracking/mod.rs`
    ```rust
    pub struct ChangeTracker {
        changed_files: HashSet<PathBuf>,
        dependencies: HashMap<PathBuf, HashSet<PathBuf>>,
    }
    ```

- **子任务 3.6.2: 实现增量检查 (4小时)**
  - **3.6.2.1**: 实现文件变更检测
  - **3.6.2.2**: 实现依赖分析
  - **3.6.2.3**: 实现增量类型检查

- **子任务 3.6.3: 优化缓存策略 (2小时)**
  - **3.6.3.1**: 实现缓存失效策略
  - **3.6.3.2**: 实现内存管理

**Day 57-60: 性能调优与验证**

- **子任务 3.7.1: 性能基准测试 (3小时)**
  - **3.7.1.1**: 创建综合基准测试
    ```rust
    #[bench]
    fn bench_full_compilation(b: &mut Bencher) {
        // 完整编译基准测试
    }
    ```
  - **3.7.1.2**: 测试缓存效果
  - **3.7.1.3**: 测试增量编译效果

- **子任务 3.7.2: 瓶颈分析 (3小时)**
  - **3.7.2.1**: 使用 profiling 工具分析
  - **3.7.2.2**: 识别性能热点
  - **3.7.2.3**: 针对性优化

- **子任务 3.7.3: 优化实现 (3小时)**
  - **3.7.3.1**: 内联热点函数
  - **3.7.3.2**: 优化数据结构
  - **3.7.3.3**: 减少内存分配

- **子任务 3.7.4: 最终验证 (2小时)**
  - **3.7.4.1**: 编译检查
    ```bash
    cargo check --all
    ```
  - **3.7.4.2**: 完整测试
    ```bash
    cargo test --all
    ```
  - **3.7.4.3**: 性能对比
  - **3.7.4.4**: 提交最终成果

**阶段3验收标准**:
- [ ] 架构清晰: 无循环依赖
- [ ] 依赖注入: 所有模块可替换
- [ ] 事件系统: 正常工作
- [ ] 缓存效果: 命中率 > 50%
- [ ] 增量编译: 性能提升 > 20%
- [ ] 性能优化: 编译时间减少 20%

---

## 🎯 总结与下一步

### 分阶段实施路径

#### **阶段 1: 紧急拆分 (Week 1-3)**
- Week 1: 拆分 types.rs → 4个模块 (Day 1-7)
- Week 2: 拆分 lexer/mod.rs → 3个模块 (Day 8-14)
- Week 3: 拆分 parser/stmt.rs → 3个模块 (Day 15-21)

#### **阶段 2: 抽象提取 (Week 4-6)**
- Week 4: 统一错误处理系统 (Day 22-29)
- Week 5: 类型推断抽象 (Day 30-35)
- Week 6: 完成抽象提取 (Day 36-40)

#### **阶段 3: 架构优化 (Week 7-10)**
- Week 7-8: 洋葱架构改造 (Day 41-50)
- Week 9-10: 性能优化 (Day 51-60)

### 长期规划

- **Q2 2026**: 实现完整的事件驱动架构
- **Q3 2026**: 引入并行类型检查
- **Q4 2026**: 支持增量编译缓存

---

## 🔄 依赖关系优化

### 当前问题

```
❌ 当前耦合 (存在问题)
lexer → parser → typecheck → const_eval
           ↓
        type_level (独立，但typecheck依赖)
```

### 重构后

```
✅ 新架构 (低耦合)
     ┌─────────────┐
     │ Frontend API│ ← 公共入口
     └──────┬──────┘
            │
     ┌──────▼──────┐
     │   Pipeline  │ ← 组装层
     └──────┬──────┘
            │
    ┌───────┴────────┐
    ▼                ▼
┌────────┐     ┌──────────┐
│ Core   │     │ Shared   │ ← 无循环依赖
│ Layer  │     │ Utilities│
└───┬────┘     └──────────┘
    │
    ▼
┌──────────┐
│  Types   │ ← 纯算法，无副作用
└──────────┘
```

### 依赖注入 (Dependency Injection)

**消除硬编码依赖**:
- 修改结构体使用泛型参数
- 依赖抽象而非具体实现
- 提高可测试性和可替换性

---

## 📊 预期收益

### 可维护性提升

| 指标 | 重构前 | 重构后 | 提升 |
|------|--------|--------|------|
| 最大文件行数 | 1948行 (types.rs) | <500行 | **74%** ↓ |
| 模块数量 | 4个主要模块 | 15+个细分模块 | **3.75倍** ↑ |
| 文件平均行数 | 856行 | 320行 | **63%** ↓ |
| 重复代码量 | ~2000行 | <200行 | **90%** ↓ |

### 性能优化

| 优化项 | 预期收益 | 实现方式 |
|--------|----------|----------|
| 增量编译 | 编译时间减少20% | 缓存+依赖跟踪 |
| 内存使用 | 减少15% | Arena分配器 |
| 并行类型检查 | 提升30% | 无锁数据结构 |
| 模块缓存 | 冷启动减少40% | 编译时缓存 |

### 开发效率

| 场景 | 重构前 | 重构后 |
|------|--------|--------|
| 新增语言特性 | 需要修改3-5个文件 | 仅需修改1-2个文件 |
| Bug修复定位 | 平均20分钟 | 平均5分钟 |
| 新人上手 | 2周熟悉 | 3天熟悉 |
| 代码审查 | 1小时审查一个大文件 | 15分钟审查清晰模块 |

---

## ⚠️ 风险评估与缓解

### 🔴 高风险 (需要预案)

#### **风险1: 拆分types.rs引入bug**

**影响**: 类型系统核心，可能导致编译错误

**缓解策略**:
- 分步迁移: 创建新模块，保持旧文件 → 逐步迁移功能 → 删除旧文件
- 双重编译: 使用feature flag启用新类型系统
- 回归测试: 每个拆分步骤后运行完整测试套件

#### **风险2: 性能回退**

**影响**: 拆分可能引入额外抽象层，影响性能

**缓解策略**:
- 性能基准测试: 每个阶段前后对比性能
- 启用优化编译: 在性能关键路径使用 #[inline]
- LTO优化: 启用链接时优化

### 🟡 中风险

#### **风险3: 测试覆盖不足**

**影响**: 重构后出现回归bug

**缓解策略**:
- 每个阶段完成后运行完整测试套件
- 新增集成测试覆盖模块间交互
- 引入Property-based Testing验证类型系统

### 🟢 低风险

#### **风险4: 编译错误**

**影响**: 短期编译失败

**缓解策略**:
- 每个文件单独编译验证
- 使用feature flag逐步启用新架构
- 保持旧接口直到完全迁移

---

## 🛠️ 实施指南

### 开发环境配置

- 配置Cargo.toml新增工作空间成员
- 配置feature flags支持渐进式迁移
- 配置独立的目标目录避免冲突

### 代码审查检查清单

**每个PR必须满足**:
- [ ] 所有测试通过 (包括新增测试)
- [ ] 代码覆盖率达到85%+
- [ ] 文档更新 (API docs)
- [ ] 无新的clippy警告
- [ ] 性能基准无明显退化
- [ ] 依赖关系图无循环依赖

### 迁移工具

- 自动更新import路径的工具脚本
- 检查循环依赖的工具脚本
- 验证模块独立性的测试脚本

---

## 📈 监控与度量

### 关键指标 (KPIs)

| 指标 | 当前值 | 目标值 | 监控方式 |
|------|--------|--------|----------|
| 最大文件行数 | 1948 | <500 | weekly_cloc |
| 平均模块复杂度 | 8.5 | <3.0 | cargo-cyclonedds |
| 编译时间 | 45s | <30s | ci_build_time |
| 测试覆盖率 | 72% | 85%+ | tarpaulin |
| 代码重复率 | 12% | <2% | cpd |

### 质量门禁

**PR合并前自动检查**:
- 检查文件大小是否超限
- 检查循环依赖
- 运行测试覆盖率检查

---

## 🎯 总结与下一步

### 重构收益总结

✅ **架构清晰**: 从4个混乱模块 → 15+个清晰模块
✅ **低耦合**: 消除循环依赖，实现依赖倒置
✅ **高内聚**: 每个模块职责单一，<500行
✅ **易扩展**: 为RFC-012等新特性预留空间
✅ **高性能**: 增量编译+缓存+并行处理

### 分阶段实施路径

#### **阶段 1: 紧急拆分 (Week 1-3)**
- Week 1: 拆分 types.rs → 4个模块
- Week 2: 拆分 lexer/mod.rs → 3个模块
- Week 3: 拆分 parser/stmt.rs → 3个模块

#### **阶段 2: 抽象提取 (Week 4-6)**
- Week 4: 统一错误处理系统
- Week 5: 类型推断抽象
- Week 6: 完成抽象提取

#### **阶段 3: 架构优化 (Week 7-10)**
- Week 7-8: 洋葱架构改造
- Week 9-10: 性能优化

### 长期规划

- **Q2 2026**: 实现完整的事件驱动架构
- **Q3 2026**: 引入并行类型检查
- **Q4 2026**: 支持增量编译缓存

---

## 🚀 立即行动

**现在开始实施:**

1. **执行准备步骤**:
   - 创建git分支进行重构
   - 创建新目录结构
   - 运行测试基准

2. **开始第一阶段**:
   - 分析原始文件结构
   - 创建新模块文件
   - 逐步迁移代码

3. **持续验证**:
   - 每完成一个小步骤就测试
   - 确保不会破坏现有功能
   - 记录问题和解决方案

**记住**: 采用渐进式重构，确保每个阶段都经过充分测试和验证！

---

> **注意**: 这是一个激进但可行的重构方案。建议采用渐进式迁移，确保每个阶段都经过充分测试和验证。重构过程中保持与团队的密切沟通，及时调整方案。

**文档版本**: 2.0
**最后更新**: 2026-01-27
**下次审查**: 2026-02-03