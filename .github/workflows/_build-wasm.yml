name: Build Wasm (Reusable)

on:
  workflow_call:
    inputs:
      artifact-name:
        description: 'Artifact name'
        type: string
        default: 'yaoxiang-wasm'
      retention-days:
        description: 'Artifact retention days'
        type: number
        default: 30

env:
  CARGO_TERM_COLOR: always

jobs:
  build:
    name: Build Wasm
    runs-on: ubuntu-latest
    timeout-minutes: 15
    # 跨 run 下载 Z3 wasm（dawidd6/action-download-artifact）需 actions: read。
    # 显式声明而非依赖仓库默认：下载失败有 continue-on-error + 降级分支，
    # 权限不够时会静默变成「Z3 features disabled」而不报错。
    permissions:
      contents: read
      actions: read
    steps:
      - uses: actions/checkout@v7

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: ${{ vars.RUST_TOOLCHAIN }}
          targets: wasm32-unknown-unknown

      - name: Install wasm-pack
        run: curl https://rustwasm.github.io/wasm-pack/installer/init.sh -sSf | sh

      - name: Download Z3 wasm (cross-run)
        uses: dawidd6/action-download-artifact@v24
        with:
          workflow: _build-z3-wasm.yml
          name: z3-wasm-4.16.0
          path: .z3/z3-4.16.0-wasm
        continue-on-error: true

      - name: Verify Z3 wasm
        run: |
          if [ -f ".z3/z3-4.16.0-wasm/lib/libz3.a" ]; then
            echo "Z3 wasm found"
          else
            echo "Z3 wasm not found, Z3 features disabled"
          fi

      - name: Cache cargo
        uses: actions/cache@v6
        with:
          path: |
            ~/.cargo/registry/index
            ~/.cargo/registry/cache
            ~/.cargo/git/db
            target
          key: wasm-cargo-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            wasm-cargo-

      - name: Build Wasm
        run: cd wasm && wasm-pack build --target web --out-name yaoxiang --out-dir ../pkg

      - name: Package wasm
        run: cd pkg && zip -r ../yaoxiang-wasm.zip .

      - name: Upload wasm artifact
        uses: actions/upload-artifact@v7
        with:
          name: ${{ inputs.artifact-name }}
          path: yaoxiang-wasm.zip
          retention-days: ${{ inputs.retention-days }}
