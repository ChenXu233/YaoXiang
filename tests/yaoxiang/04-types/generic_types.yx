// 04-types/generic_types.yx
// 覆盖: 规范 §3.8 泛型类型
// 验证: Option: (T: Type) -> Type = { some(T) | none }
// 状态: 🔴 未实现 - 泛型类型在运行时未支持

// Option: (T: Type) -> Type = { some(T) | none }
// x = Option.some(42)

use std.io
main = {
    io.println("ALL TESTS PASSED")
}
