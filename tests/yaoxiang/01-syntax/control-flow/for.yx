// 01-syntax/control-flow/for.yx
// 覆盖: 规范 §5.9 for 语句
// 验证: for 循环遍历
// 状态: ✅ 可运行

use std.assert

main = {
    mut items = [10, 20, 30]

    // for 累加
    mut sum = 0
    for n in items {
        sum = sum + n
    }
    assert.assert(sum == 60, "for sum 10+20+30 == 60")
}
