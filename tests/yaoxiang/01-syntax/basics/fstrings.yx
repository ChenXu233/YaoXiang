// 01-syntax/basics/fstrings.yx
// 覆盖: 规范 RFC-012 F-String 模板字符串
// 验证: f"hello {name}" 插值
// 状态: ✅ 可运行

use std.assert

main = {
    name = "world"
    msg = f"hello {name}"
    assert.assert(msg == "hello world", "f-string name == world")

    x = 42
    val = f"value: {x}"
    assert.assert(val == "value: 42", "f-string x == 42")
}
