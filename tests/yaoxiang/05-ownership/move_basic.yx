// 05-ownership/move_basic.yx
// 验证: Move 语义 — 赋值后原变量不可用
// 状态: 待测试

use std.assert

Point: Type = { x: Int, y: Int }

main = {
    p = Point(1, 2)
    p2 = p
    assert.assert(true, "move completed — p2 should own the value")
}
