// 测试: spawn + 解构赋值

use std.io

main = {
    result = spawn {
        t1 = 1 + 1
        t2 = 2 + 2
        return (t1, t2)
    }
    a, b = result
    io.println(a)
    io.println(b)
    io.println("ALL TESTS PASSED")
}
