// 09-concurrency/spawn_basic.yx
// 验证: spawn {} 块 — 直接子表达式并行执行
// 状态: 待测试

use std.io

main = {
    (a, b) = spawn {
        t1 = 1 + 1
        t2 = 2 + 2
        return (t1, t2)
    }
    io.println(a)
    io.println(b)
    io.println("ALL TESTS PASSED")
}
