// 09-concurrency/spawn_for.yx
// 验证: spawn for 数据并行循环（RFC-024 §2.6）

use std.io

main = {
    items = [1, 2, 3, 4, 5]
    results = spawn for item in items {
        return item * 2
    }
    io.println(results)
    io.println("ALL TESTS PASSED")
}
