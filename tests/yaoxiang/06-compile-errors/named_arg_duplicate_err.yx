// 06-compile-errors/named_arg_duplicate_err.yx
// 同一形参被位置实参与命名实参同时指定应报 E1015
// expect: compile-error E1015
// 覆盖: 命名参数（教程 §命名参数「位置参数必须在前面」的必然推论）
// 验证: `add(1, a = 2)` 报 E1015（a 既由位置传入又由命名传入）
// 状态: ✅
//
// 位置实参按序占前导槽位、命名实参按名入槽，两者撞同一槽位时若放任，
// 后写者覆盖先写者——静默取一个值，无从判断该用哪个。必须报错。

add: (a: Int, b: Int) -> Int = a + b

main: () -> Void = {
    x = add(1, a = 2)   // ❌ a 被指定两次
}
