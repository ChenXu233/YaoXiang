// 06-compile-errors/index_oob_read.yx
// [test:error]: 列表越界读取应报运行时错误（#279：原静默返回 void、EXIT=0）
// 覆盖: RFC-013 错误码规范（Runtime index out of bounds 条目）+ #279
// 验证: items[5] 在 length=3 的列表上报越界错误，不再静默返回 void
// 状态: ❌ 应运行失败

use std.io

main = {
    items = [1, 2, 3]
    io.println(items[5])
}
