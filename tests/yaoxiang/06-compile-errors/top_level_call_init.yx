// 06-compile-errors/top_level_call_init.yx
// [test:error]: 顶层绑定 init 是函数调用 → E3007（#271 清单 #2，静默填 0 → 硬错误）
// 验证: 顶层绑定是编译期常量，不支持运行时计算的初始化
// 状态: 待测试

f: (Int) -> Int = (x) => x + 1

sum = f(1)

main = {
}
