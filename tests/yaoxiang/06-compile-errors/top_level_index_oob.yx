// 06-compile-errors/top_level_index_oob.yx
// expect: runtime-error E6003
// 覆盖: #368 Script 模式顶层语句的诊断可达性
// 验证: 顶层语句（编入合成函数 __yx_module_init）的越界索引报 E6003，
//       且带源码位置（--> 行）与索引变量名 (i)——此前两者都没有
// 状态: ❌ 应运行失败
//
// 注：本文件是**运行期失败测试**（`// expect: runtime-error`），判定契约是
// 「check 退出 0 + run 非 0 且输出含 E6003」，不执行 assert 路径——
// 语句在断言之前就已越界退出。故此处不写 assert.assert（与 TEST_STANDARDS
// §2.2 的三类判定对应：行为测试才需要显式断言）。

a = [1, 2, 3]
i = 5
print(a[i])
