// Bug 验证: `?` 作为参数类型被静默吞掉（#250）
// [test:error]: `?` 作为参数类型应报 E0012，不得静默吞掉（#250）
// `?` 是 try 运算符（表达式级，标准库规范 §1.4 ErrorPropagate），不是合法类型（类型系统规范 §1.1 TypeExpr）
// 解析器必须在定义处报错（E0012），而非静默丢弃整个顶层绑定
//
// 期望行为: 编译失败，报 E0012，错误指向 `(a: ?)` 中的 `?`
// 实际行为: 修复前零错误，`f` 绑定被静默丢弃，仅在调用点报 E1001（错误位置远离根因）

f: (a: ?) -> Void = (a) => {
    println("defined")
}
main = {
    println("main only")
}
