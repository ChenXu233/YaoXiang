// 00-smoke/hello.yx
// 覆盖: 规范 §1 - 最简可用性验证
// 验证: 基本程序能编译运行
// 状态: ✅ 可运行
//
// 注：空块 `{ }` 被解析为**空 Dict 字面量**（parser 的 `{}` 二义，见 #359），
// 故用 Fn 注解明确它是函数体（裁决 C：注解为 Fn → 函数）。

main: () -> Void = {
}

main()
