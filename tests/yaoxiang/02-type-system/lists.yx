// 02-type-system/lists.yx
// 覆盖: 规范 §2.6.4 集合字面量
// 验证: 列表字面量、索引访问、列表操作
// 状态: ✅ 可运行

use std.io

main = {
    mut items = [1, 2, 3]

    // 索引访问
    first = items[0]
    io.println(first)

    io.println("ALL TESTS PASSED")
}
