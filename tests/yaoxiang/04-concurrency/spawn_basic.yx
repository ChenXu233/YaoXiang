// 04-concurrency/spawn_basic.yx
// 验证: spawn {} 块 — 直接子表达式并行执行
// 状态: ✅ 可运行

use std.assert

main = {
    result = spawn {
        t1 = 1 + 1
        t2 = 2 + 2
        return t1 + t2
    }
    assert.assert(result == 6)
}
