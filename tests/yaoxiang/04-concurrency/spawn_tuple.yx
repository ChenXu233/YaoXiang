// 04-concurrency/spawn_tuple.yx
// 验证: spawn {} 返回元组

use std.assert

main = {
    result = spawn {
        v1 = 1 + 1
        v2 = 2 + 2
        return v1 * v2
    }
    assert.assert(result == 8)
}
