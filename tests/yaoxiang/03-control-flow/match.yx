// 03-control-flow/match.yx
// 覆盖: 规范 §4.8 模式匹配
// 验证: match 表达式 (value matching)
// 状态: ⚠️ 已知 bug - ir_gen 未处理 Match 节点，返回 0
// 期望行为:
//   match 1 { 1 => "one", _ => "other" } → "one"
// 当前行为:
//   match 1 { 1 => "one", _ => "other" } → 0

use std.io

main = {
    // TODO: 修复 match bug 后取消注释
    // desc = match 1 {
    //     1 => "one",
    //     _ => "other"
    // }
    // io.println(desc)

    io.println("ALL TESTS PASSED")
}
